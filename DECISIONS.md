# Decisions Log

Judgement calls made during the build, with a one-line rationale each, in chronological order.

- **PHP runtime source**: PHP/Composer were not on PATH. `winget install PHP.PHP.8.3` failed (installer archive for that exact patch version 404'd on php.net); `PHP.PHP.8.4` installed successfully via winget. Separately discovered a pre-existing **Laravel Herd** install (`~/.config/herd/bin`) providing PHP 8.4.23 + Composer 2.10.1 + the `laravel` installer — using Herd's toolchain as the primary one since it's a complete, already-configured Laravel dev environment. Thin wrapper scripts were added at `~/bin/php` and `~/bin/composer` so the POSIX (bash) tool can resolve the `.bat`-wrapped Herd binaries. This is a local dev detail only — the cPanel deploy target still runs PHP 8.3 per `docs/DEPLOY.md`, and Laravel 12 supports 8.2+, so there's no compatibility concern.
- **Directory layout**: followed the spec's exact tree. `api/` and `app/` are populated by their respective official scaffolding tools (`composer create-project laravel/laravel`, `flutter create`) rather than hand-built, so their internals (e.g. Laravel's default `bootstrap/`, `config/`) follow framework convention beyond what the spec enumerates.
- **Framework/package majors newer than the spec named**: the spec was written assuming Laravel 12, Filament v3, and Riverpod 2 as current; by the time of this build the actual latest-stable versions are **Laravel 13.25**, **Filament v5.7**, and **Riverpod 3.3 / riverpod_annotation 4.0**. Installed latest-stable throughout rather than force-pinning to the older majors the spec named, since (a) `composer create-project laravel/laravel` and `flutter pub add <pkg>` both resolve to current-latest by default, (b) Filament v3 is incompatible with the `symfony/console` v8 that ships with Laravel 13 (see below), and (c) fighting the resolver back to years-old majors would mean losing security patches for no benefit — the spec's intent ("don't re-litigate the stack") is about *which* framework, not pinning to a specific point-in-time minor. All spec-described behavior (Sanctum tokens, Filament admin resources, Riverpod providers/generators) works the same shape across these majors.
- **Filament version conflict**: `composer require filament/filament:^3` failed — Filament v3's `filament/support` requires `symfony/console ^6.0|^7.0`, but Laravel 13's dependency tree already pins `symfony/console` to v8.1.4, so v3 could never be installed alongside a fresh Laravel 13 app. Installed the latest Filament major (v5.7.6) unpinned instead, which resolves cleanly.
- **flutter_riverpod resolved to v3.x**: the spec says "Riverpod 2 + generator," but Riverpod 2 is no longer the version `flutter pub add` resolves to (latest stable is 3.3.2, with `riverpod_annotation` 4.0.3). Kept latest-stable per the framework-major note above.
- **win32 dependency conflict (Flutter)**: `file_picker`, `share_plus`, and `wakelock_plus` (a transitive dep of `chewie`) each pin incompatible major ranges of the Windows-desktop-only `win32` package (5.x vs 6.x). Since this app only targets Android/iOS (`flutter create --platforms android,ios`), the actual win32 FFI bindings are never compiled into the app. Added a `dependency_overrides: win32: ^6.3.0` in `app/pubspec.yaml` to settle pub's version bookkeeping — this only affects dependency resolution, not the Android/iOS build output.
- **Dropped `custom_lint` + `riverpod_lint` dev dependencies**: every version of `custom_lint` requires `freezed_annotation ^2.2.0`, which conflicts with the `freezed_annotation ^3.x` that current `freezed` (needed for the app's models) requires — a real, currently-unresolved incompatibility in the package ecosystem, not something fixable by version-juggling on our end. Baseline linting still comes from `flutter_lints` (already included, rules tightened in `analysis_options.yaml`). If the ecosystem catches up, these can be added back.
- **Flutter SDK upgraded**: the pre-installed Flutter (3.41.6, Dart 3.11.4) was too old for `riverpod_generator` (needs Dart SDK ≥3.12 as of its recent majors) and conflicted with `geocoding ^5.0.0`'s analyzer/dart_style toolchain deps. Ran `flutter upgrade` to Flutter 3.47.0 / Dart 3.13.0, which resolved both cleanly.
- **Brand logo duplicated into `app/assets/brand/`**: the spec's canonical asset lives at repo-root `assets/brand/sokoni_logo.png`, but Flutter's `pubspec.yaml` can only bundle assets that live inside the Flutter package itself — it can't reference a parent directory. Copied the logo into `app/assets/brand/sokoni_logo.png` for the app to actually bundle (used by `flutter_launcher_icons`, `flutter_native_splash`, and the splash primitive); the repo-root copy stays the canonical source referenced by the spec and design docs. If the logo changes, both copies need updating.
- **No real brand logo supplied**: none was found in the workspace or Downloads, so one was generated programmatically (a simple yellow shopping-bag mark with a black "S" glyph on a black rounded-square background, using the spec's exact palette values) via a small Python PNG writer, since no image-editing tool was available. This is a placeholder pending a real logo from the client — swap `assets/brand/sokoni_logo.png` (both copies) and re-run `flutter pub run flutter_launcher_icons` / `flutter pub run flutter_native_splash:create` when one arrives.
- **Motion primitive bug caught by the widget test**: `SokoniSplashTransition`, `StaggeredReveal`, and the success-checkmark overlay all originally called `MediaQuery.disableAnimationsOf(context)` inside `initState()`, which Flutter forbids (inherited-widget lookups must happen in `build()` or `didChangeDependencies()` — the element isn't fully mounted yet during `initState`). This only surfaces at runtime, not via `flutter analyze`; the Phase 1 widget test caught it immediately. Fixed by moving the check into `didChangeDependencies()` guarded by a `_started` flag so it only runs once.
- **Geo storage on `seller_profiles`**: the spec's data model lists `shop_location(POINT, SPATIAL INDEX)`, but that's MySQL-only and unusable for local dev/tests on SQLite. Stored `lat`/`lng` decimals as the portable source of truth (what the app actually reads/writes), and — only when the connection driver is MySQL — added a generated `shop_location POINT` column with a spatial index derived from them via raw SQL in the migration. `App\Services\Geo\DistanceQuery` uses `ST_Distance_Sphere` on MySQL and a PHP Haversine calculation over lat/lng on SQLite, so radius search is correct either way. `orders.delivery_location` similarly became plain `delivery_lat`/`delivery_lng` — it's stored data, not a query target, so it didn't need the spatial treatment at all.
- **`products.title`/`description` full-text index is MySQL-only**: SQLite's schema builder throws `RuntimeException` on `Blueprint::fullText()`. Guarded it behind a driver check in the migration; `Product::scopeSearch()` uses `whereFullText` on MySQL and falls back to a `LIKE '%term%'` query on SQLite.
- **Seed data: 70 orders, not the spec's 40** — see the seeder's own docblock: 60 reviews require 60 *unique* completed orders (`reviews.order_id` is unique — the actual anti-fake-review mechanism), which 40 total orders can't supply alongside pending/accepted/ready/cancelled examples for a realistic demo.
- **Social login verifies tokens server-side for real** (`HttpSocialAuthVerifier`): Google via `tokeninfo` audience check, Facebook via `debug_token`, Apple via JWKS signature verification (`firebase/php-jwt`, already a transitive dependency via kreait/laravel-firebase). All three make genuine HTTP/JWT calls and fail closed with `InvalidSocialTokenException` until real client IDs are configured (BLOCKERS.md item 3) — never trust a client-supplied `provider_id` directly.
- **Several real bugs caught by manual smoke-testing before writing formal tests** (all fixed, see commit): (1) `App\Http\Controllers\Controller` was missing `AuthorizesRequests`, so every `$this->authorize()` call in the app threw `Call to undefined method`. (2) `Order`/`Review` status-transition fields (`status`, `accepted_at`/etc., `reply`/`replied_at`) were deliberately left out of each model's `#[Fillable]` (so they can't be mass-assigned by a buyer), but the controllers were then mass-assigning them via `->update()`, which Eloquent silently no-ops instead of erroring — fixed by using `forceFill()` for those specific, validated, server-controlled transitions. (3) `$request->string('delivery_method') === 'delivery'` was always false — `->string()` returns a `Stringable` object, never `===` to a plain string; switched to `->input()`. (4) `User::isSeller()`/`isVerifiedSeller()` and several policy/controller checks read the lazy `sellerProfile` relation property, which can be cached as `null` from an earlier access in the same request/object lifetime (e.g. rendering `UserResource` before a seller profile existed) and then goes stale once one is created — switched every authorization-relevant check to a fresh `sellerProfile()` query (`User::sellerProfileId()` helper). This class of bug is real in any long-lived object context (queued jobs reusing models, Laravel's own `actingAs()` test helper), even though ordinary per-request production traffic re-resolves the user fresh each time.
- **Tests authenticate via `Sanctum::actingAs()`, not the base `actingAs()`**: overridden once in `Tests\TestCase` rather than per test file. The base Laravel `actingAs()` sets an authenticated user without a token context, so `$request->user()->currentAccessToken()` (used by `AuthController::logout`) is `null` in tests even though a real bearer-token request always has one — confirmed the real endpoint works correctly via a manual `curl` request with an actual token before concluding this was test-only.
- **Riverpod providers hand-written, not `@riverpod`-generated, for core infra** (`core/providers.dart`: Dio, drift, secure storage, auth state, connectivity): these are plain singleton services with no need for the generator's family/auto-dispose ergonomics, and skipping codegen here avoids a build_runner round-trip for infrastructure that rarely changes. Reserving `riverpod_generator` for feature-level data providers (Phase 4+) where its caching actually earns its keep.
- **`data/api` uses retrofit as the spec's file tree specifies**, but two `AuthApi` methods (`me()`, `acceptTerms()`) are typed `Future<dynamic>` rather than `Future<Map<String, dynamic>>`: retrofit_generator mis-generates a `dynamic.fromJson(...)` call (which doesn't exist) when the declared return type is literally `Map<String, dynamic>`. `dynamic` sidesteps the codegen bug; the repository still safely indexes `json['data']` either way.
- **Auth UI pulled slightly ahead of Phase 6**: rather than leave the Profile/Orders/Sell tabs' "Sign in" buttons non-functional until the seller-onboarding phase, built a minimal but fully working phone-OTP sign-in bottom sheet now (`features/auth/presentation/phone_sign_in_sheet.dart`) against the Phase 2 API. Google/Facebook/Apple buttons and the 4-step seller wizard remain Phase 6 — a dead button would have been worse than a real slice of auth built early.
- **go_router's `StatefulShellRoute.indexedStack()` factory doesn't expose `navigatorContainerBuilder`** (it's hardcoded to a plain, non-animated `IndexedStack`), so the fade-through tab-switch motion primitive needs the base `StatefulShellRoute(...)` constructor instead, with a custom `navigatorContainerBuilder` (`FadeThroughIndexedStack` in `app_shell.dart`) that fades the container on branch change while `IndexedStack` still keeps every branch's Navigator/scroll state alive underneath.
- **Nav bar item index ≠ shell branch index**: the 5 router branches are `[home, search, sell, orders, profile]`, but `AnimatedNavBar` only renders 4 tab icons (Sell is the centre FAB instead), so item index 2 in the nav bar is "orders" while branch index 2 is "sell". `AppShell` maps between them explicitly (`_navItemBranchIndexes`) rather than assuming they line up — passing the raw tab index straight to `goBranch()` would have silently opened the wrong tab from "Orders" onward.
- **`sqlite3_flutter_libs` is now a no-op stub package** (kept only so other packages can still depend on `0.6.0` after the underlying `sqlite3` package went to v3.x, which bundles its own native-library loading) — its `applyWorkaroundToOpenSqlite3OnOldAndroidVersions()` function no longer exists. Removed the call; drift's `NativeDatabase.createInBackground` works without it on the current sqlite3.
- **`SellerSummaryResource` gained `lat`/`lng`**: the discovery map view (CLAUDE.md feature 1) needs real seller coordinates to place pins. The first pass jittered fake pin positions around the search centre to work around `ProductResource` only exposing `distance_km`; that produced pins at *wrong* locations, which is worse than deferring the feature — a genuinely misleading map is not "a real implementation with a mocked external call," the bar the spec sets for what's allowed to be incomplete. Fixed at the source instead: `seller_profiles.lat`/`lng` were already stored, just not serialized on the compact seller embed, so exposing them (nullable, only when set) was the correct fix rather than a client-side workaround.
- **Marker clustering deferred**: `google_maps_flutter` has no built-in pin clustering; the discovery map places one real, correctly-positioned marker per seller, but doesn't visually merge nearby pins at low zoom. A dedicated clustering package is a reasonable follow-up, not urgent enough to pull into this build alongside everything else in Phase 4.
- **`ProductCard` widget test wraps it in a fixed-aspect-ratio `SizedBox`**, not a bare `Scaffold`: the card's square cover image plus its text block only fit within the `childAspectRatio: 0.62` grid cell the discovery/search grids actually give it (see `home_screen.dart`/`search_screen.dart`) — an unconstrained parent height correctly overflows, since that's not how the widget is ever really used. The first version of the test used a bare `Scaffold` and caught this immediately.
- **Widget tests override `dioProvider` with a fail-fast `HttpClientAdapter`** rather than leaving real network calls unmocked: any screen that fires a provider-driven API call on build (the home feed does, immediately) leaves a pending `Timer`/microtask when `flutter test` tears down before a real request would resolve or time out, which `flutter test` treats as a hard failure ("A Timer is still pending"). Failing the request synchronously exercises `ProductRepository`'s existing offline-cache fallback path instead, which is both faster and closer to a real "offline" scenario than an unmocked hang.
- **`SellerSummaryResource` gained `whatsapp`** (toggleable via the existing `show_whatsapp` flag), for the same reason it gained `lat`/`lng` in Phase 4: the WhatsApp deep-link button on the product detail page needs the seller's real number. The first draft of that button used the seller's database *id* as the phone number — caught immediately since it's an obviously-wrong value, but it's the same underlying pattern as the map-pin bug: a compact embed missing a field the UI actually needs, worth fixing at the API rather than routing around client-side.
- **Added `Product::scopeForSeller()` + a `seller_id` filter on `GET /products`**: the shop profile's Products/Gallery tabs need one seller's products, and the discovery endpoint had no way to ask for that. Small, backward-compatible addition (defaults to no filtering) rather than a separate endpoint, consistent with how `category_id`/`q`/`sort` already work.
- **Every `Navigator.of(context).pushNamed(...)` call written during this phase was a bug**: this app uses go_router exclusively (`MaterialApp.router`) with no named-route table configured via `MaterialApp.routes`, so `pushNamed` would have failed at runtime for every "tap a product/shop/message" action — analyze doesn't catch this since `pushNamed` is a real, valid `Navigator` method, just one this app never wires up. Replaced with `context.push(SokoniRoutes.xxx(...))` throughout and added the missing route definitions (`/products/:id`, `/shop/:handle`, `/conversations/:id`, `/favorites`) to `app_router.dart`. Worth flagging in case the same mistake recurs in Phase 6+.
- **`geocoding` v5's public API is instance-based, not top-level functions**: earlier Sokoni code assumed a free `placemarkFromCoordinates(...)` function (the pre-v5 API); v5 requires constructing a `Geocoding()` instance and calling the method on it. Fixed in the onboarding location step (`step2_location.dart`) rather than pinning an older `geocoding` version, since the instance API is otherwise equivalent.
- **`file_picker`'s Windows implementation (11.0.3, the current latest) is written against `win32` 5.x's API and fails to compile under `win32` 6.x** (`HRESULT` became a distinct extension type, `convertToIID` was renamed, `IShellItem.getDisplayName`'s signature changed) — a real upstream incompatibility, not just a semver-range mismatch. The `win32: ^6.3.0` override added earlier (to reconcile `file_picker`/`share_plus`/`wakelock_plus`'s differing win32 ranges) satisfied pub's version solver but silently broke `file_picker`'s actual Windows source, which only surfaces when something forces a full compile of the host-platform plugin registrant — `flutter analyze` doesn't do this, but `flutter test` does (it always builds for the Windows host on this dev machine, even though the app itself only targets Android/iOS and has no `windows/` platform folder). Fixed by lowering the override to `win32: ^5.9.0`; pub then resolves compatible (older) `share_plus`/`wakelock_plus` versions automatically, and all three packages' actual Windows source compiles cleanly again. Purely a local dev-loop fix — irrelevant to the shipped Android/iOS build either way — but needed for `flutter test` to run at all on this machine.
- **`FilePicker`'s static entry point in v11 is `FilePicker.pickFiles(...)` directly**, not `FilePicker.platform.pickFiles(...)` (the pre-federated-plugin-refactor API) — used directly in the licence-upload step (`step4_licence.dart`).
- **`product_media` gained a `card_path` column**: CLAUDE.md's media feature explicitly calls for three generated sizes ("thumb 300, card 800, full 1600"), but the original migration only had two URL columns (`path`, `thumb_path`). Rather than silently dropping the middle size to fit the existing schema, added `card_path` via a new migration — `path` is the 1600px full/detail-carousel image, `card_path` the 800px general-purpose size, `thumb_path` the 300px grid/list thumbnail. Video rows only populate `thumb_path` (a poster frame) since there's no "full-size video" concept.
- **Video thumbnails are supplied by the client, not generated server-side**: `ProductMediaStoreRequest` requires a `thumbnail` image alongside any video upload. Generating a poster frame from video server-side needs an `ffmpeg` binary, which isn't guaranteed (or installable) on Namecheap shared hosting — the deploy target this app is built for. The client already compresses video with `video_compress` before upload (CLAUDE.md feature 7), and that package can extract a frame directly, so producing the poster client-side avoids a server dependency the production host can't reliably provide.
- **`product_media.path`/`thumb_path`/`card_path` store full public URLs, not disk-relative paths**: matches the existing seeded convention (`ProductMediaFactory` already stores absolute `picsum.photos` URLs) and what the Flutter client already assumes (`ProductCard`/`ProductMediaCarousel` pass these fields straight to `CachedNetworkImage` with no base-URL prefixing). `ProductMediaController` stores via `Storage::disk('public')->url(...)` accordingly.
- **Added `GET /shop/products`** (mirrors the existing `GET /shop/orders` pattern): the public `GET /products` index always applies `Product::visible()`, which a seller's own My Shop dashboard can't use — a pending or newly-added-but-inactive product needs to show up on the seller's own dashboard even though it's correctly hidden from public discovery (CLAUDE.md feature 4).
- **`OrderResource` gained `conversation_id`**: CLAUDE.md feature 8 says the order thread "opens automatically in chat" on placement — the client needs the new conversation's id right out of the checkout response to navigate straight there, rather than making a second round-trip to list conversations and match by `order_id`.
- **`ProductResource` gained `is_active`/`is_hidden`**: neither was exposed to clients before Phase 7, which was fine while nothing needed them, but the My Shop dashboard is the seller's own view of their products and has to be able to show "this one's inactive" / explain why something isn't showing in the public feed (e.g. auto-hidden after three upheld reports, CLAUDE.md feature 11) — information the owner needs that a stranger browsing the public feed doesn't.
- **Cart is a plain in-memory Riverpod `Notifier`, not persisted to drift**: CLAUDE.md's cart is explicitly single-seller and checkout-in-one-sitting (no saved-for-later concept anywhere in the spec), so there's no real scenario where surviving an app restart matters enough to justify a persistence layer for it — losing an in-progress cart on a kill is an acceptable, standard mobile-commerce trade-off here.
- **Riverpod gotcha, caught before shipping**: `ref.watch(cartProvider.notifier).subtotal` does *not* rebuild the widget when cart contents change — watching `.notifier` subscribes to the controller instance (which never changes for a `Notifier`), not its state. Any derived value read off a controller needs the *state* provider (`ref.watch(cartProvider)`) watched directly and computed from that. Fixed in both `cart_screen.dart` and `checkout_screen.dart` before it shipped as a "totals never update" bug — worth remembering for any future derived-value read off a Notifier.
- **Video thumbnails, again, client-side**: the product-form's video picker uses `video_compress`'s `getFileThumbnail()` to produce the poster frame it uploads alongside the compressed clip — same reasoning as the server-side decision above (no `ffmpeg` on the shared-hosting deploy target), just the client half of it.
- **"Am I the buyer or the seller on this order" is derived by comparing `currentUser.id == order.buyer.id`**, not by comparing seller-profile ids: `UserResource` doesn't expose a seller-profile id (no client-side use for it existed before Phase 7), and the backend's `OrderPolicy::view` already guarantees only the buyer or the owning seller can ever fetch an order in the first place — so "not the buyer" reliably means "the seller" for anyone who can see the order at all, without needing a new field.
- **Firebase push registration is fully implemented client-side but fails soft with no project configured yet** (BLOCKERS.md item 1): `PushService.registerDevice()` calls the real `firebase_core`/`firebase_messaging` APIs and POSTs a real token to `/devices`, triggered on every successful sign-in. With no `google-services.json`/`GoogleService-Info.plist`, `Firebase.initializeApp()` throws on the very first call (there's no native default to fall back to) — caught and logged there, so the rest of the app is unaffected; chat still works via polling regardless of push. Found and corrected a stale BLOCKERS.md claim from an earlier session that this was already wired behind a `firebase_options.dart` — it wasn't; nothing under `core/push/` existed until this phase.
- **A typing indicator over 5s polling is implemented as a real per-party "typing until" timestamp**, not simulated client-side: `conversations` gained `buyer_typing_until`/`seller_typing_until` columns, refreshed via a small `PATCH /conversations/{id}/typing` the client calls (debounced to once per 3s) while the user types, and read back on the same poll cycle that already fetches messages every 5s. CLAUDE.md explicitly lists a typing indicator as a chat feature despite the no-WebSocket architecture, and "no placeholder code" rules out faking it locally with no signal from the other device — this is the honest real-time-with-latency equivalent, accurate to within the existing 5s poll cadence rather than instant.
- **Chat image attachments and product-media are stored the same way**: `MessageController::store` now runs the uploaded attachment through `Storage::disk('public')->url()` before saving, matching `ProductMediaController`'s convention — previously it stored the bare disk-relative path (`chat/xxx.jpg`), which the client can't load directly since it has no notion of the storage disk's base URL. Caught while wiring the Phase 8 chat thread UI, which would otherwise have rendered broken images for every attachment.
- **`UserResource` gained `seller_handle`**: the shop review tab needs to know whether the currently signed-in user *is* the shop's owner (to show/hide the "Reply" button), and the only clean signal for that already used elsewhere (`order.buyer.id` comparison for orders) doesn't apply here — a review has no buyer/seller-relative viewer to compare against directly. Comparing the viewer's own `seller_handle` against the shop `handle` being viewed is a direct, honest check reusing a field-per-need pattern already established for `seller_status` on the same resource.
- **Riverpod family `AsyncNotifier` pattern**: `ChatThreadController` (one polling timer per open conversation) is a hand-written, non-codegen family notifier — `AsyncNotifierProvider.autoDispose.family<ChatThreadController, ThreadState, int>(ChatThreadController.new)`, where the family argument is threaded through the notifier's own constructor parameter (`ChatThreadController(this.conversationId)`), not through `build(arg)` — `AsyncNotifier.build()` stays zero-argument even for family notifiers in this (non-generator) API. First hand-written family notifier in the codebase; worth remembering the constructor-carries-the-arg shape if another one comes up.
- **Filament panel installed and built out in Phase 9**, not earlier: `composer.json` had `filament/filament` since Phase 0 and `User implements FilamentUser` (with `canAccessPanel()` gating on `is_admin`) was already written back in Phase 2 anticipating this, but `php artisan filament:install --panels` itself — the step that actually creates `AdminPanelProvider` and registers it in `bootstrap/providers.php` — had never been run. No admin UI existed until this phase despite the scaffolding being in place.
- **Generated Filament resources dropped their create/edit pages where there's no legitimate reason for an admin to hand-create that record**: `SellerProfileResource` and `ReportResource` are pure review-and-decide surfaces (sellers create profiles through the app's onboarding wizard; reports are only ever created by users through the app's report flow) — keeping Filament's auto-generated Create/Edit pages around would have let an admin "edit" fields like `status`/`verified_at` that aren't even mass-assignable (`SellerProfile`/`Report` deliberately exclude them from `#[Fillable]` for the same reason `Order`'s status fields are excluded), producing a form that silently no-ops on save. `UserResource` and `CategoryResource` kept theirs, since creating a staff/admin account or a new category are real, legitimate admin actions.
- **Seller verify/reject and the moderation queue's uphold/dismiss/hide/warn/suspend/ban all use `forceFill()` inside Filament `Action` closures**, not the framework's default record-update behavior: same reasoning as everywhere else in the codebase that a status transition is deliberately excluded from a model's `#[Fillable]` — these are validated, server-controlled transitions, not free-form field edits, so a plain `update()` call would silently drop them.
- **"Suspend" needed its own schema fields, not just reuse of `banned_at`**: CLAUDE.md feature 11 lists hide/warn/suspend/ban as four distinct admin actions, but the spec's own data model only persists `banned_at` — no way to distinguish a temporary suspension from a permanent ban, and no field to persist *why* (only an ephemeral push-notification body would carry the reason otherwise). Added `banned_until` (null = permanent ban, set = suspension expiring at that time) and `ban_reason` (persisted, not just pushed) — the same "action + persisted reason" pattern already used for `SellerProfile.rejection_reason` and `Order.cancelled_reason`. `User::isBanned()` now treats an expired `banned_until` as no-longer-banned automatically.
- **Suspending/banning a user also revokes their existing Sanctum tokens** (`$user->tokens()->delete()`), not just their ability to get a *new* one: `AuthController::issueToken()` already blocked banned users from signing in, but a user banned mid-session would have kept using their existing bearer token indefinitely otherwise — the ban wouldn't take effect until their token happened to expire.
- **`Report::offendingUser()` added to resolve "the user behind this content"** for warn/suspend/ban actions: each of the three reportable types (`Product`, `SellerProfile`, `Message`) reaches its owning user through a different relationship chain (`product.seller.user`, `sellerProfile.user`, `message.sender`), and the moderation queue needs one uniform way to act on "this account," regardless of what got reported.
- **Real bug caught by the new Filament panel tests**: `User::canAccessPanel(): bool { return $this->is_admin; }` throws a `TypeError` whenever `is_admin` reads as `null` in memory — which happens for any freshly-created model whose insert relied on the column's DB-level default rather than setting it explicitly (true of almost every seeded/factory user, since `UserFactory::definition()` never sets `is_admin`). Fixed with an explicit `(bool)` cast. This means every non-admin login attempt against the panel was one `TypeError` away from a 500 instead of a clean 403 — a real, previously-undetected bug, not a test-only artifact.
- **Filament panel tests need a dedicated `actingAsAdmin()` helper, distinct from the suite's global `actingAs()` override**: `Tests\TestCase::actingAs()` is globally routed through `Sanctum::actingAs()` (see the Phase 2 decision on this) because every API route uses the `sanctum` guard — but Filament's panel authenticates on the `web` session guard, so reusing that override left every panel-page test hitting a 302 redirect to `/admin/login` instead of the page. Added `actingAsAdmin()`, calling `parent::actingAs($user, 'web')` directly, alongside (not replacing) the existing override.
- **`phpunit.xml` needed a bumped `memory_limit`**: the full backend suite started hitting PHP's default 128M CLI limit once Filament's panel/resources are bootstrapped on every test alongside Intervention Image's GD driver decoding fake uploaded images (`ProductMediaTest`) — set to 512M via `<ini>` in `phpunit.xml`, scoped to test runs only.
- **Terms of Service and Privacy Policy content is real, versioned static text in the app, not fetched from the API or routed through the ARB catalog**: CLAUDE.md's data model records acceptance as a timestamp + version (`terms_accepted_at`/`terms_version`), not the document text itself, and long-form legal prose is a different kind of content than the UI microcopy the "never hardcode a user-facing string" rule is really about — most apps ship legal text this way rather than through their UI string catalog. Both documents are genuinely written for Sokoni's actual features (NIDA collection, location use, cash-on-delivery, moderation) in EN and SW, not placeholder lorem ipsum, and share one `sokoniTermsVersion` constant with the acceptance-gate screen so the sent version always matches what's displayed.
- **Terms acceptance is enforced once, client-side, right after every sign-in** (`ensureTermsAccepted()`, called from both the phone-OTP and social sign-in flows before their bottom sheet closes) — not via a server-side gate on other endpoints. Confirmed `terms_accepted_at` is read nowhere outside `AuthController` itself, so this is genuinely the only enforcement point; a `PopScope(canPop: false)` on the acceptance screen is what actually makes it non-skippable, not any server-side check.
- **Three more win32-version casualties, chased down for the Phase 10 release build**: `flutter build apk` type-checks the whole Dart dependency graph regardless of build target (confirmed by disabling the `enable-windows-desktop` Flutter feature flag entirely — made no difference), so any transitive Windows-only plugin implementation incompatible with whichever win32 line is pinned still fails the build on this Windows dev machine. Beyond `file_picker` (documented in Phase 6), `package_info_plus` ^10.x and `flutter_secure_storage_windows` (pulled in by `flutter_secure_storage` ^10.3.1) both require win32 ^6.0.1, conflicting with the ^5.9.0 pin `file_picker` needs. Rather than keep chasing whichever package needs which major version, pinned both to older lines that target win32 5.x instead: `package_info_plus: ^9.0.0` (transitive-only, no direct call site in this app) and `flutter_secure_storage: ^9.2.4` (down from ^11.0.0 originally, then ^10.3.1 after the compileSdk-37 fix — 9.2.4 is otherwise a drop-in for the plain read/write/delete API `SokoniSecureStorage` actually uses).
- **`android/build.gradle.kts` gained a `subprojects { afterEvaluate { ... } }` block forcing a single JVM 17 target across every module**: `flutter_facebook_auth` 6.0.4 (the version pubspec.yaml pins) ships an Android `build.gradle` with no `compileOptions`/`kotlinOptions` at all, so Gradle defaults its Java compilation to JVM 1.8 while its Kotlin compilation task picks up whatever JDK is actually running the build (21, via Android Studio's bundled JDK on this machine) — an "Inconsistent JVM Target Compatibility" hard failure. Rather than patch that one outdated plugin template (a newer 7.1.5 release has the identical gap), forced a consistent target for every subproject from the root build file — a more durable fix if another dependency has the same gap later. Had to land in the *first* `subprojects` block, before the existing `evaluationDependsOn(":app")` call: `afterEvaluate` throws if registered after a project has already been evaluated, and `evaluationDependsOn` forces exactly that.
- **`config/filesystems.php`'s `public` disk now reads its root/URL from `PUBLIC_UPLOADS_ROOT`/`PUBLIC_UPLOADS_URL` env vars**: `.env.example` already had a `PUBLIC_UPLOADS_URL` placeholder (anticipating the cPanel deploy runbook's "point the public disk at `/home/sokoftsn/public_html/uploads`, don't rely on `storage:link`" requirement — see CLAUDE.md) but nothing in `config/filesystems.php` actually read it; production would have silently kept writing to the default `storage/app/public` path no matter what `.env` said. Fixed while writing `docs/DEPLOY.md`, which is what surfaced the gap — the doc needed to reference a config that didn't exist yet.
- **A real release keystore was generated for this build** (`android/sokoni-release.jks`, alias `sokoni`, credentials in `android/key.properties` — both gitignored per the root `.gitignore`'s existing `*.jks`/`key.properties` rules). `android/app/build.gradle.kts` prefers this signing config when `key.properties` is present and falls back to debug signing otherwise, so a fresh checkout without the keystore still builds. This keystore is what Play Store update compatibility depends on going forward — losing it means the app can never be updated under the same listing again, so it needs a real secure backup outside this repo before shipping to production, not just the local `.gitignore` protection.
- **Another stale BLOCKERS.md claim found and corrected** (same pattern as the Firebase one caught in Phase 7): item 2 claimed a `MAPS_API_KEY=REPLACE_ME` placeholder was already wired into Android/iOS config — neither `AndroidManifest.xml` nor `AppDelegate.swift` actually had it. Added the real placeholders now (`com.google.android.geo.API_KEY` meta-data, `GMSServices.provideAPIKey(...)`), since without them the native Maps SDK fails to render tiles at all regardless of any Dart-side fallback state — `DiscoveryMapView`'s empty state only covers "no sellers have coordinates," not "the SDK itself has no key." Two for two on BLOCKERS.md entries that described work ahead of when it was actually done; worth treating every "Stub: X is already wired" claim in that file as unverified until re-checked against the actual files, not just trusted at face value.
- **Real brand logo dropped in post-launch, cropped into two derived assets rather than used as-is everywhere**: the client's real mark (a 1080×1080 lockup — bag+S glyph over a "Sokoni" wordmark, on black) arrived as a `.jpeg` at both canonical `assets/brand/sokoni_logo.png` paths (converted losslessly to PNG via a throwaway `dart:image` script — that package was added as a `dev_dependency`, used, then removed again rather than left in `pubspec.yaml`, since nothing at runtime needs it). Using the full lockup directly as `flutter_launcher_icons`' `image_path`/`adaptive_icon_foreground` produced a legible-nowhere launcher icon: the wordmark shrinks to an illegible smudge at mipmap sizes and gets partially clipped by the adaptive-icon mask's ~66% safe zone, which only crops the *center* of the square. Cropped a second asset, `sokoni_logo_icon.png` (bag+S glyph only, re-centered on a fresh black 1080×1080 canvas at ~55% fill so it sits safely inside the mask), and pointed both launcher-icon keys at that instead — the full lockup stays as `flutter_native_splash`'s source, where a full-screen splash has room for the wordmark and it reads fine. A pixel-value-as-alpha attempt at a transparent cutout (so the glyph could float over light backgrounds without its own black backing) was tried and discarded: the logo's black is load-bearing (it's the S-cutout's outline stroke, not just background), so keying it to transparent also erased the linework that defines the shape against anything lighter than black — unusable outside a pure-black backdrop. Went with the simpler, robust option instead.
- **`SokoniSplashTransition` and `SokoniBagSpinner` (pull-to-refresh) swapped their generic `Icons.shopping_bag_rounded` placeholder for the real `sokoni_logo_icon.png` crop**, now that it exists — both had been built against the old placeholder logo and used a stock Material icon as a stand-in for "the Sokoni bag mark" CLAUDE.md's motion spec calls for. Both wrap the image in a clip (`ClipRRect`/`ClipOval`) rather than swapping in a bare `Image.asset`, since the source PNG is an opaque black square — the clip is what gives the splash its rounded-square badge and the spinner its circular badge, and being opaque (not transparent) it stays legible over any background color the badge happens to sit on, including the white/light feed behind the refresh spinner.
- **Google Services Gradle plugin wired in for real** now that a real `google-services.json` exists (`app/android/app/google-services.json`, Firebase project `sokoni-71910`): `com.google.gms.google-services` version `4.4.2` declared `apply false` in `android/settings.gradle.kts`'s `plugins {}` block (alongside the existing AGP/Kotlin declarations there) and applied with no version in `android/app/build.gradle.kts`. Confirmed with a full `flutter build apk --debug` — the plugin resolves, downloads, and processes the config file with no errors, so FCM push registration (`push_service.dart`, already fully implemented since Phase 7) now actually runs on Android instead of failing at `Firebase.initializeApp()`. iOS still has no `GoogleService-Info.plist`, so it's unaffected and keeps failing the same soft, caught way as before.
- **Production MariaDB 11.4 rejects the `seller_profiles.shop_location` generated-column DDL** that works on real MySQL 8 — `POINT SRID 4326 GENERATED ALWAYS AS (...) STORED` is MySQL-only grammar, even though MariaDB reports the exact same `mysql` PDO driver name Laravel/the migration was branching on, so the existing `getDriverName() === 'mysql'` check couldn't tell them apart. Added `App\Services\Geo\DatabaseDialect::isMariaDb()`, which additionally runs `select version()` and checks the string for "MariaDB" (memoized per-process — the server's dialect can't change mid-request). The DDL itself got pulled out of the migration into `App\Services\Geo\ShopLocationSchema::statements(bool $isMariaDb)`, a pure function returning either the two original statements or an empty array, purely so the MariaDB-skip branch is unit-testable without a live MySQL/MariaDB connection (the suite runs on SQLite — see phpunit.xml) — `ShopLocationSchemaTest` asserts directly on its output, which is the exact list of statements the migration executes. `DistanceQuery` gained a matching `mariaDbDistances()` path alongside the existing `mysqlDistances()`/`phpDistances()` ones: since MariaDB has no `shop_location` column at all, it computes `ST_Distance_Sphere(POINT(lng, lat), POINT(?, ?))` inline from the plain columns instead of reading a precomputed spatial one — confirmed working on the production server before writing it. Audited every other migration for the same class of MySQL-only DDL (other generated columns, SRID, spatial indexes, `fullText()`) — `fullText()` on the `products` table is the only other spatial/text-index feature in the schema, and it's standard InnoDB fulltext supported identically by MariaDB 10.0.5+, so it needed no change.
- **`fakerphp/faker` moved from `require-dev` to `require`** in `api/composer.json`: `db:seed` (and any `Model::factory()` call) needs `fake()`/`Faker\Factory` at runtime, not just in tests, but a production `composer install --no-dev` strips `require-dev` entirely — the moment someone runs the seeder against a `--no-dev` production `vendor/`, it fails on an undefined `fake()` function. This was a genuine misclassification, not a new need: the factories under `database/factories/` were always meant to run outside test context (re-seeding demo data, per CLAUDE.md), so Faker was never actually dev-only. `composer update fakerphp/faker --no-dev --optimize-autoloader` pulled it into the `--no-dev` vendor tree with no new transitive packages — its own two dependencies (`psr/container`, `symfony/deprecation-contracts`) were already present via Laravel's core.
- **Google Maps billing couldn't be set up for this build, so the API key stays a `REPLACE_ME` placeholder in both native files by instruction** — rather than leave `DiscoveryMapView` and seller onboarding's step 2 attempting to render a `GoogleMap` that fails silently to blank grey tiles (a native-SDK-level failure Dart can't intercept), added `SokoniMapsConfig.isConfigured` (`app/lib/core/config/maps_config.dart`) as a hardcoded `false` flag mirroring the native placeholder, and gated both call sites on it. This is the same shape as the existing `SokoniSocialAuthConfig` gate for the Google/Apple sign-in blocker — a manually-maintained Dart-side mirror of a native-only placeholder, since there's no programmatic way to ask the native Maps SDK whether it has a working key. `DiscoveryMapView` now shows a `SokoniEmptyState` ("Map view isn't available yet") with a "View as list" action wired back to `feedIsMapViewProvider`. Seller onboarding step 2 already gated its "Confirm location" button purely on the three free-text address/region/district fields being non-empty, not on any pin interaction — so it was already completable without the map; the only gap was that it still tried to render a broken `GoogleMap` box above those fields. Swapped that for an info panel explaining maps aren't available yet, plus a "Use current location" button that still works (device geolocation and the `geocoding` package's reverse-geocoding both use platform-native services, not the Google Maps API, so they're unaffected by the missing key). When a real key eventually lands, it needs updating in three places with no single source of truth: both native placeholders and this Dart flag.
- **Release APK hung indefinitely on the splash screen, never reaching the feed — root cause: Firebase's Android SDK auto-initialises via a `ContentProvider` at process start**, now that `google-services.json` exists, independent of and earlier than `PushService.registerDevice()`'s own explicit `Firebase.initializeApp()` call (which is gated behind sign-in state and was never the trigger on a fresh install). On a device with no network or stale/missing Play Services, that auto-init's own attempt to register for an FCM token can hang before Flutter's engine ever produces a first frame — no Dart-level fix can reach that, since Dart code hasn't started running yet. Fixed with the documented native mechanism: `firebase_messaging_auto_init_enabled=false` meta-data in `AndroidManifest.xml`, deferring FCM's init to the existing explicit call site instead of firing automatically. Couldn't verify against a real device (no debugger access), so this is the highest-confidence diagnosis from tracing the startup path rather than a confirmed repro.
- **Defensive timeouts added across the rest of the startup path regardless of the above diagnosis**, since a Dart-visible hang was still plausible and worth closing off on its own merits: `LocationService.current()` now wraps its entire body (permission check + `Geolocator.getCurrentPosition()`, which has no timeout of its own and is a well-known source of indefinite hangs waiting for a GPS fix) in an 8s `.timeout()`, degrading to `LocationServiceDisabled()` on any failure — the same fallback the UI already handles by offering the district picker. `PushService.registerDevice()` wraps every Firebase/FCM step individually (`Firebase.initializeApp()`, `requestPermission()`, `getToken()`, the device-registration POST) in its own 8s timeout — belt-and-suspenders alongside the manifest fix above, since it's called `unawaited()` today but that only means *our* code doesn't wait on it, not that a stuck native call couldn't still cause trouble. `AuthStateController._loadInitial()`'s secure-storage token read gained a 5s timeout too, falling back to "signed out" rather than leaving `AuthState.isLoading` stuck true forever — nothing currently gates navigation on that state, but it's one less thing that could matter later without anyone noticing.
- **New `SplashScreen` wrapper (`core/router/splash_screen.dart`) adds a 5-second watchdog around `SokoniSplashTransition`**, replacing the router's previous direct use of that widget for the `/splash` route (the motion-gallery debug route still uses `SokoniSplashTransition` directly, unaffected — this wrapper is startup-specific, not a change to the primitive itself). The animation is self-contained and always completes in ~900ms under normal conditions, so the watchdog is a last line of defence, not the primary fix — if 5 seconds pass with no navigation, it swaps in the existing `SokoniErrorState` widget (message + retry button, already used elsewhere for exactly this "never a blank hang" rule) instead of leaving the animation frozen or the screen blank. Retry re-arms the watchdog and rebuilds `SokoniSplashTransition` with a fresh `ValueKey` so its `AnimationController` starts clean rather than reusing one that already ran past its course.
- **`widget_test.dart`'s splash test needed a longer pump window** after the timeout additions above: `Future.timeout()` creates a real `Timer` internally regardless of whether the source future ever completes, and with no platform-channel mock registered for secure storage/geolocator in that test, those underlying calls don't resolve within the ~1s the test originally pumped — `flutter_test`'s teardown asserts no `Timer` is still pending, so the test now pumps an extra 9 fake seconds (past the longest 8s timeout) before finishing. Confirmed this was the actual cause, not a real leak, by watching the specific failure (`!timersPending` at teardown, not a rendering assertion) resolve once the pump window was extended.
- **Android 16 (API 36) test device audit, after the previous startup-hang fix apparently didn't resolve it on a real Infinix device** (Dart watchdog never fired — meaning the hang is before Flutter's engine even starts, before any Dart code runs at all):
  - `compileSdk`/`targetSdk` already resolve to 36 via `flutter.compileSdkVersion`/`flutter.targetSdkVersion` (this Flutter SDK's, 3.47.0's, own defaults) — nothing to raise.
  - **New prime suspect found by reading the actual plugin source, not just version numbers**: `flutter_facebook_auth` 6.0.4's Android plugin (`FlutterFacebookAuthPlugin`) has a field initializer — `private final FacebookAuth facebookAuth = new FacebookAuth();` — that runs the moment the plugin object is constructed, which is during `GeneratedPluginRegistrant.registerWith()`, i.e. **every app launch, unconditionally, regardless of whether Facebook sign-in is ever used** — this is fundamentally different from `flutter_secure_storage` (traced its `onAttachedToEngine`/constructor: lightweight, defers all Keystore/`EncryptedSharedPreferences` work to a background `HandlerThread`, only on an actual read/write call) and `google_sign_in_android` (defers its SDK object creation via lambdas, never constructed at registration time) — neither of those two do any eager work at registration. `FacebookAuth()`'s constructor calls `LoginManager.getInstance()`, which triggers the Facebook Android SDK's own initialization — and this app has **no Facebook App ID configured at all**, anywhere (`AndroidManifest.xml`, `strings.xml` — both checked, neither has `com.facebook.sdk.ApplicationId`/`facebook_app_id`; BLOCKERS.md item 3 confirms a real one was never provided). A missing App ID is a well-documented Facebook SDK failure mode; on a security-locked device (developer options locked by a financing app — a strong signal of restricted background/network/advertising-ID access), whatever the SDK does in that situation is far more likely to genuinely hang than on a normal device, and since it runs during plugin registration, no Dart-level fix (including the 5s watchdog just added) can reach it — Dart hasn't started running yet. `package_info_plus` 9.0.0 wasn't re-examined as deeply since its `onAttachedToEngine` is trivial (channel setup only, confirmed by reading the file) and it has no direct call site in this app per the earlier win32 decision log.
  - `main()` (`app/lib/main.dart`) re-confirmed clean: two synchronous `PaintingBinding.instance.imageCache` calls, then `runApp()` — no awaits, no plugin calls, nothing that could hang. Consistent with the hang being entirely native and pre-Dart.
  - Added a `diagnostic` Gradle product flavor (`android/app/build.gradle.kts`) to isolate the above without needing a device debugger: `DiagnosticMainActivity` (`android/app/src/diagnostic/kotlin/.../DiagnosticMainActivity.kt`) overrides `provideFlutterEngine()` to supply a bare `FlutterEngine` and start it manually — since taking over engine creation means Flutter's own default path (the one that normally calls `GeneratedPluginRegistrant.registerWith`) never runs, **zero plugins get registered**, including the Facebook one. The flavor's manifest (`android/app/src/diagnostic/AndroidManifest.xml`) also `tools:node="remove"`s the real `.MainActivity` and, for extra isolation against native-AAR-level (not just Dart-plugin-level) auto-init, Firebase's and Facebook's own auto-init `ContentProvider`s by name — those run at process start via manifest merging from the AAR dependencies, independent of `GeneratedPluginRegistrant` entirely, so skipping Dart plugin registration alone wouldn't rule them out. A separate Dart entrypoint (`lib/main_diagnostic.dart`) renders a plain "Sokoni starting" screen with no providers/router/theme. Build with `flutter build apk --flavor diagnostic --release -t lib/main_diagnostic.dart` — installs alongside the real app via `applicationIdSuffix = ".diagnostic"` rather than replacing it, so both can be compared side by side on the same device.
  - **Adding any Gradle flavor at all removes the previous flavor-less default** — `flutter build apk --release` (no `--flavor`) no longer works; every build command needs `--flavor prod` now, including CI/deploy docs (updated `README.md` and `docs/DEPLOY.md`) and the output filename gains the flavor name (`app-release.apk` → `app-prod-release.apk`). Verified both `prod` and `diagnostic` release builds succeed and are signed with the real release keystore (same SHA-1 fingerprint as before).
  - The Google Services Gradle plugin processes every variant by default and fails the whole build if `google-services.json` has no client entry matching that variant's `applicationId` — true for `diagnostic` once `applicationIdSuffix` changes the package name. Rather than fabricate a second client entry in a file that otherwise holds real production credentials, disabled that specific task for the diagnostic flavor via a `tasks.configureEach` name match in `app/build.gradle.kts`.
  - Could not verify any of this against the actual device (no logcat access) — this is the most well-evidenced diagnosis available from reading plugin source and cross-referencing the device's own symptoms (locked dev options → restricted device, "before Flutter engine starts" → native not Dart), not a confirmed repro. The diagnostic APK is the intended next step to actually confirm it.
- **Confirmed on the real Android 16 device: the diagnostic build boots successfully, the full app doesn't — a plugin is hanging during native registration, not Flutter or the device itself.** `flutter_facebook_auth` was the strongest suspect (see the entry above) and, with no Facebook App ID ever configured and no product requirement forcing a fix, the simpler and more robust call is removing it entirely rather than trying to work around a plugin bug in a package with no working configuration anyway. Removed everywhere: `pubspec.yaml`/`pubspec.lock` (package gone, confirmed via `flutter pub get` — the long-standing `flutter_facebook_auth:macos references facebook_auth_desktop` warning that's nagged every build log all session is gone too, since it only existed because of this dependency), the Facebook button/handler/enum case in `social_sign_in_buttons.dart`, `isFacebookConfigured` from `SokoniSocialAuthConfig`, the `facebook_rounded` icon and `signInWithFacebook` l10n string (both ARB files), the Facebook branch and its `graph.facebook.com` calls from `HttpSocialAuthVerifier`, `facebook` from `SocialLoginRequest`'s provider whitelist, the `services.facebook` config block, and `FACEBOOK_APP_ID`/`FACEBOOK_APP_SECRET` from both `.env` and `.env.example`. Also corrected the Terms/Privacy screens' "sign in via Google/Facebook/Apple" copy and `docs/API.md`/`DEMO.md`/`DEPLOY.md` — didn't bump `sokoniTermsVersion`, since this is a factual correction (a sign-in method that no longer exists) rather than a change to what data is collected or how, which is what that acceptance-recording mechanism is actually for. Google, Apple, and phone OTP are untouched and still fully real. Neither `AndroidManifest.xml` nor `strings.xml` (which doesn't exist in this project) had any Facebook config to remove — confirms BLOCKERS.md's original note that the App ID was never actually supplied.
- **The `subprojects { afterEvaluate }` JVM 17 workaround in `android/build.gradle.kts` was checked empirically, not assumed, before removing it**: temporarily deleted the block, ran `gradlew clean` (forcing every subproject to recompile from scratch, since a stale build-cache hit could hide a real regression), then a full `flutter build apk --flavor prod --release`. It succeeded — only harmless "source value 8 is obsolete" deprecation warnings, not the "Inconsistent JVM Target Compatibility" hard failure the workaround was written for. Removed it for good, with a comment explaining the empirical check for whoever finds a plugin needing it again someday.
- **The diagnostic flavor's manifest no longer removes `com.facebook.internal.FacebookInitProvider`** — now that `flutter_facebook_auth` is gone entirely, that provider is never merged into any manifest in the first place, so a `tools:node="remove"` directive targeting it would be a no-op referencing a dependency that no longer exists. Left the Firebase provider-removal directive in place (still relevant, unrelated to Facebook) and updated the flavor's docstring to record what it actually found, so it reads as a resolved investigation rather than an open one — the `DiagnosticMainActivity` infrastructure itself stays, as general-purpose tooling for isolating any future plugin-registration-time hang.
- **Facebook removal didn't fix the Android 16 hang — confirmed by the project owner on the real device, with the diagnostic build (zero plugins) still booting fine** — so `DiagnosticMainActivity` was rebuilt to actually bisect *which* plugin is at fault, rather than only proving *that* one is, since there's still no adb/logcat access on this device (developer options locked by a device-financing app).
  - Rewrote it as a plain `Activity` (not `FlutterActivity`) for full manual control over exactly what's on screen and when — copied every plugin's concrete class name from the generated `GeneratedPluginRegistrant.java` (never edited directly, it's regenerated every build) and calls `engine.plugins.add(PluginInstance())` for each one individually, in a fixed order, instead of the generated file's all-at-once loop. Ordered the eight named suspects last (geolocator, google_maps_flutter, flutter_secure_storage, video_compress, firebase_core, firebase_messaging, google_sign_in, sign_in_with_apple), everything else first.
  - Before each registration attempt, both an on-screen `TextView` and a log file get the plugin's name — deliberately in that order and with a short scheduled delay (`Handler.postDelayed`, 50ms) between setting the text and making the actual (possibly hanging) call, not immediately after: `TextView.setText()` only *schedules* a draw, it doesn't paint synchronously, so writing the label and immediately calling straight into a hang risked the screen never actually rendering the update — freezing on stale text from the *previous* step instead of naming the real culprit. The delay gives Android's rendering pipeline a full frame (50ms is generous at 60fps) to actually paint before risking the freeze.
  - Logging goes to `Downloads/sokoni-boot.txt` via `MediaStore.Downloads`, which needs no storage permission at all on API 29+ (this device is API 36) — queries for an existing row by display name first and reuses it across relaunches (append mode, flushed after every single line) rather than letting MediaStore mint `sokoni-boot (1).txt`, `(2).txt`, etc. on every retry, so the phone's own Files app always has one file with the full history.
  - Firebase's `FirebaseInitProvider` — previously stripped from this flavor's manifest via `tools:node="remove"` — is deliberately left in *this* version, unlike before: `firebase_core`/`firebase_messaging` are still real dependencies (unlike Facebook), so if the true hang is at that pre-`onCreate`, native-ContentProvider level rather than in Dart-plugin registration, this build will hang too, before ever showing its first frame or writing its first log line — itself a diagnostic result, distinguishable from a mid-list plugin freeze by the log file being completely absent/empty rather than ending mid-sequence. Confirmed via the merged manifest (`build/app/intermediates/merged_manifests/diagnosticRelease/.../AndroidManifest.xml`) that the provider is genuinely present in this build, not silently still stripped.
  - On full success (all 22 plugins register), hands off to a real `FlutterView` attached to the same engine and starts `main_diagnostic.dart`'s Dart entrypoint — so a clean run also positively confirms Flutter itself, and every plugin's native registration, are all fine, narrowing any remaining hang to something else entirely (Dart-level startup code, unlikely per the earlier `main.dart` audit, or something outside plugin registration altogether).
  - Verified the Kotlin compiles cleanly against the real plugin classes (a wrong package/class name for any of the 22 would be a compile-time error, not a runtime one) and that the built APK is signed with the real release keystore.
- **Device-testing round, PARTS 1–2: diagnosed via framework source and a live production probe, not guesswork, before touching any code.** The instruction was explicit ("do not guess"), so before changing anything: read `AuthController::acceptTerms`/`AcceptTermsRequest`/the `users` migration (all correct — token issued before the terms screen ever runs, route correctly gated behind `auth:sanctum`, columns present since the very first migration); then probed the *live* API directly with `curl` against `https://api.sokoni.co.tz/api/auth/terms/accept`, matching the exact headers `dio_client.dart` sends (`Accept: application/json`) — got back a clean `401 {"message":"Unauthenticated."}`, which rules out the "guard fails open/closed wrong" and "route not registered" hypotheses the instruction specifically asked to check. That left the client itself as the only remaining place the bug could live, so traced `TermsAcceptanceScreen._continue()` line by line: `PopScope(canPop: false)` wraps the screen (correct — it must not be back-swipeable while a request is in flight or unaccepted), but the success path called `Navigator.of(context).maybePop()`. Read the Flutter SDK's own `navigator.dart`/`pop_scope.dart` source directly rather than assuming: `PopScope`'s own doc states "programmatically attempting pop navigation will also result in a call to `onPopInvokedWithResult`" when blocked, and `NavigatorState.maybePop()`'s implementation literally checks `route.popDisposition`, which returns `doNotPop` for a page whose `PopScope.canPop` is false — while `NavigatorState.pop()` (no "maybe") has no such check at all, confirmed by reading its body. So the accept call always succeeded and persisted server-side, but the pop was always silently refused — spinner, then back to the same screen, deterministically, on every attempt. This is also a full, unforced explanation for PART 2's separately-reported "missing" intent screen: `ensureTermsAccepted()` awaits that pushed route's future before `completeSignIn()` can reach the `is_new_account` check that shows it, so nothing past the stuck terms screen could ever run in the same session — and a tester who force-closed and reopened the app afterward would land straight on home on the next launch, since (per the existing PART 2 decision) only the fresh sign-in path enforces the terms/intent gate at all, not a persistent check on every launch. One bug, two symptoms.
- **Fix: `context.pop()`, not `Navigator.pop()` or `maybePop()`.** `context.pop()` (go_router's extension) still ultimately calls the plain, unconditional `NavigatorState.pop()` under the hood (traced through `GoRouteInformationParser`/`GoRouterDelegate.pop()` in the installed `go_router` 17.5.0 source) — it's the documented, intentional escape hatch for "block back gestures via `PopScope.canPop: false`, but still allow leaving from my own button," and matches this codebase's existing convention of using go_router's own context extensions everywhere rather than raw `Navigator` calls (see the Phase 5 decision on the same point). Also broadened `_continue()`'s catch from `on ApiException` to a second `catch (Object)`, since the instruction was explicit that a failure must never be silently swallowed — a typed `ApiException` was already being shown, but any *other* exception (a future response-shape change, for instance) would previously propagate past the `try/catch` as an unhandled Future error with the spinner just stopping and no message shown, which is its own version of the same "looks like nothing happened" bug class.
- **New regression test had to use a real `GoRouter`, not a bare `Navigator.push`**: a first draft of `terms_acceptance_screen_test.dart` pushed the screen via plain `MaterialPageRoute`/`Navigator.push`, and it *passed* even before the fix was applied — because `context.pop()` throws "no GoRouter found" outside a `GoRouter` tree, which the broadened `catch (Object)` swallowed into a visible error, coincidentally also leaving the screen open (the same *symptom* as the real bug, for a completely different, test-artifact-only reason). Rebuilt the test around a minimal real `GoRouter` with two routes, mirroring exactly how `ensureTermsAccepted()` reaches this screen in production (`context.push`), which is what actually exercises the `PopScope`-vs-`maybePop` interaction and would have caught the original bug.
- **Device-testing round, PART 3 — feed composition algorithm.** CLAUDE.md's exact wording ("mix, don't segregate: followed shops first, then nearby verified sellers, then Offers running now, then Showcase videos inline every 6-8 items") reads as a literal ordering, not just a ranking weight, so `FeedService` builds it that way: concatenate three ordered typed lists (followed → non-followed, sponsored-first-within-that-tier → running Offers) into one "spine," then walk it inserting one Showcase after every 6th item, cycling through available Showcases if there are more 6-item blocks than Showcases exist. Picked 6 (the tight end of "6-8") as a fixed interval specifically so the result is deterministic and testable — `test_showcases_are_woven_into_the_feed_at_a_fixed_interval` asserts `types[6] == 'showcase'` directly, which a randomised or content-dependent interval couldn't support as cleanly.
- **Sponsored Listings rank ahead of the non-followed tier, never ahead of a followed shop.** Re-read the brief's own framing ("Sponsored/boosted products... appear in the feed with a discreet 'Sponsored' label") alongside its Instagram comparison — a boost is understood everywhere as buying visibility among strangers, not among people who already chose to follow you; ranking a stranger's paid boost above a followed shop's organic post would read as the app selling out its own "people you follow first" promise. Implemented as a sort key on the *non-followed* tier only.
- **Offers get their own dedicated feed card, not *also* a strikethrough overlay bolted onto their underlying product's regular card.** The brief describes both "Product line:... strikethrough original price when an Offer is running" (implying inline) and "Offers running now" as its own composition bucket (implying a separate card) — read literally these overlap for a product that has an active Offer. Chose one clear treatment (the dedicated card, reusing the same layout `OffersRow`'s existing `_OfferCard` already established) over building both, since a product appearing twice in one feed — once plain, once with a strikethrough — with no de-duplication logic between the two paths would read as a bug, not a feature. Logged explicitly rather than silently picking one.
- **The Order button on every feed card variant (product/offer/showcase) opens the existing product detail screen, not an inline mini-checkout.** The app already has one real, fully-built order flow (cart → checkout → confirmation) reached from product detail; duplicating any part of that inline in a scrolling feed card — even just "add to cart directly" — would mean two checkout entry points to keep in sync (single-seller-cart rules, delivery method, payment method) for a feed that's explicitly supposed to feel like browsing, not a second commerce surface. "Prominent Order button" is satisfied by a real, working, one-tap path into the real flow.
- **A real `Fillable`-vs-`update()` silent-no-op bug recurred, caught by a test before shipping**: `NotificationController::markRead`'s first draft called `$notification->update(['read_at' => now()])`, which silently no-oped — `read_at` is deliberately excluded from `AppNotification`'s `#[Fillable]` (a user should never mass-assign it via some other write path), the exact same trap `Order`/`Review` status transitions hit back in Phase 2 and the same fix (`forceFill()`) applies. Notable only because it's the *third* time this specific bug class has appeared in this codebase across sessions — worth treating any new `->update([...])` call touching a deliberately-non-fillable field as guilty until proven innocent.
- **A second, genuinely new Laravel bug found while debugging that same test failure (after the `forceFill()` fix, the test *still* failed)**: `AppNotificationResource`'s `toArray()` returned a `'data'` key (the notification's own arbitrary JSON payload column) — and a `JsonResource` whose output array already has a top-level key matching the wrap key ('data') causes Laravel to skip auto-wrapping *entirely*, confirmed by reading `Illuminate\Http\Resources\Json\ResourceResponse::haveDefaultWrapperAndDataIsUnwrapped()`, which literally checks `! array_key_exists($this->wrapper(), $data)` before deciding whether to wrap. So instead of `{"data": {"id":1,...,"data":{...}}}`, the response came back as the bare inner array with no envelope at all — every other endpoint in this API relies on that envelope (`assertJsonPath('data.xxx', ...)` throughout the test suite, and the Flutter client's repositories all do `json['data']`). Fixed by renaming the resource's output key to `payload` (the underlying `app_notifications.data` DB column keeps its name — only the API-facing field changed) and documented the mechanism directly in the resource file, since this is the kind of trap that will resurface silently the moment any *other* future resource happens to expose a field literally called `data`.
- **`seller_profiles.logo` added, a real gap not previously covered by any field**: both Part 3's feed card and Part 4's Instagram-style shop header need a circular shop avatar, and the only avatar field in the entire schema is `users.avatar` — the signed-in person's own account photo (from Google/Apple sign-in), which is a *different* thing from a shop's brand logo and would be actively wrong to substitute (a seller's personal profile photo showing up as their shop's identity). Added the column, an owner-only upload endpoint (`PATCH /sellers/{id}/logo`, mirroring the existing NIDA/licence upload pattern — full-URL-stored, unlike those two, since this one is actually rendered as an avatar image client-side rather than only opened by a human reviewer) and exposed it on both `SellerSummaryResource` and `SellerProfileResource`.
- **A real, persisted in-app notification inbox, not a dead top-bar icon.** CLAUDE.md Part 3 names a "Notifications" icon in the required top bar; auditing what already exists found only ephemeral, currently-mocked FCM pushes (`LogPushNotifier`, logs and discards) — nothing durable a "Notifications" screen could actually list. Building a fake/static list would violate the "no placeholder code" rule as much as an empty stub would, so added a real (if intentionally minimal) `app_notifications` table and wired persistence into the *one* currently-bound `PushNotifier` implementation, so every existing call site (new order, status change, new message, new followed-shop Update/Offer) now durably lands in the inbox with no per-call-site changes needed. Named the table `app_notifications`, not `notifications`, specifically to avoid colliding with Laravel's own built-in notifications system conventions (UUID `id`, polymorphic `notifiable`) in case that's ever adopted later — this app never used it, and reusing its table name would only invite confusion.
- **Found and logged, not fixed here — `APP_DEBUG=true` on the live production server.** While probing `api.sokoni.co.tz` above, a request with no `Accept: application/json` header returned a full Laravel debug page (stack trace, absolute `/home/sokoftsn/...` paths) instead of Laravel's normal HTML/JSON error response — meaning production is not actually running with `APP_DEBUG=false` as `docs/DEPLOY.md` has always specified, regardless of what's in this repo's `.env.example`. This is a live-server configuration value, not something in git history or this codebase to fix directly (no SSH/hosting credentials available to this session) — logged in `BLOCKERS.md` under a new "needs the project owner's attention on the live server" section, since it doesn't fit the existing four-credential-blocker list but is a real, separate security issue found along the way.
- **Bisection came back clean — all 22 plugins registered, "Sokoni starting" reached — so the hang is somewhere in the real app's own startup path, and it happens before `SplashScreen`'s own 5s watchdog ever arms**, which itself is diagnostic: everything from `SokoniSplashTransition.initState()` onward is a pure, self-contained ~900ms animation with nothing awaited in it, so a hang *there* was never structurally plausible — the true freeze has to be at or before `main()`/`runApp()`/the first frame.
  - Traced every line from `main()` to the first frame by hand: `main.dart` (two synchronous `imageCache` calls, then `runApp()` — no awaits), `SokoniApp.build()` (one `ref.watch(routerProvider)`, a plain sync `Provider<GoRouter>`, then `MaterialApp.router(...)`), `app_router.dart` (confirmed, again: **no `redirect:` callback anywhere, on the `GoRouter` itself or any individual `GoRoute`** — the `/splash` route's `builder` unconditionally returns `SplashScreen`, nothing async gates route resolution), and `SplashScreen.initState()` (arms a plain `Timer`, no awaits). None of this should be able to block a first frame from painting, which is exactly why the previous per-widget watchdog was structurally unable to help against whatever this actually is.
  - Found one real, concrete bug along the way that matches the "only reproduces on slow 3G, never on WiFi" clue precisely, by reading `google_fonts`' own source (`google_fonts_base.dart` in the pub cache) rather than guessing: `GoogleFonts.plusJakartaSans(...)`/`plusJakartaSansTextTheme()` (used for every text style in `SokoniTypography`, evaluated as part of constructing `SokoniTheme.light`/`.dark`, which `MaterialApp.router`'s `theme:`/`darkTheme:` params reference) is **synchronous at the call site** — it returns a `TextStyle` immediately and kicks off font loading via a fire-and-forget `Future` (`loadFontIfNecessary`, added to a `pendingFontFutures` set the app never awaits) — so it does *not*, on its own, block the widget tree from building or painting. But that background fetch (`_httpFetchFontAndSaveToDevice`, since the font was never bundled as an asset and no real Plus Jakarta Sans `.ttf` files were available locally to bundle instead) calls `client.get(uri)` with **no timeout at all** — on a stalled ~2KB/s connection, that call can hang indefinitely in the background on every single cold start. Not confirmed as *the* cause of this specific freeze (the call isn't awaited by anything that would block first paint), but a real bug regardless given CLAUDE.md's explicit "mid-range Android on patchy 3G/4G" target, so fixed it: `main()` now sets `GoogleFonts.config.httpClient` to a small `http.BaseClient` wrapper (`_TimeoutHttpClient` in `main.dart`) that applies an 8s `.timeout()` to every request, bounding the risk without needing to fabricate font assets that weren't available to bundle.
  - Rearchitected the watchdog per instruction, "before any async work, not inside the widget that may never build": `main.dart` now arms a **global** `Timer` immediately, before `runApp()` — independent of whether `SplashScreen` (or any widget) ever builds at all — and cancels it via `WidgetsBinding.instance.addPostFrameCallback` the moment Flutter paints *any* first frame, whatever route that ends up being. If it fires, `runApp()` is called a second time with a minimal, self-contained `_StartupTimeoutApp` (own `MaterialApp`, still using the real `AppLocalizations` delegates since that's synchronous/asset-based, not network) showing the same error+retry pattern as `SplashScreen`'s local one. `SplashScreen`'s own watchdog stays too, as a second line of defence for anything that hangs *after* first paint but before the splash animation completes — the two are complementary, not redundant, since they protect different phases.
  - Built `BootLog` (`core/diagnostics/boot_log.dart`) — a `MethodChannel('sokoni.diagnostic/log')` call, silently swallowed via `MissingPluginException` in the real app where nothing registers that channel (same fails-soft shape as `PushService` around Firebase), so it's safe to leave permanently rather than strip once this is resolved. Instrumented `main()` (entry, pre-`runApp`, watchdog armed) and `SplashScreen` (`initState` start, watchdog armed, watchdog fired, animation complete) with `BootLog.step(...)` calls.
  - Extended `DiagnosticMainActivity` to receive these calls and append them to the *same* `sokoni-boot.txt`: registers a `MethodChannel` handler on the engine's `binaryMessenger` right before `executeDartEntrypoint()`, in `launchFlutterUi()`. Rebuilt the diagnostic APK with `-t lib/main.dart` (the real entrypoint) instead of `main_diagnostic.dart` — the class itself didn't need to change for this, since `DartExecutor.DartEntrypoint.createDefault()` already resolves whatever target the build was given; only the build command changed. `main_diagnostic.dart` (the bare "Sokoni starting" screen) is left alone, still usable for a plugin-registration-only pass later if ever needed again.
  - Rebuilt and verified both `app-prod-release.apk` and `app-diagnostic-release.apk` (signed, same release-keystore fingerprint as every prior build this session) — `flutter analyze` clean, all 3 widget tests pass.
- **Device run came back: all 22 plugins registered, "ALL 22 PLUGINS REGISTERED — starting Dart entrypoint" logged — then total silence, not one `[dart]`-prefixed line, screen black.** Two live hypotheses: Dart never actually starts running, or it does but `BootLog` itself is silently failing, leaving us blind either way. Addressed both, plus the two other concrete leads from the same instruction:
  - **Proved the channel rather than assumed it.** `DiagnosticMainActivity.launchFlutterUi()` now logs `"Dart channel handler attached"` right after `setMethodCallHandler` registers, logs `"CHANNEL RECEIVED method=... arguments=..."` for *every* message it ever gets (not just well-formed `"log"` calls), and logs the `DartEntrypoint`'s `pathToBundle`/`dartEntrypointLibrary`/`dartEntrypointFunctionName` immediately before `executeDartEntrypoint()` — plus a line right after that call returns, since `executeDartEntrypoint` itself is non-blocking (kicks off the isolate and returns immediately), so that line alone confirms the native call didn't hang, independent of whatever Dart does next.
  - **Made `main()`'s first statement a real, working ping — not literally the first line of the function, but the first line *capable of succeeding*.** `BootLog` sends over a `MethodChannel`, which needs `ServicesBinding`'s messenger to exist — calling it before `WidgetsFlutterBinding.ensureInitialized()` would make the "is Dart running" test itself unreliable, giving a false negative if binding init (needed either way) hadn't happened yet. So the true first two lines are `WidgetsFlutterBinding.ensureInitialized()` immediately followed by the ping, both wrapped in `runZonedGuarded` (see next point) so even a throw during binding init gets *some* chance of being caught — a real, if inherent, limit: if the isolate can't stand up its own bindings at all, no channel-based signal can ever prove that, since the signal itself depends on those bindings.
  - **Wrapped the entire `main()` body in `runZonedGuarded`, wired `FlutterError.onError` and `PlatformDispatcher.instance.onError`, and routed everything through `BootLog`.** A silent black screen with zero log output past plugin registration is the textbook signature of an uncaught exception before the first frame — this exists to name it instead of guessing again. Timers created inside the guarded zone (the global watchdog included) run their callbacks in the same zone, so exceptions there are covered too.
  - **Removed `google_fonts` entirely, per instruction, rather than just bounding its fetch with a timeout as the previous pass did.** Rewrote `SokoniTypography` (`core/theme/typography.dart`) to build its `TextTheme` from Flutter's own `Typography.material2021().black.apply(fontFamily: 'PlusJakartaSans')` instead of `GoogleFonts.plusJakartaSansTextTheme()`, and every individual `GoogleFonts.plusJakartaSans(...)` call became a plain `TextStyle(fontFamily: 'PlusJakartaSans', ...)`. **Could not source the real Plus Jakarta Sans `.ttf` files to bundle** — no network access, and none cached anywhere on this machine (checked the `google_fonts` package's own example assets, the Flutter SDK, the whole filesystem — only found an unrelated `Lato` example font and `Asia/Jakarta` timezone data). Fabricating a substitute binary would have been worse than not having one, so `fontFamily: 'PlusJakartaSans'` is left wired up with no matching asset yet — Flutter falls back to the platform default (Roboto) silently and safely until the real files are sourced, at which point dropping them into `assets/fonts/` and uncommenting the ready-made `fonts:` block already sitting in `pubspec.yaml` is the entire remaining step, no code changes. This was flagged as not-confirmed-as-cause last pass (the fetch is fire-and-forget, shouldn't block first paint on its own) but removed anyway per instruction: downloading a typeface at startup is wrong for a 2KB/s-3G market regardless of whether it's this specific bug.
  - Rebuilt and reverified both `app-prod-release.apk` and `app-diagnostic-release.apk` — signed, same release-keystore fingerprint as every build this session, `flutter analyze` clean, all 3 widget tests pass. Root cause still not conclusively identified; this run is specifically built to stop being blind about it.
- **Conclusive log at last: Dart runs, the first frame paints, but `SplashScreen`'s own watchdog fires — "animation never completed."** Traced `SokoniSplashTransition` end to end: `AnimationController(duration: 900ms)` + `_controller.forward()` in `didChangeDependencies`, a status listener calling `_complete()` on `AnimationStatus.completed`, nothing awaited anywhere in the class — structurally, the only way `onComplete` never fires is if the controller's ticker itself never reaches `completed`, not any Future/image-load gate in the completion path directly (`Image.asset` inside `AnimatedBuilder`'s `builder` doesn't block the controller — the ticker advances independently of whether the child widget's image has finished decoding).
  - **But the asset behind that `Image.asset` call turned out to be a real, severe bug anyway**: `assets/brand/sokoni_logo_icon.png` (and `sokoni_logo.png`, byte-identical) had become a **6250×6250** placeholder — confirmed via `file`, then visually via the Read tool — decoding to ~149MB of raw RGBA for a widget displayed at 96×96, with **no `cacheWidth`/`cacheHeight` hint**, freshly reconstructed on every single tick of `AnimatedBuilder`'s rebuild (dozens of times across the 900ms animation). On a real mid-range device, a decode that size — repeated that often, with no cap — is exactly the kind of main-thread/GC pressure that could stall ticker callbacks long enough to look like "never completes," even though the controller and the widget's own logic have no direct dependency on the image load. Not a proven single root cause (the controller's ticker is architecturally independent of the image), but a concrete, verified, serious problem in its own right, and fixed regardless.
  - **Also found, mid-investigation: my own crop script's content-detection had a real bug**, caught and fixed before it produced a bad result — the placeholder's background is fully *transparent* (alpha=0), not white as assumed from how it renders in an image viewer (viewers composite transparent regions onto white for display, which isn't the actual pixel data). A luminance/whiteness-based "is this background" check treated the *entire* image as one undifferentiated content region, since transparent pixels happened to store as `(0,0,0,0)` — same RGB as opaque black glyph pixels. Fixed by detecting content via the alpha channel directly (`pixel.a > 16`) instead, which correctly separated the icon glyph from the "Sokoni" wordmark into two distinct row-bands, same as the real logo's crop earlier in this session.
  - Also caught, along the way: NTFS is case-insensitive but case-*preserving* — writing to `sokoni_logo.png` (lowercase, as declared in `pubspec.yaml` and referenced by every `Image.asset` call) silently overwrote the *same* underlying file previously named `sokoni_logo.PNG` (uppercase extension) rather than creating a second file, which is how a first crop attempt ended up reading back its own already-resized output instead of the original — harmless here since Windows resolves either case to the same working build, but a real latent risk if this project is ever built on a case-sensitive filesystem (Linux CI). Force-renamed both canonical copies (repo root and `app/`) to proper lowercase `.png` via a two-step temp-name rename, the only way to get NTFS to actually update the stored case.
  - Fixed the asset itself: resized the full lockup down to 1080×1080 (`sokoni_logo.png`, ~20KB, alpha preserved — used by `flutter_native_splash`, which composites it onto its own background color) and re-cropped an icon-only glyph (no wordmark, same reasoning as the real logo's crop earlier this session — the wordmark is illegible at launcher-icon sizes) onto a **white** canvas this time, not black: this placeholder's glyph is black-on-transparent (the earlier real logo was white/yellow-on-black), so a black canvas made the bag body nearly invisible — white gives guaranteed contrast and matches CLAUDE.md's own "black-on-white" design language regardless. Regenerated launcher icons and splash screens (`flutter_launcher_icons`, `flutter_native_splash`) from the corrected assets.
  - Added `cacheWidth`/`cacheHeight` to *both* `Image.asset` call sites that reference this file — `SokoniSplashTransition` (192×192, covering up to 2x device pixel ratio at the 96×96 display size) and `SokoniBagSpinner`/pull-to-refresh (a fixed 96×96 regardless of `widget.size`, which varies continuously during the pull gesture — a fixed cache size means every frame reuses one decoded image instead of re-decoding at a slightly different target each time). Defense in depth: caps decode cost to the actual display size no matter what the source asset's real resolution turns out to be, so this exact class of bug — an oversized source asset silently multiplying decode cost — can't recur unnoticed even if a future asset swap reintroduces it.
  - **Rearchitected `SplashScreen` per instruction: an animation must never gate routing.** Navigation to home now runs off a plain `Timer` (950ms, just past the animation's own 900ms duration) *independent* of `SokoniSplashTransition`'s `onComplete` callback — whichever fires first wins (both converge on the same idempotent `_navigate()`, guarded by a `_navigated` flag), so the animation completing normally is just the same navigation landing a few dozen milliseconds early in the common case, not a separate code path that has to work for navigation to happen at all. The 5-second watchdog stays as a further backstop but **no longer shows an error screen** — it now calls the same `_navigate()` too. Removed the `SokoniErrorState`/retry UI and the `_attempt`/`ValueKey` retry-rebuild mechanism entirely from this widget, since there's no longer a state that needs a manual retry — every path this widget can take ends at the home feed. (`main.dart`'s separate, earlier-armed *global* watchdog is unaffected and keeps its error+retry screen — that one exists for the case where nothing has rendered at all yet, where "navigate to home" isn't a meaningful action since there's no confirmed-working router/provider tree to navigate with.)
  - `flutter analyze` clean, all 3 widget tests pass, both `app-prod-release.apk` and `app-diagnostic-release.apk` rebuilt and verified against the real release keystore.
- **PART 1 — contrast/readability audit.** Found the actual mechanism behind the location-permission dialog bug, then used it to find and fix a much longer list of the same two bug classes across the app, rather than patching the one reported dialog.
  - **Root cause of the dialog bug**: `core/theme/typography.dart` built `SokoniTypography`'s base `TextTheme` from `Typography.material2021().black` — a *fixed*, light-theme-only variant — for **both** light and dark themes. Since only 8 of `TextTheme`'s roles were explicitly overridden with theme-aware colours, every non-overridden role (`headlineLarge/Medium/Small`, `titleSmall`, `labelMedium`, `displayMedium/Small`) stayed stuck at `.black`'s colours regardless of brightness. Read `AlertDialog`'s own Material 3 default source (`_DialogDefaultsM3` in the Flutter SDK) to confirm it specifically reads `headlineSmall` for its title — exactly one of the un-overridden roles. Fixed at the root: `textTheme()` now takes an explicit `Brightness` and picks `Typography.material2021().white` for dark, `.black` for light, fixing every non-overridden role across the whole app in one change, not just this one dialog. Also made `dialogTheme.titleTextStyle`/`contentTextStyle` explicit (pulled from the now-correct `textTheme`) as a second, independent layer of defence, so `AlertDialog` never depends on Material's internal default-resolution chain at all.
  - **Bottom nav bar**: `AnimatedNavBar`'s selected-tab colour was hardcoded `SokoniColors.sokoniBlack`, unconditional — black-on-near-black in dark mode. Fixed to read "clearly Sokoni yellow" (per the brief) as a yellow **fill** behind the icon (a pill, matching Material's own indicator convention), with the icon drawn black-on-yellow and the label using the theme's actual `onSurface` — not by making the icon/label text itself yellow, which fails WCAG badly on either surface (`#FAC902` on white is ~1.6:1, nowhere near the 3:1 a graphic needs, let alone 4.5:1 for the label text) — this is the exact pattern the brief separately called out ("It is a fill and accent colour, not a text colour on light surfaces").
  - **Delegated a full-codebase sweep to a research agent** for every other instance of the same two bug classes (hardcoded colours ignoring brightness; `sokoniYellow` used as text/icon foreground rather than fill) plus a full inventory of every dialog/sheet/snackbar and every use of the TextTheme roles that used to be un-overridden. Fixed everything the sweep found real breakage in:
    - Hardcoded `sokoniBlack`/`surfaceAlt` (unconditional, no `isDark` branch) on top of already-theme-aware text: `shared/widgets/product_card.dart` (price text + 3 image-placeholder backgrounds), `features/product/presentation/product_detail_screen.dart` (price text), `features/seller/presentation/shop_profile_screen.dart` (header background + review-reply bubble, two separate widgets), `features/seller/presentation/onboarding/step2_location.dart` (maps-unavailable placeholder background + icon), `features/discovery/presentation/widgets/radius_filter_sheet.dart` (`RadioListTile.activeColor`, same bug class as the nav bar — switched to yellow, which needs no brightness branch at all and matches the chip-selection accent language already used elsewhere).
    - `sokoniYellow` as text foreground on its own pale tint background (order status chip, seller "pending" status) — both redesigned to the same pattern: the status colour tints the pill and marks a small dot, the label text itself uses the theme's `onSurface`. Also caught the same shape of bug in the chat message bubble: "mine" bubbles stay `sokoniYellow` regardless of theme, but the body text had no explicit colour and was inheriting the *ambient* theme text colour (near-white in dark mode) — near-white on bright yellow is poor contrast; fixed by forcing `onYellow` (black) specifically for that bubble.
    - `shared/widgets/empty_state.dart`'s icon was `sokoniYellow` directly on `surfaceAlt` — in light mode that's functionally yellow-on-white (~1.6:1). Fixed to the same fill/foreground split as everywhere else: yellow-filled circle, black icon on top.
    - Lower-severity, cosmetic-only (theme-inconsistent but never actually unreadable, fixed anyway for consistency across the whole app): `review_distribution_bar.dart`'s progress-track background, `seller_onboarding_screen.dart`'s inactive step-indicator colour, `app_shell.dart`'s Sell FAB (raw `Color(0xFFFAC902)`/`Color(0xFF0A0A0A)` literals instead of the `SokoniColors` constants — duplication, not a contrast bug), `motion_gallery_screen.dart`'s demo card background (debug-only route). Deliberately left `connectivity_banner.dart` alone — its black/white/yellow banner is a fully self-consistent, intentionally high-contrast alert treatment regardless of app theme, not a bug.
  - **Built `/theme-audit`** (`features/debug/theme_audit_screen.dart`), reachable regardless of the device's actual system theme: renders both light and dark sections stacked on one scrollable page, each under its own explicit `Theme` override (typography scale, buttons enabled/disabled, chips, fields normal/error, a card, a real `AlertDialog` trigger, a static `snackBarTheme` preview, `SokoniEmptyState`/`SokoniErrorState`, and a static nav-bar preview). The dialog trigger explicitly wraps its `AlertDialog` in the *section's* `Theme` rather than relying on `showDialog`'s default root-navigator theme resolution — otherwise the button in the dark section would show whatever theme the *device* is actually in, not dark, defeating the point of a side-by-side audit tool.
  - **Assets, per instruction**: re-checked `assets/brand/` — still the placeholder from the startup-hang fix, now correctly sized (1080×1080, not the 6250×6250 that caused that hang), no real logo file has appeared at either canonical path. Re-checked `app/assets/fonts/` — still doesn't exist, no Plus Jakarta Sans files available. Logged both explicitly in `BLOCKERS.md` (previously only in `DECISIONS.md`, per instruction to note it there) rather than re-attempting either without new source material.
  - `flutter analyze` clean, all 3 widget tests pass, all 73 `php artisan test` cases pass (backend untouched this part, run anyway per instruction).
- **PART 2 — sign-up clarity and account intent.**
  - **What signals "show the intent screen," and why not `account_intent IS NULL`.** The obvious-looking rule — "if the signed-in user has no `account_intent` yet, show the screen" — silently breaks for every account created before this feature shipped: those rows will have `account_intent = NULL` forever unless they happen to pass through this exact flow, so a returning user from last month would hit the intent screen on their next ordinary sign-in. Used the sign-in response's own transient `is_new_account` flag instead (`User::wasRecentlyCreated`, already surfaced from `firstOrCreate`/the OTP-verify creation branch) — true only for the literal sign-in call that just created the row, never again afterwards regardless of what `account_intent` holds. `account_intent` itself stays purely a data record of the choice (including `'later'`, which is a real recorded answer, not the same as `NULL`/unanswered).
  - **One shared `completeSignIn()` helper (`features/auth/presentation/post_sign_in.dart`) rather than duplicating the terms-then-intent sequence in each of the three sign-in call sites** (phone OTP, Google, Apple) — the ordering (mark authenticated → Terms/Privacy gate if unaccepted → intent screen only for a brand-new account → otherwise just close the sign-in UI) is exactly the kind of sequence that drifts out of sync between call sites if hand-copied, and Part 2's own spec is explicit that intent must never re-show for returning users, which is easy to get right once and easy to get wrong three times.
  - **`context.go`, not `context.push`, for every step of the new-account path** (`completeSignIn` → `/account-intent`, and each of that screen's three options onward). The intent screen is reached only once, right after the sign-in sheet is torn down — pushing it would leave the sign-in sheet's route (and, if pushed further, the intent screen's own route) sitting in the back stack for the user to accidentally navigate back into after they've already made their choice. `go()` replaces the whole stack at each step instead, so there's structurally nothing to go back to.
  - **`BeemSmsGateway` bound conditionally in `AppServiceProvider`, not swapped in as the new default.** The instruction was "falling back to the log driver when unset" — read literally as a closure over both config keys (`services.beem.api_key`/`secret_key`) rather than a fixed class binding, so local dev and CI (which never set Beem credentials) keep exercising the exact same `LogSmsGateway` path as before, with zero behavior change unless the two env vars are actually populated. Locked this in with `SmsGatewayBindingTest`, since a binding-selection bug here has a much worse failure mode than most (silently starting to send, or silently failing to send, real SMS in the wrong environment) than a typical wrong-branch bug.
  - **Beem's API contract implemented from documented behavior, not tested against a live account** (no credentials — this isn't one of the four credential blockers, since a real implementation exists either way, only real *keys* are missing). `BeemSmsGateway` posts `source_addr`/`encoding`/`message`/`recipients[].dest_addr` with HTTP Basic Auth, strips the leading `+` from the E.164 number (Beem expects a bare MSISDN), and treats anything other than `successful: true` in the response — including a non-2xx HTTP status — as a hard failure (`RuntimeException`, not swallowed), since a silently-dropped OTP send is worse than a loud 500 the client can retry. Documented the full account-setup/sender-ID-registration/funding chain a real deployment needs in `docs/SMS.md`, since none of that can be verified from this environment.
  - `flutter analyze` clean, all Flutter widget tests pass, all 85 `php artisan test` cases pass (5 new Beem/SmsGateway-binding unit tests, 7 new phone-OTP/intent feature tests).
- **PART 3 — the social business layer.**
  - **Three concrete tables, not one polymorphic `posts` table.** Researched the codebase's own conventions first: `reports` is the *only* polymorphic relation anywhere in this schema, reserved for a genuinely cross-cutting concern (attaching to a heterogeneous, pre-existing set of model types). Every other relationship — Product/Review/Order/Favorite/Message — uses explicit FK columns on concrete tables. Update/Offer/Showcase are three genuinely distinct content types with different required fields (Offer needs `discount_type`/`discount_value`/`price_snapshot`/date range; Showcase needs `video_path`/`duration`; Update needs `expires_at`), so concrete tables matched house style and needed no `whereHasMorph` gymnastics for the different visibility rules each one has.
  - **Real `Customer` model with a surrogate `id`, not a bare pivot like `favorites`.** The spec explicitly asked for a denormalised `customer_count` mirroring `rating_avg`/`rating_count`, and that pattern (`#[ObservedBy]`, recompute-via-fresh-aggregate on `created`/`deleted`) only works if Eloquent model events actually fire — a bare `belongsToMany` pivot never fires them on `attach()`/`detach()`. Paid the one extra column for a real model specifically so `CustomerObserver` could exist at all; reads still go through `SellerProfile::followers()`/`User::following()` `BelongsToMany` relations for convenience, only writes go through `Customer::firstOrCreate()`/`->delete()` so the observer actually runs.
  - **Offer duration is `duration_days` (1-7), not client-supplied `starts_at`/`ends_at`.** The spec says "countdown 1-7 days," which is a length, not a date range — accepting raw timestamps would let a client backdate `starts_at` or push `ends_at` past 7 days, requiring extra validation to re-derive the same constraint the length-based input gets for free. `starts_at` is always `now()` at creation; the columns still store real timestamps (not just a stored duration) since the resource/countdown math needs a fixed `ends_at`, not day-count arithmetic redone on every read.
  - **"Save"/"Hifadhi" stays scoped to products (the existing `favorites` table/heart icon), not extended to Updates/Offers/Showcases/shops.** Re-read the spec's own data-model section: "extend favorites into Save" sits in the same list as "customers follow relation" as a *terminology* item, not a schema-extension item — `favorites` already **is** the save/bookmark mechanism the spec describes ("also bookmarks"), just needed the user-facing English copy changed from "Favourites"/"No favourites yet" to "Saved"/"Nothing saved yet" (`favoritesTitle`/`favoritesEmptyTitle`) — Swahili already said "kuihifadhi" (to save it) in the empty-state body, so it needed no change at all. Left the heart icon as-is rather than swapping to a bookmark glyph — the spec's "also bookmarks" describes the *function* Save already performs (persisting something to revisit later), not a mandated icon change, and product/detail screens across the app already use the heart consistently.
  - **No 6th bottom-nav tab for Showcase.** CLAUDE.md fixed the bottom nav at five tabs (Home/Search/Sell/Orders/Profile) back in Phase 3 and explicitly said not to re-litigate the stack — Part 3's own two mentions of "Showcase tab" are (a) a tab *within the shop profile's* `TabBar` (built exactly as asked) and (b) "the Showcase tab: full-screen vertical PageView..." describing the *experience*, not literally demanding a second bottom-nav slot. Added a `ShowcasePreviewRow` (small thumbnail strip) to the home feed instead, which pushes the same full-screen vertical feed (`/showcase`) any thumbnail anywhere (shop profile grid included) also opens — one feed, multiple entry points, zero nav-bar changes.
  - **The FAB's tap target changed from "go to Sell branch" to "open the create sheet," per spec — which orphaned the Sell tab's other job (the seller's "My Shop" dashboard) until a fix.** Before this session the "+" FAB was the *only* way back into `SellScreen`'s `_MyShopStatus`/`MyShopDashboard` once a shop existed. Fixed by making Profile's existing "Start selling" button seller-aware: it now reads `context.go(SokoniRoutes.sell)` labelled "My Shop" (reusing `myShopTitle`, no new l10n key) when `user.isSeller`, and keeps its old "Start selling" → onboarding behaviour otherwise. `go()`, not `push()`, since the Sell tab is a `StatefulShellRoute` branch, not a regular pushed screen.
  - **ShowcaseScreen's video lifecycle reuses `ProductMediaCarousel`'s active-index pattern, not `visibility_detector`** — despite it being a `pubspec.yaml` dependency since Phase 0 and CLAUDE.md's feature 7 naming it for in-feed autoplay. Confirmed via the research pass that it's imported nowhere in `lib/`; `ProductMediaCarousel`'s own docblock explains why it was never needed there either: every page in a `PageView` is already fully on- or fully off-screen, which is exactly the Showcase feed's shape too (vertical `PageView`, not a scrolling list of simultaneously-partially-visible cards). Reusing the proven, already-tested controller-lifecycle guard (`if (!mounted) { await controller.dispose(); return; }`, load-bearing against a fast-swipe race) beat debuting an unproven dependency this late for a feed that's explicitly a 3G-target, aggressive-disposal-required surface.
  - **`StoryViewerScreen` is pushed via a direct `Navigator.push(SharedAxisZPageRoute(...))`, the one screen in the app that isn't a named go_router destination.** Every other screen in this codebase is a `GoRoute`; this one takes a `List<SocialUpdate>` and a start index that have no meaningful URL representation and no deep-link use case (an ephemeral "seller X's story stack, opened from a tap" interaction, dismissed on close) — introducing go_router's `extra` parameter as a new pattern for one call site seemed like more machinery than the one genuinely un-routable screen in the app justified. Still uses the app's own `SharedAxisZPageRoute` primitive for the transition, so it's visually consistent even though it isn't in the route tree.
  - **"Seen" tracking for the Updates tray ring is local-device state only, not synced server-side.** No backend table, no API endpoint — `AppDatabase`'s existing generic `CachedKeyValues` table (already used for "last known position") stores one `updates_seen_<sellerId>` timestamp per shop the buyer has opened a story for. A second device (or a reinstall) simply shows every currently-active Update as unseen again — a minor, honest UX gap, not a bug, and consistent with how the app already treats this table's other contents as device-local. Building real synced-seen-state would mean a new table, a new endpoint, and pagination-safe upsert logic for what's ultimately a cosmetic ring colour — out of proportion to the rest of Part 3's actual commercial-content hard rule.
  - **No drift offline cache for Updates/Offers/Showcases**, unlike Products/Categories/Sellers. All three are inherently time-sensitive (an Update expiring in minutes, an Offer's countdown, a "customer_count" that's stale the moment it's cached) — a cached copy shown under the existing "showing saved results" banner would be actively misleading here in a way it isn't for a product listing that's simply a few minutes old. Skipped rather than half-built.
  - **`DatabaseDialect`/MariaDB-vs-MySQL branching wasn't reused anywhere in Part 3**, despite the instruction to reuse it "for anything MySQL-specific" — audited every new query (`scopeVisible`/`scopeActive`/`scopeFollowedBy`, the expiry cleanup command) and found nothing that needs an engine-specific function; every comparison is a plain indexed timestamp/boolean check, identical across SQLite/MySQL/MariaDB. Noted explicitly rather than silently doing nothing, since the instruction called it out by name.
  - **Terminology rename scope, stated plainly**: renamed "Products" → "Listings" on the shop profile's own tab (`shopProductsTitle`) and used "Listing"/"Bidhaa" throughout every new Part 3 string (composers, create sheet, product picker). Did **not** sweep the rest of the app's pre-existing "Product"-labelled UI copy — the product form's title bar, the seller dashboard's "Add Product" language, cart/checkout copy, search hints, and similar screens built in Phases 4-7 still say "Product." A full app-wide terminology sweep is a large, separate, mechanical pass better done as its own reviewed unit of work than folded silently into an already-large Part 3 diff; flagging the exact boundary here rather than claiming a sweep that didn't happen.
  - `flutter analyze` clean, all Flutter widget tests pass (new `StoryRing` test), all 113 `php artisan test` cases pass (31 new — including the `OfferController` `Stringable`-persistence bug the tests caught and this pass fixed).
- **PART 4 — polish & ship.**
  - **"Since the last deploy" scope, made explicit rather than assumed.** No git tag or deploy-log marker exists anywhere in the repo to anchor that phrase to — nothing has been committed since `c6de251` (Phase 10), and every "post-launch" fix session since then (the startup-hang chase, Facebook removal, logo/font fixes) touched only `app/`, never `api/`. So `git status --porcelain --untracked-files=all -- api/` against that last commit is the exact, reconstructable set of backend changes not yet on the server — used that directly rather than guessing at a boundary.
  - **The deploy zip excludes the 9 new/changed test files** (`api/tests/Feature/Social/*`, `api/tests/Unit/Services/{Geo,Sms}/*`, plus the touched `PhoneOtpTest.php`) even though they're part of the same git-diff scope — they have no functional role on the production server and aren't part of `docs/DEPLOY.md`'s own upload flow. Listed here for the record rather than silently dropped: `PhoneOtpTest.php`, `Social/{CustomerTest,OfferTest,ShowcaseTest,UpdateTest}.php`, `Unit/Services/Geo/{DatabaseDialectTest,ShopLocationSchemaTest}.php`, `Unit/Services/Sms/{BeemSmsGatewayTest,SmsGatewayBindingTest}.php`.
  - **`vendor/` isn't in the zip either**, on purpose — `composer.json`/`composer.lock` did change (the `fakerphp/faker` require/require-dev move, pre-dating this session, still undeployed), so `docs/DEPLOY.md`'s own "Updating a deployed instance" runbook already covers this: `composer install --no-dev --optimize-autoloader` on the server if SSH is available. Re-zipping a rebuilt `vendor/` is only needed on the No-SSH path the runbook separately documents; didn't duplicate that here since which path applies is a fact about the live server this environment has no way to know.
  - **Found, while auditing what's actually uncommitted, that `App\Services\Geo\DatabaseDialect`/`ShopLocationSchema` and their Unit tests were never added to git at all** (`git status` reports them fully untracked, not modified) despite `DistanceQuery.php` and the seller_profiles migration depending on them and the whole test suite passing — pre-existing debt from before this session, not something introduced here. Included them in the deploy zip regardless (the zip's job is "what's on disk vs what's live," not "what's tracked vs untracked") and flagged it here since it's a real gap in this repo's git history worth someone's attention independent of this deploy.
  - **Performance pass was mostly confirmation, not new work**: `prefer_const_constructors` has been an enforced lint since Phase 1 and `flutter analyze` came back clean across every Part 1-3 file, so const-correctness for all the new widgets is already verified by the existing lint, not a separate audit. The Phase 10 image-cache cap (`PaintingBinding.instance.imageCache`, 60MB/200 entries) is a single global setting in `main.dart`, so it automatically covers every new `CachedNetworkImage` call site in the Updates tray/Offers row/Showcase feed with zero additional code. Didn't profile the Showcase feed on a real device (none available in this environment) — the video-controller lifecycle is the same proven pattern `ProductMediaCarousel` already uses in production, including its load-bearing post-await `mounted` guard, which is the actual protection against the fast-swipe-leak class of jank a vertical feed like this risks.
  - `flutter analyze` clean, both `app-prod-release.apk`/`app-diagnostic-release.apk` built and confirmed on the real release-keystore signing path (`android/key.properties` present, so `build.gradle.kts`'s conditional never falls back to debug signing).
- **2026-08-17 — Admin panel rebuild, Section 1: seller verification's real bug was never in `forceFill`/the policy/the request lifecycle — it was that the only page with the evidence had no buttons.** Diagnosed with the real Filament Livewire action, not guesswork: `Livewire::test(ListSellerProfiles::class)->callTableAction('verify', $seller)->assertHasNoTableActionErrors()` passed cleanly on the very first try, backend untouched — the `forceFill(['status'=>'verified','verified_at'=>now(),...])->save()` closure, `SellerProfilePolicy` (irrelevant here: a custom-named `Action` isn't auto-gated by a policy method that doesn't exist for it — `update()` is the only method on that policy, for the seller's own app-side edits, not this), and the request lifecycle were all already correct. Then checked the page an admin actually needs to review a submission — NIDA photo, licence, map — via `Livewire::test(ViewSellerProfile::class, [...])->instance()->getCachedHeaderActions()`: **empty array.** `ViewSellerProfile` was a bare, unmodified `ViewRecord` with no header actions at all; verify/reject only existed as row buttons on the *list* table, which shows shop name/owner/category/status/NIDA number/submitted date — none of the evidence needed to decide. `SellerProfileInfolist`'s own docblock even rationalized this ("a reviewer can act straight from the queue without an extra click into each record") — a reasonable-sounding claim that doesn't survive contact with what the list table actually displays. Also caught, while diagnosing: the *existing* "verify" test (`FilamentPanelTest::test_an_admin_can_verify_a_pending_seller`) never called the real action at all — it called `$seller->forceFill([...])->save()` directly in the test body, so it would have passed regardless of whether the button was reachable. That's exactly the class of false confidence the task's instruction to "diagnose properly" was guarding against.
  - **Fix**: extracted `SellerProfileResource::verifyAction()`/`rejectAction()` as shared static factories (both return `Filament\Actions\Action`, the same class Filament v5 uses for both table row actions and page header actions, so nothing needed re-implementing) and wired them into `ViewSellerProfile::getHeaderActions()` alongside the existing table row actions, which now call the same two factories instead of duplicating the closures. Behavior identical everywhere; the only change is that the review page can now act on what it shows.
  - **Replaced the false-confidence test** with real Livewire-driven coverage in `FilamentPanelTest.php`: verify from the view page (asserting the seller's *previously 404'ing* product becomes `GET`-able immediately after, per the task's explicit ask), verify from the list table, reject from the view page with a reason, and reject rejecting an empty reason. Left `SellerVerificationTest::test_verifying_a_seller_makes_their_products_appear_in_the_feed` as-is — its docblock is honest that it's testing the model/scope layer in isolation, a legitimate narrower scope, now backed up by the real end-to-end version in `FilamentPanelTest.php`.
  - `php artisan test`: 150/150 passing (was 147). No Flutter/SMS/seeder files touched, per instruction.
- **2026-08-17 — `CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate` against `api.sokoni.co.tz`: two hypotheses raised and ruled out, root cause still open.** Reported on-device with SSL Labs and Android trust paths both showing no problem. Audited the whole app for any custom `SecurityContext`, `HttpOverrides`, bundled CA `.pem`, `badCertificateCallback`, or Android `network_security_config.xml` — found none; `dio_client.dart` builds a completely vanilla `Dio(BaseOptions(...))` with the default adapter untouched. Raw handshake via `openssl s_client -connect api.sokoni.co.tz:443 -servername api.sokoni.co.tz -showcerts` shows the server sends exactly 2 certificates (leaf `CN=api.sokoni.co.tz` + intermediate `SSL.com TLS Issuing RSA CA R1`, chaining to root `SSL.com TLS RSA Root CA 2022` which is correctly *not* sent), `Verify return code: 0 (ok)` — chain as served is complete, ruling out a missing-intermediate theory. **Then tested whether Dart's own bundled root store (same compiled-in BoringSSL bundle on every platform, including Android — it never delegates to the OS trust store) accepts this chain at all**, independent of the phone: a throwaway `dart:io`-only script (`_throwaway_tls_check.dart` at repo root, no dio, no packages) hitting `https://api.sokoni.co.tz/api/categories` via `dart run` returned `STATUS: 200` on this Windows machine. **This rules out "Dart's bundled store doesn't trust the SSL.com 2022 root" as the cause — do not revisit that theory.** Whatever's actually rejecting the cert on-device is something this Windows-side check can't see: a real lead surfaced along the way is the leaf certificate's very recent `NotBefore` (Aug 14 2026, three days before this was reported), suggesting a recent CA/cert rotation worth chasing — device clock skew and a carrier-level TLS-intercepting proxy (only reachable from the phone's actual mobile network, not from here) are the two live hypotheses being checked next, on-device. No code changed for any of this — audit and diagnosis only.
- **2026-08-18 — Admin rebuild Section 2: activity log built custom, not via a package.** `spatie/laravel-activitylog` (or the Filament plugin wrapping it) is the conventional choice, but installing a new Composer dependency mid-task adds a network round-trip and a version-resolution risk this environment can't easily recover from if it goes wrong, for a feature whose actual requirement ("who, what, when, why", queryable by causer/subject) is a handful of columns. Built `activity_logs` + `ActivityLog` + `App\Support\ActivityLogger::record()` instead — full control over the exact shape, zero new dependency risk. If richer features are ever needed (diffing changed attributes, etc.) the spatie package is the natural upgrade path later.
- **Roles: `role` is a second column alongside `is_admin`, not a replacement for it.** `is_admin` already gates `canAccessPanel()` throughout the existing test suite and codebase; redefining what it means (e.g. repurposing it as the role itself) would have touched every existing admin-permission check for no real benefit. `role` only answers "which kind of admin" and is meaningless when `is_admin` is false — kept nullable rather than defaulting new non-admin users to some third value.
- **`Report::AUTO_HIDE_THRESHOLD` and `ProductMediaController::MAX_ITEMS` deleted, not deprecated-and-kept**: both were only referenced in exactly the one place now reading `App\Support\Settings` instead — grepped for other usages before removing either, found none. Keeping a now-dead constant around "just in case" is exactly the kind of stale, misleading code CLAUDE.md's own working rules argue against.
- **Settings page fields are plain `TextInput::make(...)->numeric()`, not `Select`/slider controls**: every one of the four values (radius km, media count, offer days, report threshold) is a single admin-tunable integer/float with a sensible min/max, not a fixed enum of choices — a numeric text field with `minValue`/`maxValue` is the honest match for that, and matches how Filament's own generators would scaffold it.
- **Admin rebuild Section 4: Products/Orders admin-view policies added, not the existing owner-scoped ones repurposed.** `ProductPolicy::update()`/`OrderPolicy::view()` already existed for the app's own seller/buyer authorization and correctly return false for an admin (an admin is neither the owning seller nor the buyer) — Filament's `HasAuthorization` checks the SPECIFIC ability method by name per action (`viewAny`/`view`/`update`/etc.), so adding NEW `viewAny`/`view` methods that OR in `$user->is_admin` alongside the existing owner check was additive and left the API's own authorization untouched; there was no need to touch `update()`/`cancel()`/etc. at all since those still correctly gate the owner-only mutation paths.
- **Admin rebuild Section 4: Updates/Offers/Showcases moderate via hard delete, not a new `is_hidden` column.** Reviews and Comments genuinely needed a reversible hide (an admin might want to un-hide a borderline review after re-reading it, and the public rating average needs to recompute either way) — these three don't have the same "might want it back" shape: Updates already auto-expire in 24h by design, and a removed Offer/Showcase is trivial for a seller to just repost if it was a mistake. Adding a third moderation flag pattern for content that's already ephemeral would be schema churn without a real behavioral payoff; the removal is still fully logged via `ActivityLogger` either way, so there's no loss of accountability, just no soft-hide/restore UI for these three specifically.
- **Admin rebuild Section 5: dashboard widgets rely on the panel's existing `discoverWidgets()`, not manual registration in `AdminPanelProvider`.** Confirmed this was already wired (`SokoniStatsOverview` was rendering before any of this session's changes) rather than assumed — every new widget just needed to exist in `app/Filament/Widgets/` with the right namespace to be picked up automatically. Explicit `$sort` on each controls layout order since discovery order isn't guaranteed to match file-creation order.
- **Admin rebuild Section 6: CSV export intentionally skips Filament's native `ExportAction`/`ExportBulkAction`.** These exist in the installed `filament/actions` package and would have been the "obvious" choice, but they dispatch an async job (`PrepareCsvExport`/`ExportCsv`) tracked via an `exports` table that needs its own migration — and this app's actual production target (cPanel shared hosting, `docs/DEPLOY.md`) has no queue worker process, only a once-a-minute `schedule:run` cron entry. A `QUEUE_CONNECTION=database` job with nothing ever calling `queue:work` sits in the `jobs` table indefinitely — an export button that silently never produces a file is worse than no button. Built `App\Support\CsvExporter` instead: a plain synchronous `StreamedResponse` returned directly from a Filament `Action` closure. Verified (by reading `Filament\Actions\Concerns\InteractsWithActions::callMountedAction()`, not assumed) that this is a real, supported path — the method returns whatever the action closure returns, and Livewire 3 natively converts a returned `Response`/`StreamedResponse` from a component method into the actual HTTP response sent to the browser. Exact same reasoning CLAUDE.md itself already used for chat (5s polling, not a WebSocket server) given the identical hosting constraint — this isn't a new precedent, it's the same one applied again.
  - **Testing the export required a different approach than testing a normal action**: `Livewire::test(...)->callAction('exportCsv')` doesn't cleanly expose the resulting `StreamedResponse` object for inspection the way it does for actions that just mutate a record — chasing the exact internal plumbing Livewire uses to detect and forward a returned Response cost real time before deciding it wasn't worth it. Split into two more reliable pieces instead: a plain unit test of `CsvExporter::stream()` (proves the CSV bytes and headers are correct) plus a test of `$page->getFilteredSortedTableQuery()` after applying a table filter (proves an export triggered from that state would include exactly the right rows) — together these prove everything that matters without needing to reverse-engineer Livewire's response-capture internals in a test.
  - **Testing global search hit the same class of friction, resolved the same way**: `Livewire::test(\Filament\Livewire\GlobalSearch::class)` in isolation returned zero results even with `Filament::setCurrentPanel('admin')` set manually — the panel's resource registry is normally reachable through request middleware that a bare component test never runs. Confirmed the actual resource-level method (`Resource::getGlobalSearchResults()`) works correctly when called directly with the same panel/auth state, and tested at that level instead — Filament's own `GlobalSearch` Livewire wrapper around it is the framework's tested code, not this session's.
- **2026-08-18/19 — Public website (sokoni.co.tz), server-rendered in the same Laravel app.** Built as `routes/web.php` + `App\Http\Controllers\Web\*` on the plain `web` session guard, deliberately kept separate from both the API's `sanctum` guard and the admin panel's Filament `web` guard usage — same underlying Laravel guard as the admin panel (both are session-based), but a distinct login flow/user journey, so `web.*`-namespaced routes throughout rather than reusing `admin.*` or bare route names, avoiding the exact "route [login] not defined" class of collision this caused once already (Laravel's default `Authenticate` middleware assumes a route named literally `login`; fixed via `redirectGuestsTo(fn () => route('web.login'))` in `bootstrap/app.php`).
  - **Shared business logic extracted into `App\Services\*` rather than the web controllers calling the API controllers internally, or duplicating the logic.** `ProductSearchService` (search/sort/pagination-by-distance, previously private methods on `Api\ProductController`), `SocialUserResolver` (previously inline matching logic in `Api\AuthController::socialLogin`), and `SellerDashboardStats` (previously inline in `Api\SellerDashboardController::show`) are now called by both the API controller and the new web controllers — confirmed behavior-preserving by re-running the existing API test suite after each extraction, not just the new web tests.
  - **`opening_hours` added to `seller_profiles` as a nullable JSON column, web-consumed only — the Flutter app's Edit Profile screen is explicitly NOT built as part of this task.** The shop profile page needs real opening-hours data for both its own display and `LocalBusiness` schema.org markup, but building the app-side editor is outside this task's scope (a website-build task, not an app task) — sellers can't actually set this yet from anywhere; `OpeningHours::parse()` treats a null/missing value as "hours not provided" everywhere it's read, so the page degrades cleanly rather than showing fabricated hours. Flagged here explicitly since it's exactly the kind of half-finished-looking feature CLAUDE.md's working rules would otherwise flag — the column and rendering are real and complete, only the input surface is deliberately deferred.
  - **`Legal::TERMS_VERSION` (PHP) mirrors the Flutter app's `sokoniTermsVersion` (Dart) as two independently-maintained constants, not a single generated source.** No cross-language codegen/shared-config mechanism exists in this repo, and building one just for a single version string would be disproportionate — both constants must be bumped together by hand whenever Terms/Privacy content changes; the web Terms/Privacy pages port the same verbatim EN/SW text from the Flutter screens for the same reason (see the Phase 9 decision on legal text) so they only need to actually agree at the moment either one changes, not stay mechanically in sync.
  - **Web chat reuses the app's 5-second-polling architecture, not real-time, via a new session-authenticated `poll()` JSON endpoint** (`Account\MessagesController::poll`) rather than trying to route the browser through the API's Sanctum-token auth. The browser session has no Sanctum token to present, and issuing one just for this would mean either exposing a token to client-side JS (a real XSS/theft surface a plain session cookie doesn't have) or a second silent auth mechanism running alongside the session — polling under the existing `web` guard keeps exactly one auth boundary for the whole website. Matches CLAUDE.md's own no-WebSocket-on-shared-hosting reasoning already applied twice (app chat, admin CSV export).
  - **"Near you" on the website is a fixed Dar es Salaam anchor point (`App\Support\DarEsSalaam::LAT/LNG`), not real device geolocation.** The website's whole architectural premise is server-rendered HTML for SEO (explicitly stated as job #1 in the task) — a geolocation-gated first paint would mean either blocking render on a browser permission prompt (bad for both SEO crawlers, which don't grant permissions, and real users on slow 3G) or a client-side re-fetch after the fact, which is exactly the "no client-side-only rendering" pattern the task rules out for anything that matters to indexability. A future enhancement could progressively re-rank an already-rendered page client-side once/if permission is granted, without changing what Google indexes — not built now since it's pure enhancement, not core to any of the 9 required sections.
  - **`public/.htaccess` gained `mod_deflate`/`mod_expires`/`mod_headers` blocks** (gzip for text/html/css/js/json/xml, far-future immutable caching for content-hashed Vite build output, a 1-week cache ceiling for uploaded media/fonts) for the task's performance section (target: usable first paint under 3s on 3G). Scoped by `<FilesMatch>`/content-type rather than blanket directory rules, so the API's JSON responses (served from the same `public/` docroot on `api.sokoni.co.tz`) are unaffected — this `.htaccess` is shared by both hostnames per the deployment plan, and an API JSON response getting an aggressive HTML cache header would be a real bug, not just an inefficiency.
  - **Explicitly deferred, per the task's own "out of scope" list — noted here rather than silently dropped:**
    - **New listing verticals** (Real Estate, Jobs, Events, Courses) — each needs its own schema (a job listing has no `condition`/`stock`, a real-estate listing needs bedroom/bathroom/lot-size fields product's schema has no room for) — not attempted. Kept the category system data-driven (`CategoryCatalogService`, no hardcoded category list in any controller/view) specifically so a future vertical's categories can be added without a code change, even though the *listing* schema itself would still need real work.
    - **Subscription tiers / paid placement billing** — `products.is_sponsored`/`sponsored_until` (added in an earlier phase) already exist and the website's search/category results correctly pin and label sponsored listings (`ProductSearchService::sponsoredSlot()`, always with a visible "Sponsored" chip, never disguised — a direct requirement), but no billing/checkout flow toggles that flag; it's still only settable the way it already was (a seller-side boost dialog with no payment step, per the Part 3 decision this predates). Website surfaces the flag correctly; doesn't add a way to pay for it.
    - **Banner advertising inventory** — no ad-slot schema, placement system, or UI added anywhere. Nothing in the current design reserves space for this, so adding it later is a real page-layout change, not a hidden gap — flagged as a genuine future scope decision, not a deferred-but-ready slot.
    - **AI listing generation / natural-language search** — `SearchController`/`ProductSearchService` do plain field matching (title/description `LIKE`/full-text per the existing `Product::scopeSearch()`), no embedding/LLM layer. `SearchFilterInput::fromRequest()` is a clean single entry point for query parsing, so a future NL-query layer could sit in front of it without restructuring the controller, but nothing NL-aware exists now.
  - `php artisan test`: 250/250 passing (38 new web tests: Home/Category/Search/Product/Shop pages, Auth flow, Account area, SEO infrastructure).
- **2026-08-20 — Production 500 incident, Part 1: cache audit, a route-sweep regression test, one confirmed-and-fixed bug, and one honestly-unresolved one.**
  - **Full-codebase audit of every `Cache::remember`/`Cache::rememberForever`/`Cache::put` call** (repo-wide grep, not scoped to `Web/`): only four call sites exist. `CategoryCatalogService::withCounts()` and `HomeController::__invoke()` cached Eloquent model/Collection graphs — both already stripped of caching in the prior hotfix (see the entry above this one). The other two — `SeoController`'s three sitemap caches (`web:sitemap:products/shops/categories`) and `PhoneOtpService::requestCode()`'s OTP-code cache — were read in full and confirmed to cache only plain strings (`view(...)->render()` output, a 6-digit code string), never an object. Left unchanged; flagged here as *audited*, not just *unexamined*. No other `cache()` helper usage exists anywhere in `app/`.
  - **No other serialization boundary found.** Checked session writes app-wide (`session()->put`/`session([...])`): every one stores a scalar (phone string, bool, locale string) — never a model. No `ShouldQueue` job exists anywhere in the codebase and no code calls `dispatch()`/`Bus::dispatch()`, so `QUEUE_CONNECTION` is moot regardless of its production value. No `Cache::store()` override anywhere. Config values are plain scalars/arrays, and `config:cache` serializes via `var_export`, not PHP `serialize()`/`unserialize()`, so it isn't a plausible vector for `__PHP_Incomplete_Class` specifically (unlike a stale/misdirected cache store).
  - **New regression test**: `tests/Feature/Web/AllWebRoutesAvoidServerErrorsTest.php` walks every registered `web.*` GET route via the real router (`Route::getRoutes()`, not a hardcoded URL list — a future route is covered automatically), substituting real seeded records for every route parameter, split into a guest-accessible sweep and an authenticated-account sweep based on each route's actual resolved middleware. Only 5xx fails the test; a redirect/403/404 from an auth or ownership check is a legitimate, passing outcome. This test is exactly the guard that should have caught the next bug before it shipped.
  - **Confirmed, root-caused, and fixed: `/account/messages/{conversation}` was a genuine 500 in every environment, unrelated to caching or production schema.** The route sweep caught it immediately, locally, on first run. Root cause: `resources/views/web/account/message-thread.blade.php` passed a multi-key array literal (`$conversation->messages->map(fn ($m) => ['id' => ..., 'body' => ..., ...])`) directly inside `@json(...)`. Laravel's `@json` directive (`Illuminate\View\Compilers\Concerns\CompilesJson::compileJson`) does a plain `explode(',', $expression)` to separate its value argument from an optional encoding-options/depth argument — it has no paren/bracket awareness at all, so every comma *inside* the array literal was treated as a directive-argument separator, truncating the compiled `json_encode(...)` call mid-array into syntactically invalid PHP. Confirmed by reading the actual compiled output in `storage/framework/views/` (not just Blade's own error message, which mis-reports the failure as an "unclosed bracket" on the wrong line) — the compiled file genuinely stopped at `'attachment' => $m->attachment)`, cutting off `mine`/`sender_name`/`created_at` and the closing `])`. Fixed by computing the array into a plain `$initialMessages` variable in a `@php` block first, then passing `@json($initialMessages)` — a single comma-free argument, sidestepping the directive's naive parsing entirely. Grepped every other view for `@json(`: this was the only occurrence, so the bug class doesn't recur elsewhere.
  - **A second, genuinely separate Blade quirk surfaced and was fixed while building that same fix**: two `@php` directives back-to-back with no blank line between them — one in the parenthesized inline form (`@php($x = ...)`), one in block form (`@php ... @endphp`) — corrupted Blade's directive parsing, leaving the first `<?php` tag unclosed in the compiled output and dumping the literal, uncompiled text `@php` into the middle of executable PHP (confirmed by inspecting the compiled file directly, the same way the first bug was confirmed). Fixed by merging both assignments into a single `@php ... @endphp` block rather than stacking two directives adjacently — the safe, general pattern for any future edit to this file.
  - **The originally-reported bug, `/c/{category}` (specifically `/c/food-groceries`) returning 500, was NOT reproduced locally and its root cause was NOT identified — stated plainly rather than guessed at.** Read `CategoryController`, `CategoryCatalogService`, `ProductSearchService`, `SearchFilterInput`, `Product`'s scopes (`visible`, `sponsoredActive`, `search`, `inCategory`, `forSeller`), `TanzaniaRegions`, and `web/category.blade.php` plus every partial it includes, end to end. Tested locally against a real request/response cycle (not just static analysis) across every variant that could plausibly differ: a category with no children and no products (the empty-state branch), a category with children and products (the populated branch), and every filter combination (`region`, `price_min`/`price_max`, `condition`, `has_video`, `sponsored`) both with and without a child segment — all pass cleanly with `assertOk()`, including with `withoutExceptionHandling()` enabled to surface any swallowed exception. The two known MySQL/MariaDB-divergence points in this exact code path (`Product::scopeSearch()`'s `whereFullText` branch, `DistanceQuery`'s `ST_Distance_Sphere` branch) are both provably unreached for a plain category-browse request with no `q`/`lat`/`lng` parameters, so this isn't a repeat of the earlier `shop_location`-generated-column MariaDB incident. Ruled out stale autoloading/classmap as a cause of *this* bug specifically — not by assumption this time, but because the user's own report confirms the homepage (which depends on multiple of the same newly-deployed classes, including `CategoryCatalogService` itself) is rendering correctly in production, which wouldn't be possible if those classes weren't resolving. What's left unruled-out, because it requires production access this session doesn't have: a genuine schema drift between production's actual database and what this code assumes (e.g. a migration from an earlier, pre-this-session deploy zip that was never actually run) — no evidence for this beyond it being the only category of explanation not yet eliminated by a passing local test. **Next step, not yet taken**: read the real exception from `storage/logs/laravel.log` on the production server immediately after reproducing the 500 there — Laravel logs the full exception and stack trace on every unhandled 500 regardless of `APP_DEBUG`, so that log line will name the actual file/line/class in a way no further local guessing can.
  - `php artisan test`: 253/253 passing (3 new — the route sweep's two bucketed tests plus an explicit no-child-segment category assertion).
- **2026-08-20 — Production layout incident, Part 2: root cause found and fixed, not just patched around.** `resources/css/app.css`'s `@theme` block named only 7 specific spacing steps (`--spacing-4` through `--spacing-32`) as overrides, on the mistaken assumption this fully replaced Tailwind v4's numeric spacing scale. It didn't: Tailwind v4 generates *every* numeric spacing/sizing utility (`p-N`, `gap-N`, `w-N`, `h-N`, ...) as `calc(var(--spacing) * N)` from one base multiplier (default `0.25rem`), falling back to that default for any `N` not explicitly named — confirmed empirically by grepping the actual compiled CSS, not assumed: `.py-14 { padding-block: calc(var(--spacing) * 14) }` resolved to 56px, not the 14px every view's own class names assume. Every one of the ~50 templates built this website used spacing/sizing numbers far beyond the 7 named steps (`py-10`, `py-14`, `py-40`, `py-56`, `h-24`, `h-48`, ...) on the working assumption that "N" always means "N pixels" — a reasonable, in fact *correct*, mental model that the CSS config simply failed to implement consistently. This one bug explains all three of Part 2's visual symptoms at once: oversized search inputs (padding utilities 4x too large), horizontal scrollbars (any container/gap past the 7 named steps overflowing), and "nothing renders below the hero" (a hero section with genuinely-160px-not-40px vertical padding on each side, plus similarly bloated form padding, can easily run well past 1500px tall — confirmed this wasn't *also* a separate missing-content bug by strengthening `HomePageTest` to assert real product/offer/shop content is actually present in the response body, which it is). **Fix: override the base `--spacing` multiplier itself (`--spacing: 1px`)**, not the seven named steps — every numeric utility site-wide now means literally its number in pixels, matching what every template already assumed, with zero exceptions instead of seven.
  - **Two genuine outliers, audited for individually rather than assumed away.** Grepped every `w-N`/`h-N`/`max-w-N`/`min-w-N` usage across every view before trusting the global fix — the overwhelming majority (icon sizes, avatar/logo sizes) were already correct literal-pixel values that had been rendering *4x too large* under the old bug (a "48px" category icon rendering at 192px is exactly the "oversized" complaint), confirming the fix was right for those. Two were the opposite case — a value that had been *accidentally correct* under the old broken 4px-per-step default: the category/search sidebar (`lg:w-64`, intended as Tailwind's own default 256px sidebar) and the home page's offer-card width (`w-56`/`w-64`, intended as 224px/256px card widths) — both would have shrunk to a genuinely-broken 56-64px under the corrected scale. Fixed with explicit `w-[256px]`/`w-[224px]` arbitrary values at those two call sites specifically, rather than either leaving them broken or watering down the global fix with more named exceptions.
  - **Header search duplication was a missing page-scope check, not a CSS bug**: the header's compact search form used `hidden md:flex` (correct responsive behavior on its own — hidden on mobile, visible at desktop widths) but had no awareness of which *page* it was on, so it rendered at full size on the home page too, right alongside the hero's own dominant search. Added `request()->routeIs('web.home')` and suppress the header's copy specifically there — the header search is for every other page, which has no hero to carry it.
  - `php artisan test`: 254/254 passing (1 strengthened — `HomePageTest` now asserts real below-the-hero content, not just a 200 status).
- **2026-08-20 — Production layout incident, Part 3: design system pass, scoped honestly rather than claimed complete.** Given the size of the full brief, prioritized the highest-leverage, most-repeated, and explicitly-required pieces over exhaustively hand-polishing every template:
  - **Done**: the full typography scale (Display/H1/H2/H3/Body/Small/Caption, tight tracking on 24px+) and text-hierarchy color tokens (`#4A4A48` body, `#8A8A85` caption) as reusable `.text-*` component classes in `app.css`; a `.product-card` hover treatment (2px lift, shadow, 1.02 image scale, 200ms ease-out) applied to the actual card component, replacing its previous ad hoc hover classes; an offer badge (top-left, yellow, takes precedence over the sponsored badge when both apply) added to the product card, which previously only showed a sponsored badge and never surfaced an active offer at all; a `.btn-whatsapp` green button class, now used both on the product page's WhatsApp action (previously indistinguishable black-outline `.btn-secondary`, contradicting the explicit "WhatsApp is secondary, in its own green" requirement) and its new mobile sticky-bar counterpart; a sticky gallery on the product page's desktop two-column layout; a mobile bottom sticky bar on the product page with Order and WhatsApp always reachable (didn't exist before); a full 4-column dark footer (About/Categories/For sellers/Support) with a real language switcher, social links, and app-store badges, replacing the previous light 3-ish-column footer that didn't match any of the named columns; branded 404/500 error pages (there were none — Laravel was serving its bare default framework pages) with an illustration, one sentence, and one action each, 404 additionally offering popular categories; a trust band and "See all" links on every home-page product/shop section that didn't already have one, each converted to horizontal-scroll-on-mobile instead of a squashed grid; a sticky header with a scroll-triggered shadow after 100px; a fade-masked, scrollbar-hidden category strip with the active category highlighted in yellow; a `prefers-reduced-motion` media query disabling all transitions/animations globally, and a plain-JS (not Alpine — a one-shot reveal has no interactive state) `IntersectionObserver` driving the section-heading fade-in-on-scroll, skipped entirely when reduced motion is requested.
  - **Deliberately not built this pass, flagged here rather than silently skipped**: the desktop category mega-menu (a genuinely separate, larger interaction pattern than anything else in this list); a mobile header with a search icon distinct from the hamburger menu (current mobile header still opens search *inside* the same hamburger panel — functional, 44px tap targets, no overflow, just not pixel-identical to the spec's three-separate-icons description); a deeper visual restyle of the category/search results page and the shop profile page beyond the width-scale fixes both already received as part of Part 2's overflow audit; a skeleton shimmer that's actually wired to a real "image not yet loaded" JS event rather than the current CSS-only approximation (a shimmer background sits behind every product-card image via `.skeleton`, but since there's no loading-state class toggle, it's only ever visible for the brief real gap before an image paints — correct in spirit, not the full "blocks render distinctly before/after load" treatment more JS would give it). None of these are needed for any route to return 200 or for the overflow/duplication bugs to be fixed — they're pure polish, deferred for a follow-up pass rather than rushed.
  - `php artisan test`: 256/256 passing (2 new — both branded error-page tests).
- **2026-08-20 — Second production incident round, Part 1: /c/{category} still open, diagnostic tooling shipped instead of more guessing.** Per instruction, stopped trying to reproduce locally and built the tools to let production answer directly. Added `/_diagnostic/log?token=...` (`DiagnosticLogController`), a token-gated route tailing the last 50 lines of `storage/logs/laravel.log` as plain text with no SSH needed — reads the token via `env()` directly rather than `config()`, a deliberate, commented exception to normal Laravel convention so it works the moment `DIAGNOSTIC_LOG_TOKEN` is set in `.env` with no `config:cache` round-trip; 404s (not 403, so it doesn't confirm its own existence) whenever the token is unset or wrong. Marked TEMPORARY in three places (the controller's own docblock, the route's comment, `.env.example`) with the exact two artifacts to delete once the incident is closed. Wrapped `CategoryController`'s entire body in a try/catch that logs with a grep-able `[CATEGORY_500_DIAGNOSTIC]` marker (exception class, message, file, line, a trimmed trace, and the request's category/child/query params) and re-throws unchanged — visibility only, no behavior change, confirmed by the full suite staying green.
  - **Went looking for the two divergences the instruction named as candidates, rather than just building tooling and stopping.** `CategorySeeder` (the real production category set — same 10 rows, same icons, same names) creates zero subcategories; nothing else in this codebase creates one either except the admin panel itself, which means a fresh local seed structurally *cannot* reproduce a "category with children" bug without deliberately constructing one — so built that scenario by hand (a real child category, a real product under it) and ran the actual route against it: 200, both for the parent and the child. Also specifically exercised the one code path the category page runs that the home page never does — `Product::scopeSponsoredActive()`, called unconditionally on every `/c/*` load via `ProductSearchService::sponsoredSlot()` — with a real sponsored, in-date product: also 200. Both are now permanent tests (`CategoryPageAllSeededCategoriesTest`), not just one-off checks, alongside a sweep of the real 10-category set (not synthetic factory data) through the actual route.
  - **Status: still open.** Every candidate this session could test for — real seeded categories, a category with a real child, a live sponsored product, every filter combination, the empty-state branch — returns 200 locally. The diagnostic route and the CategoryController log marker are what should finally close this: hit `/c/food-groceries` in production, then `/_diagnostic/log?token=<the value set in .env>`, and look for a `[CATEGORY_500_DIAGNOSTIC]` line — it will name the real exception class, message, file and line directly, which no further local guessing can substitute for.
  - `php artisan test`: 262/262 passing (6 new — 3 diagnostic-route tests, 3 every-seeded-category tests).
- **2026-08-20 — Second production incident round, Part 2: category icons were printing raw Material Symbols names as text.** `categories.icon` stores names like `devices`/`checkroom`/`restaurant` for the Flutter app's bundled Material Icons font to resolve; the website has no icon font (deliberately — a whole font for ten glyphs on a 3G connection is the wrong tradeoff CLAUDE.md's own performance section already ruled against), so the raw string was rendering as literal visible text in the "Browse categories" cards — the only place in the entire codebase `->icon` is printed (grepped the whole `resources/views` tree, not assumed). Built `<x-category-icon>`, a Blade component mapping each of `CategorySeeder`'s real 10 icon names to a small hand-authored inline SVG (Heroicons-outline style, matching every other icon already used across the site — `stroke="currentColor" stroke-width="1.5"`, 24×24 viewBox) plus a generic shopping-bag fallback for any future unmapped name (e.g. one added later via the admin panel), so an unrecognized icon degrades to a sensible generic glyph instead of reprinting raw text or erroring. No icon font, no extra HTTP request — every icon is ~150-250 bytes of inline markup, cached with the rest of the page's HTML. Covered by a test asserting the raw icon-name string never appears as visible text for any real seeded category, plus a dedicated fallback case.
  - `php artisan test`: 264/264 passing (2 new).
- **2026-08-20 — Second production incident round, Part 3: Amazon-cue design pass.**
  - **Alignment/rhythm**: audited every product/shop-card grid on the public site (home, category, search, product's similar/more-from-shop, shop profile) for gap consistency — found one real inconsistency (the home page's own category grid used `gap-12` while every other grid used `gap-16`) and standardized all of them to `gap-16 lg:gap-24` per the explicit spec. Equal card heights and a shared price baseline were mostly already structural (CSS Grid's default `align-items: stretch` plus each card's own `flex flex-col` + `mt-auto` footer), reinforced with an explicit `h-full` on both `product-card` and `shop-card` rather than relying solely on the implicit default. Section headings already shared the same container/padding as the card grids beneath them (`mx-auto max-w-7xl px-16 lg:px-24` throughout), so no change was needed there.
  - **Banners** (`banners` table, `Banner` model, `BannerResource` under a new "Marketing" nav group): `position` is a fixed enum (`home_hero`/`home_mid`/`category_top`/`sidebar`), not free-text — every rendering site only ever asks "give me the live banner for this exact slot," so an enum keeps that a plain `WHERE`, no placement-registry concept this schema doesn't otherwise have. `<x-banner-slot>` renders at most one banner per position ("restrained," not a carousel) and emits genuinely nothing — no wrapping element, no placeholder box — when the slot is empty, proven by a test asserting the slot's own markup is entirely absent when no banner exists. Impressions are counted server-side, once, at the exact point in the request lifecycle a banner is actually about to be shown (inside the Blade component itself, not a separate tracking call that could fire without a real render); clicks go through `/banners/{banner}/click`, which increments a counter then redirects to the real (possibly external) URL via `redirect()->away()`. Wired restrained per the instruction — one `home_hero` slot below the hero, one `home_mid` between sections — plus `category_top` and `sidebar` slots on the category/search pages, since building admin support for a position nothing ever renders would be its own kind of half-finished feature.
  - **A real bug caught by the admin resource's own tests, not shipped and found later**: the banner form's `FileUpload` field converts the uploaded path to a full public URL on save (`dehydrateStateUsing`, matching the "full URLs stored" convention every other media field in this app already follows — product photos, seller logos), but the first version had no reverse conversion for *loading* an existing banner back into the edit form. Filament's `FileUpload` expects the value it's hydrated with to be in the same disk-relative shape it stores by default; handing it a full URL instead made it treat every existing banner as having no image, and since the field is required, editing *any* field on an existing banner without also re-uploading a new image failed validation outright. Caught by a test that edits a real banner without touching its image and asserts the save succeeds — it didn't, on the first pass — fixed by adding `afterStateHydrated`, stripping the disk's own base URL back off the stored value so `FileUpload` recognizes the existing file. The same asymmetric-hydration shape is worth remembering for any future `FileUpload` field that also needs a stored-value transform.
  - **Language dropdown**: replaced the side-by-side EN/SW text-chip pair with a real dropdown (globe icon, current locale, a click panel listing both languages with a checkmark on the active one) — Amazon's pattern, language only, per instruction.
  - **No currency switcher — the reasoning, as instructed.** Sokoni is a single-currency Tanzanian marketplace: sellers price in TZS, `Money::format()` only ever renders TZS, checkout is cash-on-delivery/pay-on-pickup settled in TZS, and there is no payment gateway, no stored FX rate, no dual-pricing schema, and no settlement policy for any other currency anywhere in this codebase. A currency dropdown here would do one of two things, both wrong: silently convert displayed prices using a rate that's either hardcoded-and-immediately-stale or fetched from a live FX API this app has no integration for, while the actual amount a buyer pays (cash, in TZS, on delivery) never changes — or require building live rates, stored dual pricing, and a real settlement/refund policy across currencies, none of which exist and none of which this task asked for. Amazon shows a currency selector because Amazon is a multi-country business that actually settles transactions in more than one currency; Sokoni is not that business. If a client requests this later, the honest minimum version is a **display-only** converter (an approximate reference price in a second currency, refreshed from a real rate source, with a visible "prices settle in TZS" disclaimer next to it) — a scoped feature with its own rate-source decision and UI treatment, not a dropdown bolted on next to the language switcher.
  - **Header two-tier restructure**: the category strip moved from a light band under the main row into its own dark (`bg-sokoni-black`) secondary bar — Amazon's two-tier pattern — so category navigation has a clear visual home instead of sharing a border-adjacent strip with the search row. Everything else in the header (single 72px row, search hidden on the home page, sticky-with-shadow-after-100px) was already built in the prior session's Part 2/3 pass and needed no further change.
  - `php artisan test`: 277/277 passing (13 new — 7 banner tests, 4 admin banner-resource tests, 2 language-dropdown tests).
- **2026-08-22 — Tester feedback round, Part 1: buyer/seller registration.** The intent screen already existed (`Web\Auth\IntentController`), but choosing "sell" led nowhere real: `ShopDashboardController` redirected a seller-less user to `web.sell`, a static marketing page whose only CTA pointed at `/login` — even for an already-authenticated visitor. There was no seller registration form on the website at all, matching the tester report exactly.
  - **One page, four sections, reusing the app's own validation rather than re-typing it.** `SellerWebRegistrationRequest` composes its rule set directly from the four existing step requests (`SellerOnboardBusinessRequest`, `SellerOnboardLocationRequest`, `SellerOnboardIdentityRequest`, `SellerOnboardLicenceRequest`) by calling each one's `rules()` — the same regexes, the same NIDA/handle/licence constraints the app's wizard already enforces, with nothing duplicated. `SellerRegistrationController::store()` writes the exact same `SellerProfile` fields in the same order the app's `Api\SellerProfileController` does (business → location → identity/NIDA → licence), so a profile created from the website is indistinguishable from one created through the app — same `pending` status, same manual-review queue, same `NidaVerifier::submit()` call.
  - **No interactive map pin — a deliberate scope call, not a corner cut.** The app's own onboarding step 2 uses a real Google Maps pin picker; the website has no Google Maps key (see BLOCKERS.md) and Part 2 of this same round adopts OpenStreetMap for *display* purposes, but an interactive click-to-drop-pin OSM picker (which needs a real JS mapping library like Leaflet, not just an iframe) is a genuinely separate, larger piece of work than a one-page registration form justifies right now. Instead: region/district/address as plain fields, `SellerOnboardLocationRequest`'s `lat`/`lng` loosened from `required` to `nullable` (a change shared with the app's own API — safe, since the app always sends both anyway and `SellerProfile::hasLocation()` already treats a missing pin as a normal, supported state everywhere else in this codebase), and a new `App\Services\Geo\NominatimGeocoder` attempts a best-effort server-side geocode of the typed address via OpenStreetMap's free Nominatim API (no key, no billing — matching this app's existing OSM choice) with a 4s timeout, catching every failure mode internally and returning null rather than ever blocking submission. If a real interactive map is wanted later, Leaflet+OSM (unlike Google Maps) needs no billing/key to add — noted here for whoever picks this up.
  - **A real bug the tests caught before it shipped**: `$request->validated()` doesn't include a key at all when that field is completely absent from the submitted form data, even for a `nullable` rule — only when the key is present (e.g. as an empty string) does it validate through and appear in the array. Since the web form never sends `lat`/`lng` at all (no map pin to source them from), `$data['lat']` threw "undefined array key" on every single submission. Fixed with `$data['lat'] ??= null` / `$data['lng'] ??= null` right after `validated()` — the app's own API-driven form (which always sends both keys) never hits this gap, which is exactly why it wasn't caught by any existing test.
  - **"Start selling" added to the account nav for non-sellers** (it only ever showed "My shop" for existing sellers — a buyer had no path to registration from their own account menu at all, another part of the reported bug) and the `/sell` marketing page's CTA now checks auth state (`web.account.shop.register` when signed in, `web.login` when not) instead of unconditionally pointing at login.
  - `php artisan test`: 288/288 passing (11 new).
- **2026-08-22 — Tester feedback round, Part 2: shop page address and map.** Added a prominent full-address line in the shop header (previously only shown lower down, inline with the bio/contact column) and a "member since" year stat alongside listings/customers/rating in the header's stat row (previously not shown anywhere on the shop page, despite the translation key for it already existing and going unused). Replaced the Google Maps "get directions" link (no API key, and none is coming per this account's Google Cloud billing situation) with a plain OpenStreetMap `iframe` embed — `openstreetmap.org/export/embed.html`, a small fixed bounding box (~0.008° in each direction, roughly a neighbourhood-scale view, not a wide city view) around the seller's own point, `loading="lazy"` so it costs nothing on 3G until actually scrolled to, and rendered only when `SellerProfile::hasLocation()` is true — no empty map frame when a seller has no coordinates, exactly per instruction. Directions became two links: a `geo:` URI (opens the device's native maps app on platforms that register that scheme, chiefly Android) plus a plain web fallback link to OpenStreetMap's own directions page, since `geo:` isn't handled everywhere (desktop browsers, iOS Safari with no maps app registered) and the instruction explicitly asked for both.
  - `php artisan test`: 291/291 passing (3 new — address/member-since, map-present-with-coordinates, no-map-frame-without-coordinates).
- **2026-08-22 — Tester feedback round, Part 3: navigation restructure.** Replaced the header's ad hoc collection of links (Sell button, Sign in/My account, language toggle) with the 5 named destinations (Home/Stores/Explore/Chats/Profile) as the site's actual primary navigation, on desktop in the header's main row and on mobile as a new fixed bottom nav bar (`partials.bottom-nav`) matching the app's own bottom nav so the two "feel like one product," per instruction.
  - **The standalone "Sign in"/"My account" button was removed, not kept alongside the new Profile item.** Having both would mean two links doing almost the same thing (one opens the account area, the other opens... the account area) — Profile itself now serves as the single account entry point: redirects straight to the dashboard when signed in, shows an explanatory sign-in prompt when not. This mirrors how Amazon's own "Account & Lists" is a single nav item, not a nav item plus a separate sign-in button.
  - **Chats and Profile sit outside the `auth:web` middleware group entirely, on purpose** — every other protected route on this site redirects a guest straight to `/login` (via `bootstrap/app.php`'s `redirectGuestsTo`), which is exactly the behavior the instruction asked these two to *not* have. `NavGateController`'s two actions check auth state directly: signed in → redirect to the real `web.account.messages`/`web.account.dashboard`; signed out → render `web.auth.signin-prompt`, explaining what signing in unlocks, with its own "Sign in" CTA.
  - **Return-to-where-you-were-after-signing-in, built as a closed allowlist, not a raw redirect parameter** — a naive `?redirect=<url>` accepted verbatim and handed to `redirect()->intended()` would be a textbook open-redirect vulnerability (`?redirect=https://evil.example` sent through unchanged). `OtpAuthController::show()` instead accepts only two literal, named values (`chats`, `profile`) mapped to their real routes via a fixed internal array; anything else is silently ignored. Proven with a test that submits `?redirect=https://evil.example` and asserts the session's intended URL is never set to it.
  - **Stores** (`StoresController`/`web/stores.blade.php`) is a genuinely new page — nothing on the website previously let a visitor browse sellers directly. Search by shop name/handle, filter by region/category, sort by rating/newest/most-listings (`withCount(['products' => visible])`, avoiding an N+1 per-card count query), paginated, reusing the existing `<x-shop-card>` component — extended with location and listing-count lines (guarded with `@if isset(...)`/null-checks so the same component keeps working unchanged everywhere else it's already used, like the home page's featured-shops row, which doesn't eager-load a products count).
  - **Explore** (`ExploreController`/`web/explore.blade.php`) deliberately reuses `SearchFilterInput`/`ProductSearchService` unchanged rather than a second filtering implementation — that shared code already defaults `sort` to `'newest'` with no category constraint when neither is given, which is precisely what "newest products across all categories, same filters as category pages" describes. The only real difference from an empty-query Search page is that this is a dedicated, nameable browsing destination.
  - **Unread badge on Chats** (`App\Support\UnreadMessages::countFor()`) is a plain per-request count (messages in a conversation the user is party to, sent by the other side, not yet read) — matching this app's existing polling-based chat rather than anything push-driven, and only queried for a signed-in visitor (the vastly more common case, a signed-out page view, skips it entirely via the view composer).
  - **The new fixed bottom nav bar (56px) required repositioning the product page's own pre-existing mobile sticky Order/WhatsApp bar** (built in an earlier round), which previously sat at `bottom-0` — left unchanged, it would now render underneath/behind the new global nav bar. Moved to `bottom-56` (directly above the nav bar) and reworked the page's own bottom padding so it accounts for the nav bar's clearance (now provided globally, once, by `<main>` in `layouts.app`) plus only the *extra* space its own sticky bar needs on top of that, rather than a since-doubled combined padding figure.
  - `php artisan test`: 301/301 passing (10 new).
- **2026-08-23 — Realistic demo content, Part 1: `DemoSeeder` and `php artisan demo:seed`.** Built as a genuinely separate seeder (not folded into `DatabaseSeeder`, which still seeds the Faker-generated baseline this exists to replace/supplement for demo purposes), fronted by a dedicated `php artisan demo:seed {--fresh}` Artisan command rather than trying to bolt a custom flag onto the generic `db:seed` — that command has no `--fresh` of its own (`migrate:fresh` is a different, far more destructive operation), so Symfony Console would reject an undefined option outright.
  - **Bilingual bio/description needed a real schema addition — `seller_profiles.bio_sw` and `products.description_sw`, both new nullable columns, not a rename of the existing fields.** `bio`/`description` already existed as single (English, in practice) fields; renaming either would ripple through the API resource, Filament forms, and every existing row. Categories had already established the right pattern for this (`name_en`/`name_sw`, not a single `name`), so this follows it: a row with no `_sw` value set — every real seller/product today — keeps showing the English field regardless of locale, exactly as before; only rows that explicitly set the new column (the demo catalogue) get a genuine Swahili variant. Product *titles* were deliberately left untouched — "iPhone 12 Pro" and "Tecno Spark" are brand/model names a Tanzanian buyer says the same way in either language; only descriptions needed the split.
  - **A real, sneaky bug caught immediately by the test suite, not left to surface in production**: the first version named the new locale-aware accessor methods `bio()` and `description()` — the exact same names as the underlying database columns. `description()` crashed with `ArgumentCountError` the instant anything touched `$product->description` as a plain attribute before the model was fully hydrated (e.g. right after `Model::create()`), because Eloquent's `getRelationshipFromMethod()` magic calls *any* method matching an accessed-but-not-yet-loaded property name, expecting a `Relation` object back — it doesn't distinguish "real relationship method" from "a same-named helper that happens to exist." `Category::name()` never had this problem only because `name` was never itself a real column (`name_en`/`name_sw` are) — accidental safety, not a pattern anyone had verified. Renamed to `localizedBio()`/`localizedDescription()`, which can never collide with a column name, and re-ran the full suite to confirm every call site.
  - **Two idempotency bugs found empirically (by actually running the seeder twice, not assumed from reading the code) and fixed.** Orders had no stable identity across re-runs — `Order::code` auto-generates a fresh random value via a `creating()` hook whenever it's left unset, so every non-`--fresh` re-run minted 15 entirely new "completed" order rows with new ids. That silently broke the review seeding *downstream* too: `Review::updateOrCreate(['order_id' => ...], ...)` was already correctly idempotent in isolation, but keyed against an `order_id` that was a different number every run, so it dutifully created a second, "new" review each time rather than ever finding the first. Comments had no idempotency key at all (`Comment::create()`, unconditional). Fixed by giving each demo order a deterministic `code` (`DEMO-001`..`DEMO-028`) and checking for an existing row with that code before creating, and switching comments to `updateOrCreate` keyed on `(product_id, user_id, parent_id)`. Verified by literally running `demo:seed` twice in a row and asserting the row counts didn't move — the kind of thing that's easy to get wrong by only testing the *first* run.
  - **A second real bug, found the same way**: the very first `demo:seed` run on a database with zero categories left 59 of 60 products with `category_id = null` — the seeder assumed `CategorySeeder` had already run and only ever created the one category it explicitly checked for (`Construction & Hardware`), silently leaving every other shop's category lookup empty. Fixed by having `DemoSeeder` call `CategorySeeder` itself first (safe and idempotent either way, since it's `updateOrCreate`-keyed on `name_en`) — "runnable on its own" needed to mean genuinely self-sufficient, not merely self-sufficient once a specific prior seeding step happened to have already run. Regression-tested directly: a dedicated test starts from zero categories and asserts all 11 exist afterward.
  - **"Construction & Hardware" added as a real, persistent 11th category, not demo-only data** — it isn't one of CLAUDE.md's original fixed 10, but the task explicitly asked for hardware-store pricing rules alongside the other nine as equally real content, and real Tanzanian hardware/construction shops are a genuine, common storefront type worth representing honestly rather than mis-filing under "Home & Furniture" or "Other." Created via `Category::create()` (not inside the demo-data cleanup path `--fresh` touches), so it survives a `--fresh` re-seed the same way the original 10 categories already do.
  - **Shop logos and product placeholder images are hand-generated inline SVG, not sourced stock photography** — this environment has no way to source genuine Tanzanian product/shop photography, and CLAUDE.md's own "no placeholder code" spirit rules out pretending a random `picsum.photos` image (what the pre-existing Faker-based seeder used) is "real" when it demonstrably isn't the actual product. `App\Support\InitialsLogo` draws a shop's own initials on an alternating brand-yellow/near-black circle (a few hundred bytes, no GD/font-file dependency); `App\Support\ProductPlaceholderImage` draws the product's category icon plus its name on a neutral light background — both share their icon path data with `<x-category-icon>` via a new `App\Support\CategoryIcons` class (extracted from that component, which previously had its own inline copy) so the category grid and every product placeholder draw the identical glyph. **Flagging exactly as instructed: every one of these 60 product images and 12 shop logos is a placeholder and needs replacing with real photography before an actual client launch** — this is demo content, not a finished catalogue.
  - **Cleanup identity is a fixed, hardcoded handle/phone list, not a schema flag.** Adding an `is_demo` boolean to `users`/`seller_profiles`/`products`/etc. to mark demo rows would have meant a migration touching most of the schema for a distinction only this one command ever needs. Instead, `DemoSeeder::demoHandles()`/`demoBuyerPhones()` expose the exact literal values the seeder itself uses to create rows; `--fresh` deletes only `seller_profiles` rows matching those 12 exact handles (and the users who own them) plus `users` rows matching those 15 exact phone numbers — precise enough that a real account can never collide with it, proven by a test that plants a real seller and buyer first and confirms `--fresh` leaves both alone. Every relevant foreign key in this schema is already `ON DELETE CASCADE`, so deleting just those seller_profiles/users rows is sufficient to remove everything downstream (products, media, offers, showcases, updates, reviews, comments, orders, order items) without needing to enumerate and delete each table separately.
  - `php artisan test`: 309/309 passing (8 new — DemoSeederTest, including one full end-to-end render of the stores directory, a shop page, and a product page against the real seeded content).
- **2026-08-23 — Realistic demo content, Part 2: dead links and placeholder-URL cleanup.** Audited every hardcoded external URL, phone number and email across the website templates (`grep`'d the full `resources/views` tree, not assumed from memory) rather than only touching the three links named in the task.
  - **Social links and app-download links became plain `config('sokoni.*')`-backed values** (`social.facebook`/`social.instagram`/`social.x`, `app_links.google_play`), sourced from new optional `.env` keys (`SOKONI_SOCIAL_*`, `SOKONI_APP_GOOGLE_PLAY`), not routed through the DB-backed `App\Support\Settings` admin-override system that already exists for platform tuning values (search radius, media caps) — the task only asked for "set without a code change," not an admin UI, so a plain env-backed config key is the smaller, correct-sized fix. Each icon/button in `footer.blade.php` is now individually `@if`-gated and disappears rather than linking to a fake `facebook.com/sokoni`-style URL when unset; the whole `#app-download` banner section on the home page collapses entirely (not just its button) when no Google Play link is configured — confirmed safe to hide wholesale by grepping for any other `#app-download` cross-reference first (there is none).
  - **The App Store link was deleted outright, not hidden behind a config check** — per instruction, since no iOS build exists at all yet, a config toggle for a link that could never legitimately be turned on is dead code with extra steps. `config/sokoni.php` documents in a comment why there's deliberately no `app_links.app_store` key, so a future session adding a real iOS listing knows to add the key at that point rather than wondering why it's missing.
  - **`support@sokoni.co.tz` (contact page, both privacy and terms pages in both languages) was made configurable but deliberately NOT hidden-when-unset**, unlike the social/app links — this is the client's own real production domain (`sokoni.co.tz`), not a guessed handle or a nonexistent app listing, and a support contact is essential UI a legal/contact page shouldn't ever go without. `config('sokoni.support_email')` defaults to the real address so nothing changes for anyone who doesn't set the new env var, while still being overridable without a deploy if the address ever changes.
  - **`X-Robots-Tag: noindex, nofollow` via hostname-gated middleware (`NoindexBetaHost`), not a `robots.txt Disallow`** — a `Disallow` stops the crawl entirely, which stops Google from ever reading a `noindex` directive on those pages, the exact opposite of "keep it out of search results." `config('sokoni.beta_hostname')` (default `beta.sokoni.co.tz`) is checked against the live request's `Host` header on every response in the `web` middleware group; when the site eventually moves to serving from the production domain, the beta hostname simply never matches any real request again — no manual toggle-off step is needed at go-live, matching the instruction's explicit requirement. The pre-existing `/robots.txt` (`SeoController::robots()`) was left untouched — it already only disallows account/auth/login/search paths sitewide, not a blanket disallow, so it needed no change and isn't in tension with the new header.
  - **WhatsApp links on product/shop pages were audited and left alone** — they already read `$seller->whatsapp`, a real per-seller database field, not a hardcoded placeholder number, so there was nothing to fix there.
  - `php artisan test`: 318/318 passing (9 new — social/app-link visibility and support-email config in `SiteConfigLinksTest`, beta-hostname noindex header behavior in `NoindexBetaHostTest`).
- **2026-08-23 — Production hotfix: DemoSeeder generated a handle longer than its own column.** `mbezibeach_electronics` (22 chars) and `mikocheni_homecomforts` (22 chars) both exceeded `seller_profiles.handle`'s `VARCHAR(20)` column — and `SellerProfile::HANDLE_PATTERN` (`/^[a-z0-9_]{3,20}$/`), the exact same rule `SellerOnboardBusinessRequest` enforces for a real seller signing up — meaning the seeder was generating data that violated the app's own validation rule, not an edge case the schema failed to anticipate. Renamed both to `mbezi_electronics` (17) and `mikocheni_comforts` (18), updated every product row referencing them.
  - **Root cause of why 318/318 passing tests didn't catch this: SQLite, this project's local/CI test driver, does not enforce declared `VARCHAR(n)` lengths** — it happily stores a 22-character string in a column declared `VARCHAR(20)`, so `DemoSeederTest` seeding against SQLite in-memory passed cleanly while the identical seeder failed with a hard SQL error the moment it ran against the real MySQL schema in production. This project has no MySQL server available in its dev/CI environment (confirmed: no `mysql` CLI, `phpunit.xml` hardcodes `DB_CONNECTION=sqlite`), so rather than stand up MySQL-specific test infrastructure, `test_every_seeded_value_fits_the_real_column_limits_and_validation_rules` (`DemoSeederTest`) checks every seeded value's actual PHP string length/format against the real column definitions and FormRequest rules directly — `handle` ≤20, `shop_name`/`address` ≤255, `whatsapp` against the app's own `/^\+255[67]\d{8}$/` regex, `nida_number` against its exact-20-digits rule, `title` ≤255, `description` ≤5000 — an assertion that holds regardless of which database driver actually runs the test, closing the exact gap that let this ship.
  - **A second, permanent guard was added directly in the seeder, not only in a test**: `DemoSeeder::run()` now calls `assertHandlesAreValid()` first, which throws `InvalidArgumentException` if any `self::SHOPS` handle fails `SellerProfile::HANDLE_PATTERN`, is a reserved handle, or duplicates another — so a future edit to the shop list fails loudly and immediately in *every* environment (dev, CI, or a production run through `setup.php`), not silently on whichever happens to enforce column limits. Belt-and-braces deliberately: the test catches it before deploy, the runtime assertion catches it even if a future change ships without running the test suite first (e.g. a hand-edit made directly against `setup.php`-style production access).
  - **Audited every other seeded field against its real column limit and validation rule** (`shop_name` max:255/VARCHAR(255), `bio`/`bio_sw` max:1000-equivalent/TEXT, `nida_number` exactly 20 digits/VARCHAR(20), `whatsapp` regex/VARCHAR(255), `address` max:255/VARCHAR(255), product `title` max:255/VARCHAR(255), `description`/`description_sw` max:5000/TEXT) — all already comply; `handle` was the only field where the seeder's hardcoded data actually broke the app's own rule. Also noted, not changed: `orders.code`'s seeded value (`DEMO-000`..`DEMO-027`, 8 chars) fits `orders.code`'s `VARCHAR(12)` column and is schema-legal, even though it doesn't match the real `Order::generateUniqueCode()` format (`SK`+8 random alphanumeric) — a deliberate, already-documented choice (see the 2026-08-23 Part 1 entry) for deterministic re-run idempotency, not a bug.
  - Not re-zipped per instruction — the corrected `database/seeders/DemoSeeder.php` was handed over directly for manual paste onto the server, since the Windows `Compress-Archive` packaging had already silently dropped a file from this same deploy twice.
  - `php artisan test`: 320/320 passing (2 new).
- **2026-08-24 — App bug fix, Part 1: session didn't survive an app restart.** Diagnosed before changing anything, per instruction. Confirmed both named suspects were real, found a third the user hadn't named, and ruled out a fourth (a token-refresh flow) as inapplicable.
  - **Suspect 1, confirmed**: `AuthStateController._loadInitial()` wrapped the secure-storage token read in a 5-second timeout and collapsed *any* failure (timeout or thrown exception) into `isAuthenticated: false` — indistinguishable from "never signed in." This could fire on any cold start where the keystore decrypt was momentarily slow, independent of how the app was closed.
  - **Suspect 2, confirmed but more nuanced than "9.2.4 doesn't persist correctly"**: read `flutter_secure_storage`'s actual Android Java source for both the pinned 9.2.4 and current 11.0.0. Every write path in **both** versions calls `SharedPreferences.Editor.apply()`, never `.commit()` — `await writeToken()` resolving in Dart does not mean the bytes reached disk. Android's OS flushes pending `apply()` writes during a graceful `onStop()` (covering most "close the app" gestures), but not an abrupt process kill. This means the earlier 9.2.4-for-Windows-win32-conflict downgrade (see the relevant earlier DECISIONS entry) is unrelated to this bug, and upgrading back to 11.x would not fix it — confirmed by reading 11.0.0's `FlutterSecureStorage.java` directly rather than assuming a newer major fixed it. Also confirmed: `AndroidOptions()` was using the legacy custom RSA/AES cipher (`encryptedSharedPreferences` defaulted to `false`), not AndroidX Security's `EncryptedSharedPreferences` — switched it on (`SokoniSecureStorage`) as a real hardening step (the plugin migrates existing legacy-cipher values across transparently, confirmed via its own `checkAndMigrateToEncrypted`), while being explicit in a code comment that this does **not** fix the `apply()`-vs-`commit()` gap, which is upstream and out of reach without patching or replacing the plugin's native layer.
  - **A third bug, not named by the user, found by tracing the actual request path**: `AuthInterceptor.onRequest` reads the token fresh from storage on every request, independently of `AuthStateController`. Previously, if that read ever came back `null` for any transient reason, the request went out with no `Authorization` header, the server 401'd it, and `AuthInterceptor.onError` **actively deleted the stored token** — with no way to distinguish "the server rejected a token we sent" from "we simply failed to attach one locally." This turns a transient glitch into permanent, self-inflicted data loss, worse than merely misreading a valid session. Checked every screen gating on auth (Profile/Sell/Orders) and confirmed they're all correctly gated on `isAuthenticated` before firing an authenticated call, so this wasn't firing from an unconditional startup request — but it remained a live risk on any later in-session request. Fixed by tagging each request with whether a token was actually attached (`options.extra['sokoni.had_token']`) and only clearing the session on a 401 when one was.
  - **Fourth item, checked and correctly ruled out**: the instruction asked to verify silent token refresh on expiry. Checked `config/sanctum.php`: `'expiration' => null` — Sanctum personal access tokens never expire server-side in this app, so there is no "expired token" case to refresh. Did not build a refresh mechanism for a scenario that cannot occur; the existing "only treat a 401 as a real rejection" behavior (now fixed to require a token having been sent) is the architecturally correct handling here.
  - **The fix**: `AuthState` became a 4-value `AuthStatus` enum (`loading`, `authenticated`, `unauthenticated`, `unknown`) instead of two independent bools. `_loadInitial()` no longer has a short timeout on the normal path — the read runs to completion, since nothing downstream is blocked waiting on it (the splash screen navigates on its own fixed timer regardless of auth state, confirmed by reading `splash_screen.dart`). A generous **10-second** safety-net timeout remains only as a last-resort circuit breaker against a genuinely hung native call (matched to the same order of magnitude as this app's other two bounded startup timeouts — `PushService`'s per-step timeout and `LocationService`'s position fetch are both 8s — rather than picking an arbitrary outlier); its failure, and any thrown exception, maps to `AuthStatus.unknown`, never `unauthenticated`. Added `AuthStateController.retry()` and wired a distinct "couldn't check your session, try again" state (reusing `SokoniErrorState`, new `authSessionUnknownTitle`/`authSessionUnknownBody` ARB strings in both languages) into the three screens that gate on auth (Profile/Sell/Orders), so `unknown` is visibly different from "please sign in" rather than silently collapsing into it.
  - **What was verified and what could not be, honestly**: wrote a Dart-level test proving `AuthStateController` restores `authenticated` from a stored token on a fresh `ProviderContainer` build (the actual "auth state restores from storage on cold start" requirement), plus tests for the unauthenticated/thrown-exception/never-completes/retry-recovers paths (the last two using `fake_async` to advance virtual time past the 10s safety net without a real 10-second wait), plus a round-trip test proving `SokoniSecureStorage`'s own read/write/clearSession contract is correct in-memory. What could **not** be verified in this environment: actual Android disk durability across a real OS process kill — `flutter devices` lists only Windows/Chrome/Edge, `adb` is not on PATH, no emulator is available. This specific check (sign in, force-stop or swipe the app away, relaunch, confirm still signed in) needs a real device and is flagged for manual/QA verification rather than claimed as automated.
  - `flutter analyze`: clean. `flutter test`: 22/22 passing (11 new).
- **2026-08-24 — App feature, Part 2: Instagram-style double-tap to Save.** New generic motion primitive `DoubleTapToSave` (`core/motion/double_tap_to_save.dart`) — a `GestureDetector` with both `onTap` and `onDoubleTap` set, which is Flutter's own built-in, platform-timeout-matched (`kDoubleTapTimeout`, ~300ms) tap-vs-double-tap disambiguation, not a hand-rolled one. Noted explicitly in the widget's own docs and to the user: there is no way to distinguish an isolated single tap from the first half of a double tap without waiting out that window — every correct double-tap-to-like implementation on any platform, Instagram's own included, has this same unavoidable wait; the instruction to avoid "introducing a delay" is satisfied by not adding anything *beyond* that minimum, not by eliminating it (which isn't possible while still telling the two gestures apart).
  - **Idempotent "save," never a toggle, is enforced one layer down from the gesture** — `DoubleTapToSave` itself doesn't know or care whether the item is already saved; it always plays the heart animation and always calls the supplied `onSave`. The "already saved → no-op, don't unsave" rule lives in a new `saveProductViaDoubleTap` (`favorites_providers.dart`), kept deliberately separate from the pre-existing `toggleProductFavorite` (still used by every plain heart-icon tap, which *should* toggle). This is also where the optimistic update + quiet revert-with-toast-on-failure lives, per instruction — reverting silently with no user-facing signal at all (the original `toggleProductFavorite`'s behavior) was judged not good enough here given the instruction explicitly asked for a toast, so `saveProductViaDoubleTap` takes an `onSaveFailed` callback the caller uses to show one, since the provider layer has no `BuildContext` of its own.
  - **`favorites_providers.dart`'s public functions were refactored from taking a whole `Product` to taking `productId`/`knownFavorited` as plain parameters** — discovered necessary once wiring the Showcase video feed, whose items only embed a lightweight `ProductSummary` (id/title/price/cover image, no `isFavorited` field at all), not a full `Product`. `knownFavorited: false` for a `ProductSummary` just means "not yet known saved this session" — the existing per-session optimistic-override map (keyed on plain `productId`, never tied to `Product` itself) already made this a natural, non-breaking generalization rather than a real architectural change; all three existing call sites (feed card, product detail) were updated to pass `product.id`/`product.isFavorited` explicitly.
  - **Showcase gained a Save icon where none existed before** — the video feed had no persistent favourite affordance at all prior to this (only a "Buy" button), so there was nothing for a double-tap to visibly sync with. Added a compact right-edge icon (Reels-style single icon, not a full rail — comments/share weren't asked for here) reusing the same `SpringOnTrue`-wrapped heart used everywhere else, so all three surfaces read as one consistent Save affordance rather than three different treatments.
  - **`SpringOnTrue`** (`core/motion/spring_on_true.dart`) is a second, smaller new primitive — a generic "bounce this child once when a bool flips false→true" widget, following `BadgeBounce`'s exact established shape (`didUpdateWidget`-gated, `disableAnimationsOf` guard, single `AnimationController`) rather than a bespoke one-off, so the action-row heart springs identically in the feed, product detail, and Showcase.
  - **A real animation-lifecycle bug caught by the widget's own test, not shipped and found later**: the heart's `AnimationController` only ever checked `status == AnimationStatus.dismissed` to decide whether to render — after `forward()` reaches `AnimationStatus.completed`, the icon stayed built (at opacity 0, invisible but still in the tree) forever, since `completed` was never treated as "hide." A test asserting the heart is fully gone (not just invisible) after the sequence finishes caught this; fixed with a status listener that calls `_controller.reset()` on completion, returning it to `dismissed` so the widget genuinely leaves the tree and is ready to replay cleanly.
  - **Product detail gallery**: `ProductMediaCarousel` gained a required `onDoubleTapSave` callback threaded into every page (image via `DoubleTapToSave` wrapping `ParallaxImage`; video via replacing `_VideoPage`'s bare `GestureDetector(onTap: _toggleMute)` with `DoubleTapToSave(onSingleTap: _toggleMute, onSave: ...)`) — preserving the existing single-tap-to-mute behavior on video pages exactly, since "opens the product" doesn't apply on a screen you're already viewing.
  - **Grid cards were deliberately left untouched** — per instruction, and consistent with `product_card.dart` never being imported by any file this change touched.
  - **What could not be verified live**: no Android device or emulator is available in this environment (same limitation as Part 1's session-persistence fix), and a browser/desktop run wouldn't meaningfully exercise a touch double-tap gesture or haptics anyway — verified via `flutter analyze` (clean) and a full widget/unit test suite (gesture disambiguation, animation lifecycle including the bug above, and the provider-layer optimistic/idempotent/revert logic) instead of a live device run.
  - Built and signed `app-prod-release.apk` (`flutter build apk --release --flavor prod`) against the existing release keystore — 71.5MB. No backend/API code changed in either Part 1 or Part 2 of this round, so no `sokoni-api` update zip was produced.
  - `flutter analyze`: clean. `flutter test`: 30/30 passing (8 new).
- **2026-08-25 — App bug fixes + sign-up restructure, Part 1: guests were shown failures instead of invitations.** Diagnosed before changing anything: `mapDioError` itself already correctly mapped a 401 to `UnauthenticatedException` (confirmed by reading `dio_client.dart`'s `_mapResponse` — no bug there). The actual defect was one layer up — `saveProductViaDoubleTap`/`toggleProductFavorite` swallowed every exception into the same hardcoded `productSaveFailedToast` string ("Couldn't save — check your connection...") regardless of what actually failed, so a guest's 401 and a genuine offline failure looked identical. Audited every other guest-attemptable action the instruction named:
  - **Follow** (`toggleSellerFollow`) had the same swallow-everything `catch (_)` — worse, it gave no feedback at all, not even the wrong toast.
  - **Message** (`_ChatButton` in `feed_card.dart`, and the "Message Seller" button in `product_detail_screen.dart`/`shop_profile_screen.dart`) called `startConversation` with no error handling whatsoever — a guest tapping it hit a raw uncaught exception.
  - **Order** (`checkout_screen.dart`) had no auth gate at all — a guest could fill in the entire delivery form only to be rejected with "Please sign in again." at submission. **Comment** (`comment_sheet.dart`) was already correct — checks `isAuthenticated` before attempting, prompts sign-in first — used as the template for everything else.
  - **Fix**: `toggleProductFavorite`/`saveProductViaDoubleTap`/`toggleSellerFollow`/a new `startConversationOrPromptSignIn` all now take separate `onUnauthenticated`/`onFailed(String message)` callbacks instead of one catch-all — `onFailed` receives the real `ApiException.message` (never a hardcoded string), and every call site wires `onUnauthenticated` to a new `showSignInPrompt` bottom sheet ("Sign in to save items" / "...to follow shops" / "...to message this seller" / "...to place an order" — an invitation, not an error: no red text, a Sign in button, a "Not now" dismiss). `cart_screen.dart`'s Checkout button now gates on `isAuthenticated` before ever opening the checkout form; `checkout_screen.dart` also catches `UnauthenticatedException` specifically as a defensive fallback (a revoked token mid-session, however unlikely given Sanctum tokens here don't expire) rather than surfacing it as `_error` text.
  - `php artisan test`: unaffected (backend untouched this part). `flutter analyze`: clean. `flutter test`: 39/39 passing (9 new).
- **2026-08-25, Part 2: confirmed the "returning user treated as new" report was cosmetic, not a duplicate-account bug — then fixed the flow anyway.** Per instruction, verified server-side before touching UI: `users.phone` has a real DB-level `unique()` constraint (`0001_01_01_000000_create_users_table.php`); the client always normalises to E.164 via `SokoniFormat.phoneToE164` before ever calling the API; the server's own `RequestOtpRequest`/`VerifyOtpRequest` validation rules (`regex:/^\+255[67]\d{8}$/`) reject anything that isn't already strict E.164, so a malformed number like `0741219233` can never reach `AuthController::verifyOtp`'s `User::firstOrCreate(['phone' => ...], ...)` lookup in the first place — client and server normalise to the identical string, so the lookup always finds the existing row. No duplicate accounts are possible from this path. The bug was purely client-side: `phone_sign_in_sheet.dart`'s `_otpStep()` rendered the name `TextFormField` unconditionally, never gated on `_isNewAccount` — a returning user saw the exact same "full name" field as a brand-new one, even though the server already knew better (and silently ignored a stray name for a returning user via `firstOrCreate`'s create-only attributes — the wrong data was never actually written, just wrongly displayed as needed).
  - **Fixed by rebuilding the "Sign in" path as its own flow, folding Part 2 into Part 3's larger restructure rather than patching the doomed-to-be-replaced combined sheet.** New `sign_in_sheet.dart`: phone → (server tells us via `requestOtp`'s existing `is_new_account` field, already returned, no new endpoint needed for this path) → existing account: "Welcome back" + masked number (`SokoniFormat.phoneMasked`, new — "0754 *** 456") + code field only, never a name field; new number: "We don't recognise this number" + "Create an account instead" (routes into Part 3's new flow) rather than a code field that could never succeed (the server requires a name for a genuinely new account, and this flow never collects one). The old combined `phone_sign_in_sheet.dart` was deleted outright, not kept alongside — CLAUDE.md's own "no half-finished implementations" standard: keeping a second, subtly-different sign-in surface around would be exactly the kind of drift that caused this bug's cosmetic half in the first place.
  - `php artisan test`: unaffected (no server changes needed for Part 2 itself — folded into Part 3's endpoints below). `flutter test`: +4 new (`sign_in_sheet_test.dart`), confirming the actual wire payload for a returning user's verify call sends `name: null` — not just that the field is invisible.
- **2026-08-25, Part 3: sign-up flow restructure — intent first, phone last.** New split entry point (`auth_entry_sheet.dart`, replacing `showPhoneSignInSheet` — every call site renamed to `showAuthEntrySheet`): social buttons (unchanged) + "Sign in" (existing accounts) + "Create an account" (a new full-screen 4-step wizard, not a bottom sheet — materially bigger than anything else this app puts in a sheet).
  - **New backend, all additive, no migrations**: `POST /auth/check-phone` (check-only, no OTP side effect — reuses `RequestOtpRequest`'s own E.164 rule rather than a new FormRequest) and `GET /sellers/handle-availability` (reuses `SellerProfile::HANDLE_PATTERN`/`RESERVED_HANDLES` directly) back Step 2's live validation. `POST /auth/register` (`RegisterAccountRequest`, deliberately reusing the exact literal rules `SellerOnboardBusinessRequest`/`SellerOnboardLocationRequest` already enforce — same handle regex/reserved list/uniqueness, same shop_name/whatsapp/region/district/address rules — per instruction not to write new ones) verifies the OTP and creates the User and, for a seller, the SellerProfile, in one DB transaction, re-checking phone/handle uniqueness immediately before the transaction to close most of the TOCTOU race window (the DB's own unique constraints are the real last line of defence). Distinct from the existing `verifyOtp` (which the "Sign in" flow still uses unchanged, and which never creates a seller profile) — keeping them separate meant zero risk of the two flows' behaviours bleeding into each other.
  - **NIDA and the business licence were never added to Step 2, on purpose — they're not new UI at all.** A newly-registered seller's `SellerProfile` row already has every field `SellerOnboardIdentityRequest`/`SellerOnboardLicenceRequest` (the existing onboarding wizard's steps 3-4) expect from a profile that already exists — so after a seller registers, `SellerOnboardingController.resumeAtIdentity(sellerId)` (new, three lines) seeds that wizard's own local draft straight to step index 2 and hands off to it unchanged. `SellerOnboardingScreen` needed zero modifications: it already resumes at whatever step its draft says, and its progress bar correctly shows Business/Location as done (`i <= step`) since that data really is already saved. This is the "separate 'verify your shop' step after the account exists" the instruction asked for — reusing infrastructure instead of duplicating two screens' worth of upload UI.
  - **Local draft persistence uses the same drift-backed key-value table `SellerOnboardingController` already established** (`CachedKeyValues`, via `AppDatabase.setKeyValue`/`getKeyValue`) — not a new package (`shared_preferences`/`hive` were confirmed absent from `pubspec.yaml`, and adding one for a single flow when this exact mechanism already exists and already solves "survive a killed app" would have been needless duplication). The one real difference from the seller wizard's own draft: no server row exists until Step 4 succeeds here, so the entire form's field values, not just a `{sellerId, step}` pointer, have to be the local draft — a buyer's or seller's typed name/phone/shop details survive a dropped connection or an interrupting call, exactly the "this matters on 3G" requirement.
  - **The ordering trade-off, logged as instructed**: verifying the phone number last means a seller can fill in every field — sometimes eight of them — before Sokoni ever confirms the number is reachable, and Step 4 is the single point where abandonment costs the most of anything in this flow. This is real and was not engineered away, only mitigated: Step 2 validates the phone's format and checks it isn't already registered (`/auth/check-phone`) and, for a seller, checks the handle's availability (`/sellers/handle-availability`) live, as the fields are typed — so by the time someone reaches Step 4, it is a confirmation step (send the code, type it back), never the moment a real problem is discovered for the first time. The residual risk this doesn't cover — a phone number that passes format+registration checks at Step 2 but simply can't receive SMS, or a user who mistypes it between steps 2 and 4 — was judged acceptable rather than worth a second verification hop back at Step 2, since CLAUDE.md's own phone-OTP requirement already treats "a code arrives and is entered" as the one and only proof of reachability this app relies on anywhere.
  - **Terms acceptance is recorded locally through Step 3, not sent to the server until Step 4** — no account exists yet to attach `terms_accepted_at`/`terms_version` to via the existing `POST /auth/terms/accept` (which requires `auth:sanctum`). `RegisterAccountRequest` takes `terms_version` directly and `AuthController::register()` sets `terms_accepted_at`/`terms_version` at the same moment it creates the row, reusing the exact same `sokoniTermsVersion` constant (`legal_content.dart`) and Terms/Privacy screen content `TermsAcceptanceScreen` already uses — the new Step 3 screen is close to a straight reuse of that one's body, not new copy.
  - `php artisan test`: 335/335 passing (15 new). `flutter analyze`: clean. `flutter test`: 48/48 passing (9 new this part, 18 total across all three parts of this round).
  - **A real GoRouter+testing gotcha hit and fixed while writing `sign_in_sheet_test.dart`, worth remembering**: `WidgetTester.pumpAndSettle()` hangs indefinitely (eventually times out) on a widget tree wrapped in `MaterialApp.router`/`GoRouter` in this project's test environment — confirmed by literally swapping it for a bounded `pump()`/`pump(duration)` sequence, which resolved it immediately. `feed_card_test.dart` already avoided this (uses plain `pump()`), which is presumably why the underlying issue was never previously named — any future GoRouter-wrapped widget test in this app should default to bounded `pump()` calls, not `pumpAndSettle()`.
- **2026-08-26 — Production incident: `/api/auth/check-phone` 404'd, and Step 2's live checks trapped users behind a dead Next button.** Two separate problems, diagnosed and fixed together.
  - **The 404 itself was a deploy problem, not a code bug** — confirmed the route was defined correctly in `routes/api.php` locally (`POST /auth/check-phone`, `POST /auth/register`, `GET /sellers/handle-availability`) and that the previous round's zip (`sokoni-api-update-signup-restructure.zip`) contained all four changed files with correct contents. `routes/api.php`, `AuthController.php`, and `SellerProfileController.php` were all **pre-existing** files on the server, not new ones — every one of them had to overwrite what was already there for the new routes to take effect. A "route could not be found" error (rather than a 500 from a missing controller method) specifically implicates `routes/api.php` not having actually been overwritten during extraction — most likely an unzip step that defaulted to skipping already-existing files.
  - **The real, more serious bug the incident exposed**: `CreateAccountStep2Details`'s `_onPhoneChanged`/`_onHandleChanged` called `checkPhoneExists`/`checkHandleAvailable` inside a debounce `Timer`'s async callback with no `try`/`catch` and no timeout. Any thrown exception (a 404, a genuine network failure, anything) became an uncaught error inside that `Timer` callback — `setState` never ran, so `_CheckStatus.checking` stuck forever, and `_submit()`'s gate (`if (_phoneStatus == _CheckStatus.checking) return;`) silently no-op'd on every tap of Next with zero explanation. This is exactly the failure mode flagged as having happened once before in this app — a live validation check with no failure path, indistinguishable from a broken button.
  - **Fixed by making both checks fail open, not by trying to guarantee the network call always succeeds** (it can't — a device on 3G, per CLAUDE.md's own target, will see real failures regularly). Added a new `_CheckStatus.unknown` outcome — visually identical to `idle` (no icon, no alarming red X for something that isn't actually confirmed as a problem) — and a bounded 6-second `.timeout()` on both calls (matched to the same order of magnitude as this app's other two bounded background timeouts, `PushService`/`LocationService`'s 8s, per the established precedent from the session-persistence fix). `_submit()` still briefly holds up Next while a check is *genuinely* in flight (harmless, bounded UX — the actual bug was "checking" persisting **forever**, not existing at all), but now every code path — success, a thrown exception, or the timeout firing — is guaranteed to move `_phoneStatus`/`_handleStatus` out of `checking` within 6 seconds, and `unknown` deliberately does not block `_submit()`. `RegisterAccountRequest` (Step 4) still re-validates both fields for real regardless of what Step 2 managed to confirm, so failing open here costs nothing but a slightly later error in the rare case the live check would have been right.
  - Audited the rest of the app for the same "uncaught async inside a bare debounce `Timer` + `setState`" pattern (`grep`'d every `Timer(const Duration` call site) — only these two had it; `search_screen.dart`'s debounce fires into a Riverpod `AsyncNotifier` (which has its own error-state handling), not a raw `setState` after an unguarded `await`.
  - `flutter analyze`: clean. `flutter test`: 51/51 passing (3 new — a thrown-exception case, a never-resolving/timeout case using `testWidgets`' own fake-timer clock rather than a real 6-second wait, and a regression proving a *genuinely* already-registered number still correctly blocks).
- **2026-08-26 — Real SMS provider: Africa's Talking, not Beem.** The client's actual account is Africa's Talking (dashboard app "skyfar") — `BeemSmsGateway` (added in an earlier round on a generic "good aggregator for Tanzania" assumption, never confirmed against a real client account) was simply wrong. Kept rather than deleted: it's not broken, just unused, and removing tested working code to correct a documentation assumption is a bigger and more irreversible action than the correction actually requires — `docs/SMS.md` now says plainly that Beem isn't the real provider, so no one is misled by its continued presence.
  - **Gateway selection became explicit (`SMS_DRIVER=africastalking|beem`), not credential-sniffed.** The old binding picked Beem automatically whenever both Beem keys happened to be set, which is exactly the kind of implicit behaviour that lets a wrong assumption go unnoticed — a production environment with both providers' keys present (plausible during a migration) would have silently kept using whichever the `if` checked first. Explicit selection means there is never a question of which gateway is actually live.
  - **HTTP 201 is not "delivered."** Read Africa's Talking's actual response shape rather than assuming: a 201 only means the request was accepted for processing — the real per-recipient outcome is a `status`/`statusCode` pair inside `SMSMessageData.Recipients`. `AfricasTalkingSmsGateway` treats anything other than `status: "Success"` as a failure and throws, exactly per instruction ("a 201 does not mean delivered").
  - **Insufficient balance gets its own log level, not folded into "just another failure."** It's flagged via Africa's Talking's own `InsufficientBalance` status string and logged at `critical` (every other non-Success status logs at `error`) — per instruction, this is the single most common way prepaid SMS silently stops working in production, and burying it at the same level as a one-off invalid number would defeat the entire point of asking for it to be loud.
  - **A dedicated `sms` log channel** (`config/logging.php`, daily-rotating `storage/logs/sms.log`) carries every request and response, separate from `laravel.log` — "diagnose from cPanel File Manager alone" meant the failure needs to be findable by opening one small, obviously-named file, not grepping a mixed general-purpose log by hand over SSH that doesn't exist on this host anyway.
  - **Bilingual message text needed a `$locale` parameter threaded through `SmsGateway`/`PhoneOtpService`/`RequestOtpRequest`** (`sendOtp(string $phone, string $code, string $locale = 'en')`) — the interface had no locale concept at all before this. The **website's** OTP flow (`OtpAuthController`, inside the `web` middleware group) already has a real, reliable locale via the existing `SetWebLocale` middleware, so it now passes `app()->getLocale()` straight through — both languages are reachable today from the site with no further work. The **API** (`POST /auth/otp/request`) has no equivalent locale-detection middleware, so it accepts an optional `locale` field in the request body (validated `in:en,sw`) defaulting to English. **Flagging honestly, not silently**: the Flutter client does not yet send this field on that call, so mobile OTPs are English-only until a small client-side change (passing `Localizations.localeOf(context).languageCode`) is made — out of scope for this backend-only round (no app rebuild was requested), documented as a known gap in `docs/SMS.md` rather than left as an undiscovered one.
  - **The existing `otp` RateLimiter's window changed from 10 to 15 minutes** to match the instruction exactly (3 per 15 minutes) — it already existed, already keyed on the phone number rather than the requester (protecting the number from being bombed); now that a real, paid, prepaid-balance gateway is behind it, the same limiter also directly protects the client's SMS spend, which is why the instruction called it out as worth getting exactly right rather than leaving at its previous value.
  - `php artisan test`: 350/350 passing (15 new — 8 gateway-behaviour tests covering success/failure/insufficient-balance/malformed-response/sandbox-host/phone-normalisation, 2 binding tests for the new driver switch, 2 rate-limit tests, 3 locale-forwarding tests).
  - Deploy zip built with Python's `zipfile` (not `Compress-Archive` — see the earlier, real incident where that tool's Windows path separators silently dropped files from a cPanel extract). Exactly 1 new file (`AfricasTalkingSmsGateway.php`) and 10 modified files, listed explicitly to the user rather than just handing over a zip, since cPanel's Extract skips files that already exist and modified files need uploading individually.
- **2026-08-27 — Second SMS provider swap: Textify Africa replaces Africa's Talking as the active driver.** Built `TextifySmsGateway` alongside the existing `AfricasTalkingSmsGateway`/`BeemSmsGateway` rather than replacing either — same reasoning as the first swap: a driver that still works and is still tested is worth keeping selectable (`SMS_DRIVER=textify|africastalking|beem`) even after the client moves on again, since providers evidently keep changing and a quick rollback path (flip one env var, no redeploy) is cheap insurance against exactly that. `SMS_DRIVER=textify` is now the recommended `.env.example` default; nothing else about the selection mechanism changed.
  - **Local-format phone numbers, not E.164, was the one genuinely new wrinkle.** Every prior gateway in this app either already wanted E.164 or was normalised to it; Textify's documented example is `0712345678`. Converted defensively at the boundary (`+255` stripped, `0` prefixed) exactly like the app already does the reverse for Africa's Talking, and — per instruction — logs both the original and converted number on every send regardless of outcome, not just on failure, since a silent off-by-one in this conversion would otherwise only surface as an unexplained delivery failure days later.
  - **`success: false` as the real outcome signal, independent of HTTP status, mirrors the exact lesson already learned from Africa's Talking's HTTP-201-doesn't-mean-delivered behaviour** — different shape, same underlying discipline: never trust the transport-layer status code as a proxy for whether the message actually went out. `invalid_token` gets the same `critical`-vs-`error` treatment `InsufficientBalance` already gets, for the same reason (it is the single failure mode that silently stops *every* subsequent send, not just one message), and a response missing the `success` field entirely (malformed/proxy-error/truncated) is treated as its own failure rather than being coerced into `false` or ignored.
  - `docs/SMS.md` restructured so `Logging`/`Message text`/`Rate limiting` — all driver-agnostic — sit as top-level sections rather than nested under whichever provider happened to be documented first; this was accumulating drift with every provider swap and would only have gotten more confusing with a third driver added directly under Africa's Talking's heading.
  - `php artisan test`: 358/358 passing (8 new — 7 Textify gateway-behaviour tests covering success/local-number-conversion/Swahili-message/success-false-regardless-of-status/invalid_token-critical-log/malformed-no-success-field/non-JSON-body, 1 new binding test for the `textify` driver).
  - Deploy zip (`sokoni-api-update-textify-sms.zip`, built with Python's `zipfile`) intentionally ships only the 3 files with runtime effect on the server — the new gateway class plus the 2 modified files that reference it (`AppServiceProvider`, `config/services.php`); `.env.example`, `docs/SMS.md` and the two test files are dev-only/documentation and were left out of the zip on the same precedent as every earlier deploy zip this session, though still committed to the repo.
- **2026-08-30 — Beta tester feedback round, two real bugs fixed first, then eight design fixes.**
  - **Bug 1 (product upload "only one image"): the server already accepted several files — `attachUploadedImages()` looped over `images[]` and the DB model supports 8 — the actual defect was a bare, unstyled `<input type="file" multiple>` with zero visual affordance for multi-select, no preview, no way to remove or reorder a choice before or after submit.** Rebuilt as a real AJAX photo manager (`Alpine.data('productMediaManager', ...)` in `app.js`) rather than patching the native input: drag-and-drop, per-file client-side compression (canvas-resized to the same 1600px long edge `ImageVariants` uses server-side, re-encoded at 0.8 quality — this is a 3G market, per CLAUDE.md's own stated constraint), real per-file XHR progress, thumbnail previews with a remove button, and native HTML5 drag-to-reorder that persists via a new `POST .../media/reorder`. This required a product to already exist before photos can attach to it (a media endpoint needs a product id), so `store()` now redirects to the edit page with "Product created — now add your photos" instead of the products list — deliberately mirroring the Flutter app's own product form, which already works exactly this way (`productFormSavedAddMedia` l10n string), not a new pattern invented for the web. New web-guard controller `ShopProductMediaController` mirrors `Api\ProductMediaController` (same `ImageVariants` pipeline, same `Settings::maxMediaPerProduct()` cap) rather than reusing it directly, since the API controller is bound to the `sanctum` guard and this app's website uses `web` session auth — image-only, since the website's product form has never offered video upload and this task didn't ask to extend that scope.
  - **Bug 2 (gallery flicker): traced to a real, previously-undiscovered site-wide bug, not the gallery's own logic.** Alpine's `x-cloak` attribute has no built-in CSS — every existing use of it on this site (mobile menu, this same gallery's lightbox, the phone-reveal link) has been silently inert since whichever session first added it, because `[x-cloak] { display: none !important; }` was never actually defined anywhere in `app.css`. Before Alpine hydrates, every `x-cloak`'d element was rendering fully visible using its own normal layout — for the gallery's absolutely-positioned, DOM-order-stacked slides (none of which had `x-cloak` at all, compounding it), that's exactly "the wrong photo flashes, then snaps to the first one" the instant Alpine's first tick runs. Fixed at the actual cause: added the missing global CSS rule (un-layered, so it always wins over Tailwind regardless of layer order), added `x-cloak` to every slide but the first (which stays visible from first paint with no rule needed — both for zero pre-hydration flash and so the photo still shows if script fails entirely on a bad connection), and converted the slide-swap from a hard `x-show` display toggle to an `x-transition` opacity crossfade so a slide change is a pure compositor-level fade with nothing to decode or reflow at that moment. Audited the "thumbnail hover re-triggers the main image" hypothesis directly against the markup and ruled it out — no hover binding exists on the thumbnail buttons at all, only `@click`; documented as checked-and-clear rather than silently assumed.
  - **Item 3 (category strip counts)**: removed the per-chip `(N)` count from the header's category nav only — the category *landing page* itself (a different template) still shows its own count, since that's the "already committed to this category, the number is real context" case the task explicitly wants kept.
  - **Item 4 (white dots on the dark banner)**: root cause was a `mask-image` gradient applied to the whole `<nav>` element — masking a container fades its own background too, not just its content, which is exactly what let the white page background show through as artefacts at both corners. Fixed at the container per instruction: removed the mask, kept the nav a plain solid rectangle, and added two absolutely-positioned overlay divs with a black-to-transparent gradient layered on top of the scrolling content instead — same "content fades at the edges" visual cue, but the container's own background is never punched with a real hole.
  - **Item 6 (hero background)**: no real Tanzanian market/commerce photography exists anywhere in this repo (checked — only seeded placeholder product photos), and fabricating or hotlinking a stock photo from an unverified source isn't something to do unprompted. Used the task's own explicit fallback instead: a ~260-byte inline-brand-palette SVG geometric lattice (`public/images/hero-pattern.svg`), tiled behind a dark gradient, with the hero's text flipped to white and the search card staying a bright white focal element — this is a bigger visual change (the hero was a light, flat background before) but is exactly what "dark gradient overlay so the headline and search bar stay legible" implies once there's *any* backdrop behind the text, photo or pattern. Flagged as a placeholder for real photography if the client ever supplies licensed images — swapping it in later is a one-line `background-image` change.
  - **Item 6, "lazy-loaded below the fold" not applied literally**: the hero is the very first section on the page, i.e. always above the fold — lazy-loading it would delay the page's own LCP element rather than save anything, the opposite of the stated performance goal. Implemented every other stated optimization (tiny file size, no format-fallback complexity needed at all since SVG is universally supported) and left it eagerly loaded, since that's what "restrained and fast" actually requires here.
  - **Item 7 (use my current location)**: the backend already fully supported this without any change — `SellerRegistrationController` already accepts optional `lat`/`lng` and only runs its own server-side geocode fallback `if (! $data['lat'] && ! $data['lng'])`. Added a client-side-only feature (`Alpine.data('sellerLocationPicker', ...)`): `navigator.geolocation` → Nominatim's free reverse-geocode endpoint (same provider the server already uses for the forward direction, since no Google Maps key exists on this account) → best-effort fill of the region/district/address fields, with the region matched against the app's own canonical region list by fuzzy substring match since Nominatim's exact place-name strings don't line up with it reliably. Every field stays a plain, always-bound `x-model` input — permission denied, an unsupported browser, or a failed lookup all just leave a small status message and change nothing about how the form behaves, per the explicit "never block the form" requirement.
  - **Item 9 (trust band)**: the three bare-text badges live on the home page (not literally inside `<footer>`, despite the task grouping items 8-9 under a "FOOTER" heading) — found by searching for the exact strings rather than assuming location. Added one inline SVG icon per column (shield-check, lock-with-dot, map-pin — all in `text-sokoni-yellow`, no icon font) plus a new `_desc` translation key per item in both `lang/en/site.php` and `lang/sw/site.php`, using the task's exact English copy and natural (not machine-literal) Swahili.
  - **Items 5 and 8 (logo size)**: both the header and footer logo mark went from 32px to 40px, with the adjacent wordmark bumped `text-lg` → `text-xl` to stay visually balanced at the new size — done identically in both places since the task explicitly called them "same treatment."
  - **Item 10 (About page closing line)**: this page has no `__()` translation keys at all — it's a single Blade file with a hardcoded `@if (locale === 'sw')` branch for each language, an existing pattern this change followed rather than introducing a partial, inconsistent translation-key migration for one paragraph. Replaced the removed hardware-constraint sentence with a market-focused line (sellers/buyers across Tanzania) in both branches, as instructed.
  - **`resources/views/web/account/seller-register.blade.php` has no git history at all** (shows as untracked even though PROGRESS.md's 2026-08-22 entry describes building and deploying it) — and the one prior zip that appears to include it, `sokoni-web-tester-feedback.zip`, was found to contain literal backslash path separators (`sokoni-api\resources\views\...`) when inspected with `unzip -l`, the exact known-bad pattern from the `Compress-Archive` incident this project has repeatedly guarded against since. Genuinely uncertain whether that file ever actually landed on the beta server as a result — flagged explicitly to the user rather than guessed at either way, with the practical instruction to upload it individually regardless of what cPanel's Extract reports for it.
  - `php artisan test`: 367/367 passing (11 new — 8 for the new web media-manager endpoints covering ownership/the shared media cap/reorder-set-validation, plus an updated create-flow test asserting the new edit-page redirect). Vite rebuilt (`app-D2Svolov.css`, `app-CAScviDA.js`). Deploy zip built with Python's `zipfile`, two top-level folders: `sokoni-api/` (source, extract into the existing app root) and `beta-docroot/` (a second physical copy of `build/` and the new hero pattern SVG, for beta's separate document root at `/home/sokoftsn/beta.sokoni.co.tz` — the two docroots don't share a filesystem location, per `docs/DEPLOY_WEB.md`'s existing "Updating the website later" section). No new migrations.
- **2026-08-30 — `setup.php` gained `cleanup-original-seed`: a one-time production step to remove the Phase 2 Faker `DatabaseSeeder` content (20 shops) still live alongside `DemoSeeder`'s 12, which was reported to be outranking them on the home feed.** The identification mechanism is the load-bearing decision here, since this deletes real production rows and every mistake is irreversible: rather than identify "fake" sellers by exclusion (anything not in `DemoSeeder::demoHandles()`, or by an id range, or by a creation-timestamp window) — any of which risks catching a real seller who happens to fall outside whatever list or range was used — the step positively identifies a row only when it matches a structural marker that's impossible for a real account to have. Grepped the whole app first to confirm this holds: `SellerProfileFactory::definition()` is the *only* place in the entire codebase that ever writes `nida_image`/`licence_file` under a `seed/` prefix (`'seed/nida/'.fake()->uuid().'.jpg'` / `'seed/licences/'.fake()->uuid().'.pdf'`) — every real upload (`Api\SellerProfileController`, `Web\Account\SellerRegistrationController`) stores under `sellers/nida/`+`sellers/licences/`, and `DemoSeeder` stores under `demo/nida/`+`demo/licences/`. Three genuinely disjoint prefixes, already an established convention in this codebase (the same `seed/`-prefix pattern already exists on `ShowcaseFactory`/`UpdateFactory`'s media paths) rather than something invented for this task. The step requires *both* columns to match the `seed/` prefix (not just one) as a redundant cross-check, then additionally refuses to run at all if any matched row's handle appears in `DemoSeeder::demoHandles()` or its `user_id` belongs to an admin account — a belt-and-suspenders assertion that should be structurally impossible to trip, kept anyway since the cost of checking is a few milliseconds against the cost of getting this wrong. **Verified end-to-end, not just reasoned about**, against a disposable isolated SQLite copy (never the real dev or production database): ran `migrate:fresh` + the original `db:seed` + `DemoSeeder` together (32 sellers total, 20 seed-marked + 12 demo-marked, confirmed by direct count before touching anything), ran the exact identification query (20 candidates, zero overlap with demo handles or the admin account), then the exact deletion call — result was precisely 12 sellers remaining (all demo-marked), products dropped 180→60, orders 98→28 (a clean -70, matching `DatabaseSeeder`'s own documented order count exactly), reviews 75→15 (a clean -60, matching its documented review count exactly), comments unchanged at 8 (all of them DemoSeeder's, since the original seeder never created any), the admin account and all 12 demo handles intact. Deletes via each matched seller's `users` row, not the `seller_profiles` row directly: `seller_profiles.user_id` is itself `ON DELETE CASCADE` from `users`, so a single `DELETE FROM users WHERE id IN (...)` ripples through every dependent table (products → product_media, orders → order_items/reviews, comments, conversations → messages, favorites, customers, updates, offers, showcases, leads) via real multi-level database foreign keys — confirmed every one of those tables' relevant FK is actually `cascadeOnDelete()` by reading each migration directly rather than assuming. Defaults to a dry run (lists every matched shop with its own product/order/review counts, plus a grand total, and deletes nothing) — add `&confirm=1` only after reviewing that list. The whole delete runs inside `DB::transaction()` so a failure partway through rolls back rather than leaving a half-cleaned database. Deliberately scoped to exactly what was asked (shops + their products/media/orders/reviews/comments) and no further: the original seeder's 40 anonymous Faker buyer accounts (`User::factory(40)->create()`, never seller profiles) are left untouched, since they weren't named in the request, have no equivalent structural marker to identify them safely on their own, and are harmless leftover accounts that don't appear in any shop ranking or listing — deleting user accounts beyond the seller's own login felt like a larger, unrequested action to take unprompted on production data.
- **2026-08-30 — Two homepage bugs, both traced to a real root cause rather than patched at the symptom.** "Latest listings" ascending-id order was a genuine, reproduced tie-break bug, not a misreading of `latest()`: `Product::latest()` correctly orders by `created_at DESC`, but `timestamps()` has no fractional-second precision, and a bulk-seeded/imported batch (exactly what the previous round's seed cleanup left behind) routinely inserts many rows within the same wall-clock second — confirmed directly against the dev database (27 products sharing one identical `created_at` value) before touching any code, then confirmed the *exact* observed symptom by running the unmodified query and getting back ascending id order for the tied rows. Fixed by adding `->orderByDesc('id')` as an explicit tiebreaker after `->latest()` — `id` is monotonically increasing with insertion order, so it's the only thing that still distinguishes "genuinely newest" once `created_at` ties. The "Browse categories" count removal reuses the same category-count-hiding boundary already established two rounds ago for the header nav (per tester feedback item 3 back then): `CategoryCatalogService::withCounts()` itself is untouched, since the header nav and direct `/c/{category}` links still need to resolve a temporarily-empty category correctly — the zero-count filter (`->filter(fn ($c) => $c->products_count > 0)`) is applied only to the `Collection` `HomeController` hands to the home view, scoping the fix to exactly the section that was reported rather than changing what "a category" means everywhere.
  - **Every new test was verified to actually fail against the pre-fix code, not just pass against the fix** — caught two real test-authoring mistakes doing this rather than trusting a first draft: (1) the ordering test's seller needed `lat`/`lng` explicitly nulled out, since a seller with real Dar es Salaam coordinates (the factory default) makes their products also eligible for the separate, earlier "Near You" section, which sorts by distance and would satisfy `assertSeeInOrder()` from its own unrelated ordering regardless of what "Latest listings" actually did below it — the first draft of this test passed even against the reverted, buggy controller because of exactly that contamination. (2) The "no raw count" test originally asserted `assertDontSee('(3)')`, but the actual old markup never wrapped the count in parentheses at all (it rendered the bare number in its own `<span>`) — that assertion would have passed regardless of whether the real bug was fixed, so it was rewritten to assert the exact removed HTML fragment (`'text-sokoni-black/40">3</span>'`) is gone, which does correctly fail against the reverted code.
  - `php artisan test`: 370/370 passing (3 new). One unrelated transient failure seen on an earlier run (`SmokeTest`'s real outbound call to `oauth2.googleapis.com` timing out) — confirmed non-reproducing and unrelated to this change by re-running the full suite clean immediately after.
- **2026-09-02 — `DemoSeeder` made genuinely self-healing, not just idempotent, for product/Update/Showcase media.** Root cause of the reported "logos wrote, product images didn't": `seedShops()` unconditionally rewrites the logo file every run (no guard at all), while `seedProducts()` only wrote a product's placeholder file inside `if ($product->media()->count() === 0)` — a *database-row* existence check standing in for a *file* existence check. Once a `ProductMedia` row exists (from any earlier run, including one where the file was subsequently lost to a storage reset/move), that guard skips the whole block forever, file included. Fixed by decoupling the two: every product/Update/Showcase's disk path is deterministic (derived from the row's own stable identity — handle+title for products, handle for Update/Showcase), so it's recomputed and checked against the disk itself on every run via a new `ensureFileExists(string $path, Closure $content)` helper, independent of whether the owning row already exists; the row-creation guard stays exactly as it was, now decoupled from file-writing entirely.
  - **Updates and Showcases had a second, separate bug this surfaced**: neither ever had any file-writing code at all — their `media_path`/`thumb_path` were always dangling references, seeded or re-seeded, since the seeder was first written. Applying the same self-healing logic here meant actually building it for the first time, reusing `ProductPlaceholderImage::svg()` (with the generic `more_horiz` icon, since an Update/Showcase isn't tied to one category the way a product is) rather than inventing a second generator.
  - **Changed both paths' extension from `.jpg` to `.svg` while doing this** — the placeholder generator produces SVG, and Apache's default MIME mapping serves a `.jpg`-named file as `Content-Type: image/jpeg` regardless of what bytes are actually inside it, which browsers then refuse to render as an image at all; matches the `.svg` extension `seedProducts()`/`seedShops()` already use correctly for the same generator. Re-running `demo:seed` in production will update existing Update/Showcase rows' stored paths from `.jpg` to `.svg` via the normal `updateOrCreate()` call — a one-time, self-correcting side effect of this fix, not a separate migration.
  - **`Showcase::video_path` is deliberately left unfixed, not silently skipped** — there is no video-generation capability anywhere in this codebase (no ffmpeg, no video library; a standing, repeatedly-documented constraint of this project's shared-hosting deploy target), so no genuine placeholder clip can be produced. Writing arbitrary bytes at a `.mp4` path would pass a bare `exists()` check while still being an unplayable file — a more deceptive failure than a visibly-missing one — so the video file stays exactly as absent as it's always been, called out explicitly in code comments and in a dedicated test assertion (`assertMissing($showcase->video_path)`) rather than left as an unstated gap.
  - **Every new test was verified to actually fail against the pre-fix code**, same discipline as the previous round: temporarily reverted the `ensureFileExists` calls (for products, back to the exact original inline `Storage::put` gated on `media()->count() === 0`; for Update/Showcase, removed entirely, matching that nothing was ever written there before) and confirmed all three new tests failed with the expected "file not found" message before restoring the real fix.
  - `php artisan test`: 373/373 passing (3 new).
- **2026-09-03 — Client/tester feedback round, Part A (website bugs) complete.**
  - **A1**: root cause was `@{{ $seller->handle }}` on the shop page and the admin verification queue — `@{{ }}` is Blade's escape for a *literal* `{{ }}` in the output (for JS frameworks sharing that delimiter), not a way to write a literal `@` before an expression. It never worked; fixed both to `{{ '@'.$seller->handle }}`. Grepped every view and lang file for the same pattern and for a `{{` accidentally baked into lang-file string content — found and fixed both occurrences, found zero lang-file cases.
  - **A2**: the "Message" button on product/shop pages always linked to the conversation *list*, with no way to actually start a thread — `MessagesController` had `index`/`show`/`poll`/`store` (send into an existing thread) but no equivalent of the API's `ConversationController::store()` (create-or-resume). Added `MessagesController::start()`, reusing the API's own `StartConversationRequest` so a thread started from the website is created identically. The route had to be declared *before* the existing `{conversation}` routes — `/account/messages/start` would otherwise be swallowed by `{conversation}`'s route-model binding as a literal id lookup.
  - **A3 turned out to be three separate "doesn't exist" gaps, not one "missing confirmation" gap — verified by grep before assuming, in every case**: (1) **Reviews** — no submission form existed anywhere on the website, only display; built `OrdersController::storeReview()` reusing the API's own `StoreReviewRequest` (the entire anti-fake-review mechanism — completed order, not already reviewed, ownership), so a review left on the web is validated identically to one left in the app. (2) **Reports** — CLAUDE.md feature 11 ("report button on every product, shop and message") was never built on the website at all; built `ReportController` + a shared `<x-report-button>` component reusing the API's `StoreReportRequest`, wired into product and shop pages. **Message-level reporting deliberately deferred**: messages render client-side via Alpine from polled JSON, not a server Blade loop, so a per-message report action needs real per-message inline UI wiring, a meaningfully bigger lift than the two pages already covered — logged here rather than half-built. (3) **Comments** — genuinely absent on the website, not even *displayed*, let alone submittable; building a full comment-thread UI (list + one-level replies + moderation-hide filtering) from scratch is a materially bigger feature addition than "add a confirmation toast" implies, and was judged out of proportion to squeeze into an already-large batch — flagged for a separately scoped round rather than built partially. **The actual root cause behind every one of these being silently swallowed even where they DO now exist**: no shared flash-message partial existed anywhere — three separate pages (settings, shop dashboard, shop products) had each independently remembered to add their own `@if (session('status'))` block, and every other redirect-with-flash across the site (reviews, reports, seller registration's own success path) had nothing rendering it at all. Fixed once, structurally: a single `partials.flash` included in `layouts.app`, and removed the three now-redundant per-page copies (unifying their two different color treatments — one used yellow, two used green — into the one shared green success style, a deliberate simplification, not an oversight). **A real, narrow flash-survival gotcha found while testing this**: Laravel's session-flash data only survives exactly one additional request, so a report/review submitted from a *non-canonical* URL (e.g. a stale bookmarked product link missing its slug) loses its confirmation message to `ProductController`'s own 301 canonical-slug redirect swallowing the one hop the flash had — a real visitor reporting from the page they're actually looking at is never on such a URL, so this doesn't affect the fix's correctness, but it's a genuine edge case worth knowing about if a similar canonical-redirect pattern gains a flash-bearing action later.
  - **A4**: the product-photo AJAX manager with per-file progress bars was already built and tested in an earlier round and is confirmed intact in source — the most likely explanation for "no progress indication" in production is a stale or missing Vite build on whichever docroot was actually serving live traffic at the time (this task's own closing note confirms `/home/sokoftsn/public_html` is now the live site, a docroot with its own physically separate `build/` copy per `docs/DEPLOY_WEB.md`) rather than a code defect — flagged explicitly rather than silently re-built. Seller logo upload got the full AJAX+compression+progress treatment as part of A5 below. NIDA/licence uploads are part of a single one-shot multi-field registration form, not a standalone action — real per-byte progress there would need converting each into its own staged-upload AJAX endpoint (the same "needs a parent row to attach to first" problem product photos already solved by requiring the base product to exist first), a disproportionate lift for two files inside one form; added the proportionate honest version instead — an immediate "selected: filename.jpg" acknowledgment per file, and a whole-form "Submitting…" disabled-button state — rather than fabricate a fake per-byte progress bar over a single-request submission.
  - **A5**: sellers had no way to change their shop logo from the website at all — the API endpoint (`Api\SellerProfileController::updateLogo`) already existed and was directly reusable. Built `ShopLogoController` + a `shopLogoUploader` Alpine component (client-side compression to 800px/0.85 quality, matching the product manager's own shape) in account settings, gated on the signed-in user actually having a seller profile.
  - **A6**: opening hours had never been editable on *either* surface — `App\Support\OpeningHours`'s own docblock claims "the app's Edit Profile screen writes this same shape," but grepping the entire Flutter app and every API route confirms no such screen or endpoint exists anywhere; this is a stale/aspirational comment in the same vein as several other "already wired" claims this project has caught before (see the Google Maps and Firebase entries earlier in this log) — noted here rather than trusted, and left for a future round to fix on the app side since B/C didn't ask for it. Built the first real writer: `UpdateOpeningHoursRequest` (per-day `closed` toggle or `open`/`close` in `H:i`, close-after-open enforced) + `ShopHoursController`, added to the shop dashboard as a collapsible editor with 08:00–18:00 defaults for any day not yet set. Also translated the shop page's day names while touching this — they were rendering the raw English key (`monday`, `capitalize`d) regardless of locale, a small pre-existing bilingual gap in the same area.
  - **A7**: the header wordmark was wrapped in `hidden sm:inline` — deliberately hidden below the tablet breakpoint since an earlier design pass, which is exactly what read as "the site name doesn't show at the top" on an actual phone; now always visible, sized down slightly on the smallest screens. The mobile hamburger button's real tap target was 40px (a 24px icon plus 8px padding each side) despite the icon itself already meeting the 24px visual-size bar — bumped to an explicit `min-h-44 min-w-44` wrapper. The fixed bottom nav (the actual primary mobile navigation surface) was already correct on both counts — 24px icons inside effectively 56px-tall, `flex-1`-wide tap targets — audited and left alone rather than churned.
  - `php artisan test`: 400/400 passing (27 new this part).
- **2026-09-03 — Client/tester feedback round, Part B (app bugs) complete.**
  - **B1 turned out to be three compounding, independently-real bugs, not one.** (1) `CachedNetworkImage` cannot decode SVG at all — every real seller photo is raster (`ImageVariants` always writes `.jpg` server-side regardless of upload format), but the demo catalogue's placeholder products/updates/showcases are plain SVG, so any of those rendered as a blank/broken image in the app specifically, even though the same URL displays fine on the website (browsers decode `<img src="*.svg">` natively). Built `SokoniNetworkImage`, a drop-in replacement dispatching to `flutter_svg` for `.svg` URLs and the existing `CachedNetworkImage` for everything else — mechanically replaced across all 16 call sites via script rather than by hand, to guarantee none were missed or subtly mismatched. Its SVG-vs-raster dispatch rule is covered by a real, network-free unit test on a `static isSvgUrl()` helper, not a widget test — mounting the actual widget always makes a genuine, un-mockable network call the instant it builds, which this codebase has already hit as a "Timer still pending" test failure once before (see the Phase 4 decision on this). (2) `product_media.path`/`thumb_path`/`card_path` are full absolute URLs baked in at *upload time*, not computed dynamically at read time — PUBLIC_UPLOADS_URL recently changing from the beta host to the root domain means every row uploaded before that change keeps the old, now-wrong host forever regardless of what `.env` says now. This is a data problem, not a code bug: added `rewrite-media-host` to `setup.php`, a dry-run-by-default, explicit-`&from=&to=`-required, plain SQL `REPLACE()` across every column that stores an absolute media URL (`product_media`, `seller_profiles.logo`, `users.avatar`, `updates`, `showcases`, `messages.attachment`) — no hardcoded guess at the old/new host strings, since a wrong blind replace across production media URLs is exactly the kind of mistake worth refusing to make silently. (3) `UpdateResource`/`ShowcaseResource` pass `media_path`/`thumb_path`/`video_path` straight through with no URL-prefixing of their own, unlike Product/ProductMedia's already-correct convention — and `DemoSeeder`'s `seedUpdatesAndShowcases()` was in fact storing bare relative paths for these two, meaning they were never a loadable URL for *any* client (web or app), demo or real, from the day this feature was built. Fixed by wrapping every path in `Storage::disk('public')->url()` there too, matching the real `Api\UpdateController`/`ShowcaseController`'s own convention exactly. **The app's offline cache was audited, not modified** — `ProductRepository` always overwrites its cache wholesale on a successful fetch and only reads it back on a genuine network failure, so there's no separate "dead entry" that outlives a backend fix; the only unfixable case (a user who never gets back online) is inherent to offline-first caching, not a bug.
  - **B2**: the home app bar's own docblock always called for "a top bar (wordmark, ...)", but the actual code just rendered `Text(l10n.appName)` — no image at all, ever. The brand asset itself was already correctly bundled (used by the splash and pull-to-refresh indicator) and simply never referenced from here — added it alongside the text.
  - **B3**: `DiscoveryMapView` and seller onboarding's step 2 had *never* shown a real map — `SokoniMapsConfig.isConfigured` has been hardcoded `false` since Google Maps billing was never enabled, so both always fell back to their "map unavailable"/manual-entry-only states regardless of device or connectivity. Replaced `google_maps_flutter` with `flutter_map` against OpenStreetMap's public tile server end to end — no API key or billing account needed at all, the same approach the website's own shop-location embeds already use — removing the `isConfigured` gate, the dead `SokoniMapsConfig` file, both native `REPLACE_ME` placeholders (`AndroidManifest.xml`'s `com.google.android.geo.API_KEY`, `AppDelegate.swift`'s `GMSServices.provideAPIKey`), and five now-unused ARB keys. BLOCKERS.md's Google Maps entry is closed by removal, not left waiting on billing. flutter_map has no "camera idle" event the way `GoogleMap` did (`onPositionChanged` fires on every drag frame); step 2's drag-to-reverse-geocode behaviour is reproduced with a 500ms debounce Timer instead.
  - **B4**: `AndroidManifest.xml` declared no `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION` permissions at all, and `Info.plist` had no `NSLocationWhenInUseUsageDescription` — without the manifest declaration, Android's runtime permission dialog never has anything to actually request (so `Geolocator.requestPermission()` could only ever behave as permanently denied, regardless of what the user chose), and iOS crashes outright on the very first location call with no usage-description string present at all. This alone plausibly explains the entire reported symptom ("current location not picking up") without any Dart-side logic being at fault — `LocationService`'s own permission/timeout/denied-vs-disabled handling and the district-picker fallback's messaging were both audited and found already correct (a real, explicit, non-silent dialog already exists — `locationPermissionTitle`/`Body` — a 3-line copy tweak wasn't needed on top of a two-platform permission-declaration bug that would have defeated it regardless).
  - **B5**: traced to a single, codebase-wide bug in `mapDioError`, not anything checkout-specific — Laravel's top-level `"message"` on every 422 validation response is always the same generic `"The given data was invalid."` regardless of which field actually failed; the real, specific, actionable reason (`"All items in one order must be from the same seller."`, `"The address field is required..."`, etc.) only ever lives in the response's `"errors"` map, which was already being parsed correctly into `ValidationException.errors` but never consulted for the exception's own displayed `.message`. Every field name and enum value the app sends (`payment_method`, `delivery_method`, item shape) was checked directly against `CheckoutRequest`'s validation rules and found to already match exactly — there was no app-vs-API mismatch to align, just an unhelpful message everywhere a 422 could ever occur, not only at checkout. Fixed once in `mapDioError`, benefiting every form in the app that hits a validation error, not just this one screen.
  - **B6**: added a persistent (not just on the fresh-order moment) delivery/payment clarity banner to the order detail screen, shown for every non-cancelled order — deliberately *not* crammed into the full-screen success checkmark, which CLAUDE.md itself fixes at a 1.6s auto-dismiss; a moment that short was never going to fit a real sentence of information. The order-detail screen already doubles as the de facto "confirmation screen" for the majority of orders (checkout lands there directly) — the one gap, logged rather than silently papered over, is the minority path where an order creates a conversation and checkout redirects there instead, which currently has no equivalent notice of its own.
  - `flutter analyze`: clean. `flutter test`: 62/62 passing (11 new this part, all verified to actually fail against the pre-fix code first, same discipline as every other round this session).
- **2026-09-03 — Client/tester feedback round, Part C (client changes), C1-C4 complete.**
  - **C1**: category renames needed to preserve id/FK references, which rules out the existing `Category::updateOrCreate(['name_en' => X], ...)` seeding pattern entirely — that's keyed by name, so a rename via it creates a duplicate row and orphans every product/seller pointing at the old one. Wrote a real migration (`2026_09_03_090000_rename_and_add_categories.php`) doing raw `DB::table()` updates by old name instead, with a `down()`. Because category slugs are computed from `name_en` (never stored), a rename silently breaks every existing `/c/{slug}` link and any external SEO pointing at it — added an explicit `SLUG_REDIRECTS` map in `CategoryController` returning a 301 to the new slug, preserving any child segment, so nothing 404s. Real Estate is deliberately category-only per the task's own instruction — no property-specific attributes (bedrooms, lot size, etc.), since `products` has no schema room for them and building a parallel listing type is a materially separate vertical, logged as out of scope rather than half-built. **Migration/test interaction worth remembering**: this migration unconditionally inserts Real Estate + Kids as part of the base schema every `RefreshDatabase` test now starts from — a test asserting "zero categories exist before any seeder runs" had to become "two," since that's now structurally true of a fresh migrate.
  - **C2**: shop-name matching reuses `StoresController`'s existing shop_name/handle LIKE query rather than writing a second one, grouped separately from products in the results view; shops render first only when the query looks like it's naming a shop (matched something) rather than always-first, so a product-title search doesn't get buried under an unrelated shop match. Verified from the home hero specifically (not just `/search` directly), since the task called that out as the actual regression risk.
  - **C3**: "counts per category within the current result set" needs a query that respects every active filter except category itself — added `ProductSearchFilters::withCategoryId()` as a clone-with-override and a new `ProductSearchService::categoryCounts()` doing one `GROUP BY category_id` query via `->toBase()` (dropping Eloquent's eager-loading, which isn't needed for a count). Sidebar tree is single-select drill-down as asked, not multi-select — expandable children only render once a top-level category is the active filter.
  - **C4**: read `Api\SellerProfileController::updateIdentity()` (step 3, NIDA) before touching anything, and confirmed *that* call — not licence upload — is the real trigger that's meant to put a seller in front of a human reviewer; `updateLicence()`'s own docblock claiming "submitting this puts the seller in the verification queue" was stale and wrong, matching a pattern this project has caught more than once before (see the opening-hours and Google Maps entries above). This makes licence fully optional a safe, no-side-effect change: `SellerOnboardLicenceRequest`'s rule loosened to `nullable`, both `Api\SellerProfileController::updateLicence()` and `Web\Account\SellerRegistrationController::store()` guard the file-handling behind `hasFile()`/a null-coalescing store call, and the Flutter wizard's step 4 (`step4_licence.dart`) drops its "file required" guard entirely — the same Continue button now either uploads a chosen file or advances with `licenceFilePath: null`, labeled "Skip" when nothing's selected so it's honest about what pressing it does. Updated every surface that described the old requirement: the web registration form label, Filament's verification-queue labels and infolist description ("NIDA photo (basis of verification)" / "Business licence (optional extra)"), and the public safety/sell/about copy in both languages — all reframed around NIDA as the actual basis, licence as an optional extra visible to admins if provided.
  - `php artisan test`: 423/423 passing (11 new this part so far — C1: 7, C2/C3: 9, C4: 5). `flutter analyze` clean, `flutter test`: 62/62 passing (no new Flutter tests needed for C4 — the changed logic is a removed client-side guard with server-side coverage already in place, verified manually that the widget compiles and the skip/submit label toggle renders correctly).
- **2026-09-03 — Client/tester feedback round, Part C, C5 (email infrastructure) complete.**
  - **Verification, built on the framework's own primitives, not a bespoke flow**: `User` now implements `Illuminate\Contracts\Auth\MustVerifyEmail` and uses the `Illuminate\Auth\MustVerifyEmail` trait (name-collision between interface and trait resolved by importing only the trait bare and aliasing the interface) — this is the same mechanism Laravel scaffolding (Breeze/Jetstream) wires up, just done by hand since this app has neither. `VerifyEmailNotification` extends the framework's own `VerifyEmail` and overrides only `toMail()` for bilingual content — the signed-URL generation (`verification.verify` route, id/hash, expiry) is inherited unchanged.
  - **The verification route is deliberately NOT behind an authenticated session** (a real deviation from Breeze's own default, logged because it's a judgement call, not an oversight): this app's users are primarily on the mobile app, and the browser that opens a link from an email client has no reason to already carry a logged-in *website* session. The signed URL itself (expiring, tamper-proof, id+hash matched against that exact user's current email) is the actual proof of intent — requiring the clicking browser to also be an authenticated session for that same user would lock out most people who'd otherwise successfully verify.
  - **`HandlesEmailChange` is the single place "did the email change?" logic lives** — the website's settings form and the app's new `PATCH /auth/profile` endpoint both call it, so a changed address always resets `email_verified_at` and re-sends the link identically regardless of which surface changed it; clearing the field back to null is treated as a normal action (email is never required — NIDA is still the actual verification basis, see C4).
  - **The resilience requirement ("a mail failure must never fail the user's action") is enforced structurally, not by convention**: every send goes through `SafeMail::send()`, which catches and logs to a new dedicated `mail` log channel (same reasoning as the existing `sms` channel — cPanel-File-Manager-diagnosable without shell access) rather than letting an exception bubble into the calling controller. `User::sendEmailVerificationNotification()` itself is overridden to route through this — the single framework entrypoint every caller (registration, a settings save, a resend) already uses, so there's no second call site to remember to wrap.
  - **`QUEUE_CONNECTION=database` with no worker running this host** (confirmed via survey, not assumed) rules out `ShouldQueue` on any of these notifications — every send here is synchronous, matching the existing inline `PushNotifier::notify()` calls it sits beside in `OrderController`.
  - **`MAIL_ENCRYPTION` wasn't actually being read anywhere** — modern Laravel's SMTP transport infers implicit-TLS purely from port 465 and ignores an `encryption` config key entirely. Since the client's own instruction named `MAIL_ENCRYPTION=ssl` explicitly (the way cPanel mailbox setup is always described to non-developers), wired `config/mail.php`'s `scheme` to honour it directly (`ssl`→`smtps`, `tls`→`smtp`) rather than ship a documented env var that silently does nothing.
  - **Order-placed/status-changed and review-received emails sit directly beside the existing push-notification call sites** (`OrderController::store()`/`updateStatus()`, `ReviewObserver::created()`) rather than a new observer layer — each only fires when the recipient actually has an email, and each is independently guarded by `SafeMail`.
  - **Web registration (bare phone-OTP, "under 30 seconds" per the original spec) deliberately does NOT gain an email field** — only the app's own "Create an account" details step (which already collects many more fields) does, plus the pre-existing settings pages on both surfaces. Squeezing a new field into the minimal web OTP form would work against an explicit, separate design goal (fast web sign-up) for a field this task itself calls optional; a web user can add it from Settings immediately after.
  - `php artisan test`: 444/444 passing (21 new this part — 16 in `EmailVerificationTest`, 6 in `OrderAndReviewEmailNotificationsTest` — 2 of the 21 verified to actually fail against reverted pre-fix code, same discipline as every other round). `flutter analyze` clean, `flutter test`: 69/69 passing (7 new — 2 covering the new optional email field flowing through `register()`, 5 covering the new `SokoniValidators.optionalEmail`). New migration: `2026_09_03_100000_add_email_verified_at_to_users_table`. A pre-existing, unrelated app test (`create_account_step2_details_test.dart`) had a fragile `find.byType(TextFormField).last` selector that happened to hit the phone field only because it was previously the last one on the buyer path — adding the new email field after it silently redirected that selector to the wrong field; fixed by matching on the phone field's actual label text instead, the same pattern the adjacent "Full name" assertion already used.
- **2026-09-03 — Client/tester feedback round, Part C, C6 (admin bulk SMS) complete — the whole batch (A-C) is now done.**
  - **The hardest real constraint was "throttled sending so it doesn't time out on shared hosting," not the SMS API integration itself.** Sending even a few hundred recipients sequentially inside one Filament/Livewire HTTP request would either hit PHP's `max_execution_time` or hammer the SMS provider all at once — and this project has repeatedly established there's no persistent queue worker on this host (`QUEUE_CONNECTION=database`, nothing consuming it). The fix reuses a pattern this codebase already relies on for exactly this constraint (`updates:delete-expired`, fired every minute by the cron already running `schedule:run` per docs/DEPLOY.md): the Filament page (`app/Filament/Pages/BulkSms.php`) only *snapshots* the matching recipients into new `sms_blasts`/`sms_blast_recipients` tables when "Send" is confirmed; a new `sms:process-blasts` command, scheduled `everyMinute()->withoutOverlapping()`, sends one bounded batch (`sokoni.sms_blast_batch_size`, default 20) per tick until the blast is fully sent. This is a materially bigger design than "just loop and send" would have been, but it's the only version that's actually honest about the hosting constraint rather than working until someone tries it on a real audience of a few hundred people.
  - **`SmsGateway` gained a second interface method, `sendMessage(string $phone, string $body): void`**, alongside the existing OTP-shaped `sendOtp()` — the bulk tool composes arbitrary text, not a fixed OTP/notification message. All four implementers (`TextifySmsGateway`, `AfricasTalkingSmsGateway`, `BeemSmsGateway`, `LogSmsGateway`) needed the new method; each was refactored to share its existing transport/logging/failure-handling code between `sendOtp`/`sendMessage` via a private `send()` helper rather than duplicating the HTTP call and error branches — "per-recipient logging to sms channel" (the task's own requirement) falls out of this for free, since it's the exact same logging path OTP sends already use, not a new one built to satisfy the checklist.
  - **Audience resolution lives in one place** (`App\Services\Marketing\SmsAudienceResolver`), used identically by the live recipient-count preview and the actual snapshot-at-send-time query, so they can never disagree. "All buyers" has no dedicated flag on `users` — resolved to "no seller profile" (`whereDoesntHave('sellerProfile')`), the same ground truth `User::isSeller()` already uses elsewhere, rather than trusting `account_intent`, which is only a signup-time hint that can drift from reality. Every audience additionally excludes banned/suspended accounts and anyone with no phone number — a judgement call, not asked for explicitly, but sending a promotional blast to a banned account or one with nothing to receive it at is never intended behaviour.
  - **The cap is admin-configurable, the per-tick throttle isn't** — `max_sms_blast_size` (default 500) follows the exact existing `Settings`/`config('sokoni.*')` override pattern (`search_radius_km`, `max_media_per_product`, etc.), showing up automatically on the existing admin Settings page since it's included in `Settings::all()`; `sms_blast_batch_size` (default 20) is a plain env-only infra knob, deliberately not exposed there, since it's about protecting the host/provider, not a business policy an admin should be tuning.
  - **Confirmation and the cap are both re-checked at actual send time, not just when "Preview & confirm" was clicked** — the audience is live data (`SmsAudienceResolver::query()` is re-run in `send()`, not reused from the confirm step), so a few minutes of a fluctuating audience between the two clicks can't silently blast more (or different) people than what was shown.
  - **`SmsBlast`/`SmsBlastRecipient` declare `#[Fillable([])]`, matching `ActivityLog`'s "never settable from a request" convention** — but unlike `ActivityLog` (write-once), these rows *do* need internal status transitions after creation (`ProcessSmsBlasts` moving a recipient from pending→sent/failed, a blast from pending→processing→completed) — the first draft used `->update([...])` for these and every test calling the command failed immediately with "Add [status] to fillable property," which is exactly the guard doing its job; fixed by using `forceFill()->save()` for these specific internal-only transitions, the same convention `Order::updateStatus()` already established for its own protected status columns.
  - **Activity-log entry records the queue moment, not the completion moment** — "who sent what to how many" is logged via `ActivityLogger::record()` the instant a blast is confirmed and queued (audience, consent-only flag, recipient count), since actual per-recipient sent/failed outcomes only exist once `ProcessSmsBlasts` finishes processing asynchronously over the following minutes; the blast's own row (visible in the page's "Recent blasts" history, with a per-blast "View failures" list showing which recipients bounced and why) is the durable source of truth for the final outcome, not a second retrospective activity-log write.
  - **`marketing_consent`** (new `users` boolean, default false) is opt-in everywhere it's collected: the Flutter "Create an account" wizard's Step 3 (alongside the existing required Terms checkbox — the natural, already-visited spot, not a new screen), and the equivalent moment on the website's phone-OTP signup form (shown only for a brand-new account, same as the name field beside it). Unlike C5's optional-email decision, a single checkbox is a negligible interaction cost, so it was **not** treated the way C5's email field was — it's on the fast web-OTP form too, not deferred to Settings-only. The bulk-SMS tool's own "consenting users only" toggle applies this filter directly through `SmsAudienceResolver`.
  - `php artisan test`: 467/467 passing (23 new — 2 in `TextifySmsGatewayTest` for the shared-transport `sendMessage`, 12 in `BulkSmsTest`, 5 in `ProcessSmsBlastsTest`, 4 for `marketing_consent` at registration across both API and web). `flutter analyze` clean, `flutter test`: 71/71 passing (2 new, covering the consent checkbox flowing through to `register()` and defaulting to `false` when left unchecked). New migrations: `2026_09_03_110000_add_marketing_consent_to_users_table`, `2026_09_03_120000_create_sms_blasts_table`.
- **2026-09-03 — Subcategory taxonomy, seller subcategory picker, and a mega menu (all three requested together — "two navigation features, but first the data they depend on").**
  - **Subcategory data**: extended `CategorySeeder` itself (not a separate seeder) with a `SUBCATEGORIES` map keyed by each parent's own `name_en`, ~76 children across 12 of the 13 top-level categories (4–8 each, matching the client's own examples plus filling in the rest for Food & Groceries, Home & Furniture, Beauty & Health, Cereal & Legume, Services) — kept in one file since it's the same tree, seeded and re-seeded together, and `updateOrCreate` keyed on `['parent_id', 'name_en']` makes it idempotent the same way the existing top-level seeding already was. **"Other" deliberately gets none** — it's the catch-all bucket; subdividing a catch-all defeats its purpose. `setup.php` gained a `seed-categories` step calling `CategorySeeder` directly (not the full `db:seed`), so this can be backfilled onto an already-live database without SSH.
  - **A real, previously-latent bug this surfaced**: `Product::scopeInCategory()` filtered on exact `category_id` equality — harmless while every product could only ever be tagged with a top-level category, but the moment a seller can pick "TVs" under Electronics, browsing "Electronics" itself would show *nothing* for that product, even though `ProductSearchService::categoryCounts()` (built in C3) already summed a parent's count from itself *plus* its children — the sidebar would show a real count next to Electronics that clicking into then wouldn't back up. Fixed by having `scopeInCategory()` expand a top-level category id into itself plus its children's ids (`whereIn`) before filtering; a child id stays an exact match, since categories are two levels only. Caught via a dedicated test (`browsing the parent category includes products tagged with a subcategory`) before this ever reached a real seller.
  - **Sellers pick a subcategory as a genuinely optional second step, defaulting to the parent — no schema change needed.** A product's existing single `category_id` FK can already point at either level; the "subcategory" a seller picks is just a more specific id in the same column, so `ProductStoreRequest`/`ProductUpdateRequest`'s existing `exists:categories,id` rule needed no change at all. Web: the product form's old flat "every category, mixed levels" `<select>` (which would have listed ~89 rows indiscriminately the moment subcategories existed) is now two selects — a required top-level "Category" and an optional "Subcategory" filtered client-side (Alpine, from a `parentId => [{id, name_en}]` map embedded once, no round trip) — with a hidden `category_id` input resolving to whichever is more specific. App: the same split, via two new derived providers (`topLevelCategoriesProvider`, `subcategoriesProvider(parentId)`) layered over the existing flat `categoriesProvider` — chosen specifically so the three *other* existing consumers of that flat list (home category chips, seller onboarding's shop category, "Create an account"'s seller step) could be pointed at `topLevelCategoriesProvider` instead of duplicating a filter at each call site, and so none of them silently started showing subcategories mixed in once seeded.
  - **Mega menu is entirely server-rendered — every category and subcategory link exists in the raw HTML response, `x-cloak`'d, not injected via JS or fetched on hover.** This was the explicit "render it server-side so it's indexable" requirement, and it's also what makes the mobile accordion trivial: it's the same tree, same links, just reachable by tapping instead of hovering, in the header's existing slide-in drawer rather than a second competing overlay.
  - **The cached tree deliberately does NOT repeat the shape that caused the still-open `__PHP_Incomplete_Class` production incident** (`CategoryCatalogService`'s own docblock, `withCounts()` — still uncached, out of scope here): that cached an Eloquent `Collection` of `Category` models; `megaMenuTree()` caches a plain nested array of ints/strings only, which cannot hit the same class-autoload-on-unserialize failure mode structurally, not just by luck. A test round-trips it through `serialize()`/`unserialize()` as a direct regression guard. Invalidation is immediate, not TTL-only: `Category::booted()` busts the cache key on every `saved`/`deleted`, so a rename made through the (already-existing, unmodified) Filament admin form or the seeder shows up on the next request — the 1-hour TTL is a ceiling on staleness after a cache-store hiccup, not the normal path.
  - **A real interaction bug caught before shipping, not after**: the first draft put `@click.outside="close()"` on the dropdown panel itself. Clicking a *different* trigger link is technically a click outside the panel, so Alpine's document-level outside-click listener would fire `close()` immediately after that same click's own `@click` handler had just called `toggle()` to open it — the menu would flicker open and instantly shut. Fixed by moving `@click.outside` up to the whole `<nav>` wrapper (triggers row *and* panel both), so only a click genuinely outside the entire component closes it.
  - **Keyboard/focus handling**: `@focus` on each trigger link opens the same way `@mouseenter` does (same 150ms delay, for consistency, not because keyboard users need the debounce), `@keydown.escape.window` closes from anywhere, and `@focusout` on the nav root closes only when focus actually leaves the whole component (`$event.relatedTarget` not contained in it) — tabbing from a trigger into its own panel's links never closes it prematurely.
  - **Search sidebar (C3) verification, as asked**: the existing C3 tests used ad hoc `Category::factory()` rows and never actually exercised the real seeded taxonomy or the "keeps other filters" claim end to end. Added tests against the real `CategorySeeder` tree, plus two tests extracting a rendered drill-down `<a href>` directly (not assuming a fixed query-param order, since `fullUrlWithQuery()` doesn't guarantee one) and asserting every other active filter (`q`, `region`, `price_min`, `price_max`, `sort`) survives the click — both verified to actually fail (missing filters in the href) against a deliberately reverted plain `route()`-based link before being accepted, same discipline as every other round this project has used.
  - `php artisan test`: 488/488 passing (20 new). `flutter analyze` clean, `flutter test`: 71/71 passing (no new Flutter tests needed beyond the existing suite — the changed logic is provider filtering + a UI cascade covered by the same widget rebuild `flutter analyze` already type-checks; the real business-logic risk, category filtering, is entirely backend and covered there). No new migrations — subcategories are plain rows in the existing `categories` table.
