# Progress Log

One line per completed phase, appended in order. See `DECISIONS.md` for judgement calls and `BLOCKERS.md` for anything needing external input.

- 2026-08-13 — **Phase 0 complete**: repo skeleton, docs (CLAUDE.md/README/PROGRESS/DECISIONS/BLOCKERS), `.gitignore`/`.editorconfig`, generated placeholder brand logo. Flutter app scaffolded (`app/`, upgraded to Flutter 3.47.0/Dart 3.13.0) with every spec-listed package installed and resolving cleanly — `flutter analyze` reports no issues. Laravel API scaffolded (`api/`, Laravel 13.25 — current major, spec assumed 12) with Sanctum, Filament (v5.7 — v3 is incompatible with Laravel 13's Symfony 8), Socialite, Intervention Image, and kreait/laravel-firebase installed — `php artisan test` passes. See DECISIONS.md for every version/conflict call made along the way.
- 2026-08-13 — **Phase 1 complete**: design foundation built — `core/theme/` (colors, typography via Plus Jakarta Sans, dimens, light+dark `ThemeData`), EN+SW ARB files wired through `flutter_localizations`/`intl`, and all twelve motion primitives implemented in `core/motion/` (page transitions, hero image, staggered reveal, shimmer skeletons, animated chips, badge bounce, bottom sheets, success checkmark, nav bar, pull-to-refresh, parallax carousel, splash transition) with a `/motion-gallery` debug route demonstrating each live. `flutter analyze` clean, widget test passes — the test caught a real bug (three primitives called `MediaQuery.disableAnimationsOf(context)` inside `initState()`, which Flutter forbids; fixed by moving the check to `didChangeDependencies()`). Visual verification on an Android emulator was skipped per instruction — final visual check deferred to the release APK at the end of the build.
- 2026-08-13 — **Phase 2 complete**: full API skeleton — 13 migrations (all spec tables + Sanctum's `personal_access_tokens`), Eloquent models with relationships/scopes/casts, `ReviewObserver`/`ReportObserver` for denormalised ratings and the 3-upheld-reports auto-hide rule, and every spec'd service interface (`SmsGateway`→log mock, `NidaVerifier`→manual-review mock, `PaymentGateway`→unimplemented stub, `SocialAuthVerifier`→real Google/Facebook/Apple server-side verification, `PushNotifier`→log mock, `DistanceQuery` for MySQL/SQLite-portable radius search). Seeded 10 categories, 20 sellers (14 verified/4 pending/2 rejected), 120 products with placeholder media, 70 orders (60 completed + 10 mixed status — see DECISIONS.md), 60 reviews. 37 REST endpoints covering auth (phone OTP + social), discovery, seller onboarding, products, favourites, orders/checkout, reviews, chat, reports, and devices, with rate limiting on OTP requests and writes. 45 feature tests pass (`php artisan test`), including a full-surface smoke test and targeted coverage of every money path (order-gated reviews, single-seller carts, seller-only status transitions, pending-seller product visibility). Manual smoke-testing against a real running server caught and fixed several bugs before formalizing tests as automated coverage — see DECISIONS.md for the full list (missing `AuthorizesRequests` trait, mass-assignment silently dropping status-transition fields, a `Stringable === string` always-false comparison, and stale cached-relation reads in authorization checks).
- 2026-08-13 — **Phase 3 complete**: app shell wired end to end — go_router `StatefulShellRoute` with the 5 bottom-nav tabs (Home/Search/Sell/Orders/Profile, Sell as the centre FAB) sharing a fade-through transition on switch while `IndexedStack` preserves each branch's state; dio client with an auth interceptor (attaches the Sanctum token, clears session + notifies on 401) and typed `ApiException` mapping; drift-backed offline cache (JSON-blob pattern, schema-agnostic); `flutter_secure_storage` for the token; Riverpod providers tying it together (`authStateProvider`, `isOnlineProvider`, etc.); a connectivity banner; TZS/phone formatters and form validators; and a real (not stubbed) phone-OTP sign-in bottom sheet against the Phase 2 API, pulled slightly ahead of Phase 6 so the Profile/Orders/Sell tabs' sign-in buttons actually do something. Also stood up the `data/api` (retrofit) + `data/models` (freezed) + `data/repositories` layers the spec's file tree calls for, starting with the Auth domain — the pattern Phase 4+ will reuse for products/orders/etc. `flutter analyze` clean, widget test passes. See DECISIONS.md for the go_router `navigatorContainerBuilder` workaround, the nav-item-vs-branch-index mapping bug caught before it shipped, and a retrofit_generator codegen bug worked around.
- 2026-08-13 — **Phase 4 complete**: discovery feed — Home tab with a sticky animated category chip row (`SliverPersistentHeader`), Near You/Trending/New Sellers sort, radius filter sheet (1/5/10/25km/All), location permission flow with a Dar es Salaam district-picker fallback (never blocks the app), List⇄Map toggle with real (not approximated) seller pin positions, staggered-reveal + shimmer-skeleton product grid, and cache-aware repositories so a network failure falls back to the last cached results. Search tab with 400ms-debounced search and persisted history (drift key/value). Extended the API's `SellerSummaryResource` with `lat`/`lng` after the first map implementation turned out to be showing pins at fabricated positions — a correctness fix, not a UI tweak; see DECISIONS.md. `flutter analyze` clean; 3 widget tests pass, including a new `ProductCard` test (per CLAUDE.md's testing rule) that caught a real overflow bug on first run.
- 2026-08-13 — **Phase 5 complete**: product detail (shared-axis-Z hero transition, mixed image/video media carousel with autoplay-while-active muted video, favourite/share/report actions, seller card, WhatsApp + message-seller buttons) and shop profile (products/gallery/reviews tabs, review distribution bar, share deep link). Backend gained a `seller_id` filter on `GET /products` (shop tabs need one seller's products) and `whatsapp` on `SellerSummaryResource` (the product page's WhatsApp button needs the seller's real number — the first draft used the seller's database id, caught immediately). Also caught and fixed a real routing bug: every `Navigator.pushNamed(...)` call written this phase would have failed at runtime, since the app uses go_router exclusively with no named-route table — replaced with `context.push()` throughout and registered the missing routes. `flutter analyze` clean; all widget tests and all 45 `php artisan test` cases pass.
- 2026-08-13 — **Phase 6 complete**: all four sign-in methods (phone OTP from Phase 3, plus real Google/Facebook/Apple native SDK flows in `social_sign_in_buttons.dart`, each ending in a server-verified `/auth/social` POST — tokens are never trusted client-side) and the full 4-step seller onboarding wizard (`features/seller/presentation/onboarding/`) with a progress bar, shared-axis-Z entry transition, and drift-backed draft resumption (each step PATCHes the server-side `SellerProfile` row created in step 1, so a killed app resumes at the right step): business details, a real Google-Maps pin picker with reverse geocoding, NIDA number + client-compressed ID photo, and licence upload (image or PDF, 5MB cap) via `file_picker`. Wizard completion shows the full-screen drawn-checkmark success moment, then routes into a real My Shop status view (pending/verified/rejected badge) — CLAUDE.md's "nav gains a My Shop tab once one exists," implemented as the Sell tab's content morphing rather than a 6th nav item. "Start selling" on Profile and Sell now both route into the real wizard. Google/Apple buttons gate on `SokoniSocialAuthConfig` and disable with a tooltip until their real credentials land (BLOCKERS.md item 3); Facebook's App ID is native-platform config, not Dart, so it has no such gate. Also fixed a real, previously-undetected local-dev-loop bug: `flutter test` on this Windows machine always compiles the host platform's plugin registrant even though the app ships Android/iOS-only, and the existing `win32: ^6.3.0` override (needed to reconcile `file_picker`/`share_plus`/`wakelock_plus`) silently broke `file_picker`'s actual Windows source, which is written against win32 5.x's API — `flutter analyze` never caught it since it doesn't fully compile platform-conditional code. Fixed by lowering the override to `win32: ^5.9.0`, letting pub resolve compatible `share_plus`/`wakelock_plus` versions automatically. `flutter analyze` clean, all widget tests pass.
- 2026-08-13 — **Phase 7 complete**: selling & orders, end to end. Backend gained real multi-size image processing (`ImageVariants` + Intervention Image v4 — thumb 300/card 800/full 1600, a new `card_path` column added since the original schema only had two of the three sizes CLAUDE.md calls for), a video-plus-client-thumbnail upload path (`ProductMediaController`), an 8-item-per-product cap, `GET /shop/products` for the seller's own (including hidden/pending) listing, and `is_active`/`is_hidden` on `ProductResource` — 5 new feature tests, 50/50 passing. Client gained: product create/edit (`product_form_screen.dart`) with in-place media management (camera/gallery photo, gallery video — both compressed client-side, video gets a `video_compress`-extracted poster frame); a My Shop dashboard (list, add, edit, delete) wired into the Sell tab under its existing status badge; a single-seller cart (`CartController`) with a bounce-badged cart icon on the home app bar; checkout (delivery/pickup, address with a "use current location" action, cash-on-delivery/pay-on-pickup, running total) ending in the full-screen success-checkmark motion primitive and a straight hop into the order's auto-opened chat thread; and buyer + seller order lists/detail with the real per-order timeline and status-advance/cancel actions, gated by comparing the signed-in user against `order.buyer.id` (no seller-profile-id field needed — the backend policy already guarantees only the two right parties can ever see an order). Also built the real client-side FCM registration flow (`core/push/push_service.dart`), wired to fire on every sign-in — it fails soft (logs and moves on) with no Firebase project configured yet (BLOCKERS.md item 1), which is also where a stale claim from an earlier session (that this was already wired) got corrected. Caught and fixed a real bug before it shipped: `ref.watch(cartProvider.notifier).xxx` doesn't rebuild on cart changes — Riverpod's `.notifier` modifier subscribes to the controller instance, not its state — see DECISIONS.md. `flutter analyze` clean, all widget tests and all 50 `php artisan test` cases pass.
- 2026-08-13 — **Phase 8 complete**: chat & reviews. Backend gained a real (not simulated) typing indicator over the existing 5s-polling transport — per-party `typing_until` timestamps on `conversations`, refreshed via a new `PATCH /conversations/{id}/typing` and read back on the same poll — plus fixed chat image attachments to store full URLs like every other media field already does (`MessageController` previously saved bare disk-relative paths the client couldn't load), and added `seller_handle` to `UserResource` so the shop review tab can tell its own owner apart from a browsing stranger. 7 chat feature tests, 52/52 passing overall. Client gained a full conversation list + thread UI (`features/chat/`) replacing the Phase 3 placeholder: 5s polling for both messages and typing state, read receipts (double-check icon once the other party's poll marks a message read), image attachments via `image_picker`, and the pinned per-product context card CLAUDE.md calls for — plus order-gated review submission (a star-rating bottom sheet on the order detail screen, shown only once an order is completed and unreviewed) and seller reply UI on the shop profile's review tab. First hand-written Riverpod family `AsyncNotifier` in the codebase (`ChatThreadController`, one polling timer per open conversation) — see DECISIONS.md for the constructor-carries-the-arg shape that pattern needs in this API. `flutter analyze` clean, all widget tests and all 52 `php artisan test` cases pass.
- 2026-08-13 — **Phase 9 complete**: trust & admin. The Filament panel itself had never actually been installed despite `FilamentUser` scaffolding since Phase 2 — ran `filament:install --panels`, branded it (Sokoni yellow, "Sokoni Admin"), and built four resources: the seller verification queue (`SellerProfileResource` — NIDA photo, licence file link, and a Google Maps link shown side by side, verify/reject actions using `forceFill()` for the same reason every other protected status transition in this codebase does), the moderation queue (`ReportResource` — uphold/dismiss plus direct hide/warn/suspend/ban actions resolved to the right underlying user via a new `Report::offendingUser()`), user management (`UserResource` — warn/suspend/ban/unban, admin account creation), and category management (`CategoryResource`), plus a dashboard stats widget. "Suspend" needed two new `users` columns (`banned_until`, `ban_reason`) since the spec's schema only had a permanent-ban timestamp with nowhere to persist a reason or an expiry — banning/suspending now also revokes the user's existing Sanctum tokens, not just blocks future logins. Caught and fixed a real bug via the new panel tests: `canAccessPanel()` threw a `TypeError` instead of returning `false` for any user whose `is_admin` read as `null` in memory (true of nearly every seeded user) — every non-admin panel visit was one crash away from a 500. 12 new backend tests (panel access + reports), 66/66 passing. Client gained message-level reporting (long-press in the chat thread, joining the existing product/shop report buttons) and real Terms of Service / Privacy Policy screens — genuinely written for Sokoni's actual features in EN/SW, versioned, with a one-time acceptance gate shown right after every sign-in. `flutter analyze` clean, all widget tests pass.
- 2026-08-14 — **Phase 10 complete — the app is done.** Audited every screen for CLAUDE.md's "no blank screens, every error offers retry" rule and fixed the real gaps found (bare error text with no retry on the shop dashboard, onboarding wizard, and two category dropdowns; infinite spinners with no error branch on Profile/Sell; an ad hoc empty-map widget swapped for the shared component; the "showing saved results" banner extended to Search, Shop Profile, and Home's map view — all of which fall back to cached data on a network failure but weren't saying so). Replaced four raw `Image.network` calls with `CachedNetworkImage` (the only screens still bypassing the disk-backed cache) and capped the global in-memory image cache (60MB/200 entries) against unbounded growth while browsing a long feed — CLAUDE.md's "mid-range Android on patchy 3G/4G" target taken literally. Independent full EN/SW translation review (all 210 keys 1:1, zero placeholder mismatches) found and fixed six genuine issues: a real mistranslation ("gallery" → "ghala", i.e. warehouse), two terminology-consistency slips, one ambiguous field label, and two naturalness/capitalization nits. Generated real app icon/splash from the brand logo (`flutter_launcher_icons`/`flutter_native_splash`) and a real release keystore, then spent most of this phase chasing three more win32-major-version conflicts through `flutter build apk` (`package_info_plus`, `flutter_secure_storage_windows`, both pinned to older win32-5.x-compatible lines) plus a JVM-target mismatch in an outdated plugin template (`flutter_facebook_auth`, fixed with a root-level Gradle block forcing JVM 17 across every subproject) — all local-dev-machine-only issues, irrelevant to the shipped Android/iOS build, but blocking the ability to produce one on this Windows machine at all. Wired `config/filesystems.php`'s `public` disk to the `PUBLIC_UPLOADS_ROOT`/`PUBLIC_UPLOADS_URL` env vars the deploy runbook needs (the vars existed in `.env.example` since an earlier phase but were never actually read anywhere) and found a second stale BLOCKERS.md claim — no Google Maps API key placeholder actually existed in `AndroidManifest.xml`/`AppDelegate.swift` despite BLOCKERS.md saying so — added both for real. Wrote `docs/API.md` (full endpoint reference), `docs/DEPLOY.md` (the cPanel runbook, updated with the real filesystem/build-flag specifics), and `docs/DEMO.md` (a 12-step, two-device client demo script). Final state: `flutter analyze` clean, all Flutter widget tests pass, all 66 `php artisan test` cases pass, `flutter build apk --analyze-size` shows a reasonable ~22MB arm64 payload with no runaway dependency, and both `app-release.apk` and `app-release.aab` are built, signed with a real (not debug) keystore, and verified via `apksigner`.
- 2026-08-14 — **Post-launch: real brand logo dropped in.** The client's real logo replaced the placeholder at both `assets/brand/sokoni_logo.png` paths (arrived as `.jpeg`, losslessly converted to PNG). Regenerated app icons and splash screens from it. Along the way, found the full logo lockup (bag+S glyph plus "Sokoni" wordmark) doesn't work directly as a launcher icon — the wordmark turns illegible and gets clipped by the adaptive-icon mask — so cropped a second icon-only asset (`sokoni_logo_icon.png`) and pointed `flutter_launcher_icons` at that while `flutter_native_splash` keeps the full lockup. Also swapped `SokoniSplashTransition` and the pull-to-refresh `SokoniBagSpinner`, both previously drawn with a generic `Icons.shopping_bag_rounded` stand-in from before the real logo existed, over to the real cropped mark. See DECISIONS.md for the transparent-cutout attempt that didn't work out. `flutter analyze` clean, all widget tests pass.
- 2026-08-15 — **Post-launch: fixed a release-build startup hang, tracked it to Android 16, then to Facebook.** The production API went live, but the signed release APK hung on the splash screen with no error and never reached the feed. First pass added bounded timeouts across the whole startup path (Firebase init, location, secure storage, the first API call) and a 5-second watchdog with a real error+retry screen — all real, correct fixes, but the hang persisted on the actual test device (an Android 16 Infinix Note Edge), because the Dart watchdog never even fired: the freeze was before Flutter's engine started, before any Dart code ran. Built a `diagnostic` Gradle flavor with a bare-minimum `FlutterEngine`/entrypoint that registers zero plugins, installable alongside the real app — it booted fine on the same device, isolating the cause to native plugin registration. Read the actual plugin source of every suspect rather than guessing from version numbers: `flutter_facebook_auth`'s Android plugin constructs a `FacebookAuth()` (initialising the native Facebook SDK) unconditionally at plugin-registration time, on every launch, with no Facebook App ID ever configured — a well-documented Facebook SDK hang scenario, worse on a security-locked device. Removed Facebook sign-in entirely, client and server, rather than work around a bug in an unconfigured feature: `pubspec.yaml`, the sign-in button/handler, `SokoniSocialAuthConfig`, `HttpSocialAuthVerifier`'s Facebook branch, `services.facebook` config, `.env`/`.env.example`, and the Terms/Privacy copy that mentioned it. Confirmed empirically (not assumed) that the JVM-17 Gradle workaround from Phase 10 was needed only because of that one package, and removed it too. Google, Apple, and phone OTP untouched. `flutter analyze` clean, all Flutter widget tests pass, all 73 `php artisan test` cases pass, and both a `prod` and a `diagnostic` release APK build and verify against the real release keystore.
- 2026-08-15 — **Post-launch: startup hang wasn't Facebook after all — bisected plugin registration on the real device, cleared all 22 plugins, traced the real culprit to the app's own pre-first-frame startup path.** Rebuilt `DiagnosticMainActivity` to register every plugin one at a time (in a fixed, most-likely-suspects-last order) instead of registering none, writing each plugin's name to both an on-screen label and a `Downloads/sokoni-boot.txt` log file (via `MediaStore`, no adb/storage-permission needed) immediately before each attempt — with a short scheduled delay first, since `TextView.setText()` only schedules a repaint rather than painting synchronously, and an immediate hang right after setting the label risked freezing on the *previous* step's stale text instead of naming the real one. On the real device: all 22 plugins registered clean, reaching "Sokoni starting." Traced every line from `main()` to the first frame by hand and confirmed there's no `redirect:` callback anywhere in `app_router.dart` gating route resolution, and nothing in `SplashScreen.initState()` is awaited — the hang has to be before or during that chain, which structurally rules out the per-widget watchdog ever being able to catch it. Found and fixed a real, concrete bug that matches the "3G only, never WiFi" clue exactly, by reading `google_fonts`' own source rather than guessing: its runtime font-fetch HTTP call has no timeout at all, and this app was never bundling the font locally (no real Plus Jakarta Sans files were available to bundle) — not confirmed as *the* cause since the call is fire-and-forget and shouldn't block first paint on its own, but bounded it to 8s via a wrapped `http.Client` regardless, since CLAUDE.md explicitly targets patchy 3G/4G. Rearchitected the watchdog to arm globally in `main()`, before `runApp()`, independent of whether any widget ever builds — cancelled via `WidgetsBinding.instance.addPostFrameCallback` on the first real frame, whatever route it turns out to be, with a minimal standalone fallback screen if it fires. Added `BootLog`, a silently-fails-soft `MethodChannel` call instrumenting `main()` and `SplashScreen`'s key phases, and wired `DiagnosticMainActivity` to receive and log those calls too — then rebuilt the diagnostic APK targeting the *real* `lib/main.dart` (not the placeholder `main_diagnostic.dart`) so the next test run continues logging phase-by-phase into the app's actual startup path instead of stopping at a bare "Sokoni starting" screen. `flutter analyze` clean, all widget tests pass, both `prod` and `diagnostic` release APKs rebuilt and verified against the real release keystore. Root cause still not conclusively identified — the next real-device run is what should reveal it.
- 2026-08-15 — **Post-launch: that device run came back completely dark past plugin registration — zero Dart-side log lines, so fixed the blindness itself before chasing the bug further.** All 22 plugins registered clean again, then nothing — meaning either Dart's `main()` never runs at all, or `BootLog` fails silently, and there was no way to tell which. Made `DiagnosticMainActivity` prove the channel rather than assume it: logs its own handler attachment, logs the raw method+arguments of every message it ever receives (not just well-formed ones), and logs the exact `DartEntrypoint` (bundle path, library, function name) immediately before *and* after calling `executeDartEntrypoint()` — that call is non-blocking, so the "returned" line alone rules out a hang in the native call itself. On the Dart side, wrapped the entirety of `main()` in `runZonedGuarded` and wired `FlutterError.onError`/`PlatformDispatcher.instance.onError` through `BootLog` — a silent black screen with no log output is the textbook signature of an uncaught exception before first paint, and this exists to name it. Made the true first working statement (after the binding init a `MethodChannel` ping structurally requires to have any chance of sending anything at all) a `BootLog` ping, so "did Dart even start" now has a real answer either way. Also removed `google_fonts` entirely per instruction, not just timeout-bounded it as the previous pass did — `SokoniTypography` now builds its `TextTheme` from Flutter's own `Typography.material2021()` with `fontFamily: 'PlusJakartaSans'` applied, no network involved at all. Could not source the real Plus Jakarta Sans `.ttf` files anywhere on this machine (no network access, nothing cached) to actually bundle — fabricating a substitute felt worse than leaving it wired-but-unpopulated, so it currently falls back to the platform default font safely; the `fonts:` block is ready and commented in `pubspec.yaml` for whenever the real files are sourced. `flutter analyze` clean, all widget tests pass, both `prod` and `diagnostic` release APKs rebuilt and verified against the real release keystore. Root cause still not identified — this run exists specifically to stop guessing about it.
- 2026-08-15 — **Post-launch: found it. The log finally showed a real, specific failure — "SplashScreen watchdog fired — animation never completed" — and it led to a genuinely oversized brand asset, not a hidden startup dependency.** Traced `SokoniSplashTransition` end to end: no Future, no image load, nothing awaited anywhere on the path to its `onComplete` callback — the only way it can fail to fire is the `AnimationController`'s own ticker never reaching `completed`. Checked the actual asset behind its `Image.asset` call and found `sokoni_logo_icon.png` had become a 6250×6250 placeholder (~149MB decoded as raw RGBA) for a widget rendered at 96×96, decoded fresh on every single animation tick with no `cacheWidth`/`cacheHeight` cap — exactly the kind of main-thread/memory pressure that could stall a ticker on a real mid-range device. Caught a bug in my own fix along the way: the placeholder's background is fully transparent, not white as it appears in an image viewer, so a naive brightness-based crop treated the whole image as one block — fixed by detecting content via the alpha channel instead, same as the crop technique from earlier this session. Resized the full lockup to 1080×1080 and re-cropped an icon-only glyph onto a white canvas this time (this placeholder's glyph is black-on-transparent, not white-on-black like the earlier real logo, so the previous black-canvas convention would have made it invisible) — regenerated launcher icons and splash from the corrected assets, and added `cacheWidth`/`cacheHeight` to both `Image.asset` call sites that reference this file so an oversized source asset can't silently balloon decode cost like this again. Also rearchitected `SplashScreen` on principle, not just to patch this one bug: navigation to home now runs off a plain timer independent of the animation's own completion signal — an animation must never gate routing — and the 5-second watchdog no longer shows an error screen, it forces the same navigation instead, since a user should never be stranded on a splash screen with nowhere to go. `flutter analyze` clean, all widget tests pass, both `prod` and `diagnostic` release APKs rebuilt and verified against the real release keystore.
- 2026-08-15 — **PART 1 complete — contrast/readability audit.** Traced the reported location-dialog bug to its actual mechanism rather than patching the symptom: `SokoniTypography` built its base `TextTheme` from `Typography.material2021().black` (a fixed light-theme variant) for *both* themes, so any TextTheme role not explicitly overridden with a theme-aware colour — including `headlineSmall`, which is exactly what `AlertDialog`'s Material 3 default title style reads — stayed dark-on-dark in dark mode regardless of the rest of the theme being correct. Fixed at the root (pick `.white`/`.black` based on actual brightness) and belt-and-suspenders at `dialogTheme` (explicit `titleTextStyle`/`contentTextStyle`, bypassing Material's default-resolution chain entirely). Fixed the bottom nav bar's selected-tab colour, which was separately hardcoded black — redesigned as a yellow pill behind a black icon plus theme-`onSurface` label text, since raw yellow text/icons fail WCAG contrast badly on either surface (~1.6:1), which the brief called out explicitly. Delegated a full-codebase sweep for the same two bug classes (hardcoded colours ignoring brightness; yellow-as-text) to a research agent and fixed everything it found: product cards, product/shop detail screens, seller onboarding, the radius filter sheet, order status chips, the seller "pending" status label, empty states, the chat message bubble, and several lower-severity cosmetic-only spots. Built `/theme-audit`, a debug route showing every themed component in both light and dark theme side by side regardless of the device's actual system setting. Re-checked both outstanding assets (the placeholder logo — still a placeholder but correctly sized now, not the 6250×6250 that caused the earlier startup hang; Plus Jakarta Sans — still no font files available) and logged them in `BLOCKERS.md`. `flutter analyze` clean, all Flutter widget tests pass, all 73 `php artisan test` cases pass.
- 2026-08-15 — **PART 2 complete — sign-up clarity and account intent.** Reworked the phone OTP sheet: header now reads "Sign in or create an account" in both languages, and once a code is sent to a never-seen number the OTP step plainly states a new account is about to be created (`requestOtp` now returns whether the number is new, threaded through from a new `is_new_account` field on `POST /auth/otp/request`). Added a one-question intent screen (`/account-intent`, `AccountIntentScreen`) shown exactly once, right after a brand-new account's first sign-in across every sign-in method (phone OTP, Google, Apple) via one shared `completeSignIn()` helper — "buy" and "decide later" land on the feed, "sell" goes straight into the existing seller onboarding wizard, and the choice is persisted server-side (`users.account_intent`, `POST /auth/intent`) so it can never reappear; deliberately keyed off the sign-in response's transient `is_new_account` flag rather than `account_intent IS NULL`, since the latter would incorrectly re-trigger for every pre-existing account that signed up before this field existed. One account model throughout, exactly as instructed — no new user types, no split apps. Implemented `BeemSmsGateway` for real OTP delivery against Beem Africa's send-SMS API, bound automatically in `AppServiceProvider` only when both `BEEM_SMS_API_KEY`/`BEEM_SMS_SECRET_KEY` are set, falling back to the existing `LogSmsGateway` otherwise (local dev/CI unaffected); documented the whole integration, what a production Beem account needs, and how it's tested without sending real SMS in `docs/SMS.md`. `flutter analyze` clean, all Flutter widget tests pass, all 85 `php artisan test` cases pass (5 new Beem/SmsGateway-binding unit tests, 7 new phone-OTP/intent feature tests).
- 2026-08-15 — **PART 3 complete — the social business layer.** Added Updates/Offers/Showcases/Customers as three concrete tables plus a real follow model, every one with a non-nullable `seller_id` FK to `seller_profiles` (Offers/Showcases also a non-nullable `product_id`) — a schema guarantee against standalone personal content, not just request validation, proved directly in tests (`test_the_database_rejects_an_update/offer/showcase_with_no_seller/product`, expecting a raw `QueryException`). Backend: `Update`/`Offer`/`Showcase`/`Customer` models with `scopeVisible()`/`scopeFollowedBy()`, `CustomerObserver` denormalising `seller_profiles.customer_count` the same way `ReviewObserver` already does `rating_avg`/`rating_count`, ownership Policies mirroring `ProductPolicy`, FormRequests enforcing "must be one of the acting seller's own Listings" via `withValidator`, full CRUD controllers + routes, `PushNotifier` wired to notify a shop's followers on a new Update/Offer, and an `updates:delete-expired` scheduled command (the first `Schedule::` entry in this codebase) hard-deleting Updates past their 24h expiry along with their media files. 31 new feature/unit tests, 113/113 `php artisan test` passing — caught and fixed a real bug along the way (`OfferController` was persisting an unstringified `Stringable` into `discount_type`, which crashed `discountedPrice()`'s `match` on every single offer creation; the test suite caught it before it shipped). Frontend: full data/API/repository/provider layer for all four resources, a new `StoryRing` motion primitive (yellow ring, unseen state, same single-`AnimationController`-fires-on-flip skeleton as every other primitive) with a "last seen per shop" mark stored locally via `AppDatabase`'s existing key/value cache (not synced server-side — see DECISIONS.md), an Instagram-style Updates tray + full-screen story viewer, an Offers row with live countdown + strikethrough pricing, a Showcase preview row leading into a full-screen vertical video feed (video-controller lifecycle reused from `ProductMediaCarousel`, not `visibility_detector`), a centre-FAB create sheet (Listing/Update/Offer/Showcase for a seller, "Start selling" for a buyer) with three new composer screens reusing the existing image/video compression pipeline, shop profile gained Showcase/Updates tabs plus a customer count and a real follow button, and a shared "Following" filter. Renamed "Products" to "Listings" in the shop profile tab and every new Part 3 surface per the required terminology, and "Favourites" to "Saved" — did **not** sweep every pre-existing "Product"-labelled string app-wide (product form titles, cart/checkout, search hints, etc.); see DECISIONS.md for the exact boundary. `flutter analyze` clean, all Flutter widget tests pass (1 new `StoryRing` test), all 113 `php artisan test` cases pass.
- 2026-08-16 — **Device-testing round, PARTS 1–2 complete — the account-creation blocker, and the intent screen it was silently eating.** `TermsAcceptanceScreen` wraps itself in `PopScope(canPop: false)` (correctly — it should never be back-swipeable while the request is in flight), but on a *successful* acceptance it called `Navigator.of(context).maybePop()`, and `maybePop()` explicitly honours `PopScope.canPop` the same way it honours a back gesture — confirmed against the Flutter SDK's own `NavigatorState` source (`popDisposition`/`maybePop` in `navigator.dart`), not guessed. So the accept call succeeded and persisted server-side every time, but the pop was silently refused: spinner, then back to the same screen, forever — exactly the reported symptom, and a full account-creation blocker since `ensureTermsAccepted()` awaits that route's future before anything past it (including PART 2's intent screen) can run. This also fully explains PART 2's "missing" intent screen without it being a second bug: a tester who force-closed and reopened the app after getting stuck would land straight on the home feed on the next launch (only the fresh sign-in path enforces the terms/intent gate — re-opening the app just checks for a stored token), which matches "phone → code → full name → terms → home" precisely. Verified the route, middleware, and `AcceptTermsRequest` validation are all correct by probing the live production API directly (`https://api.sokoni.co.tz/api/auth/terms/accept` returns a clean `401 {"message":"Unauthenticated."}` for the exact headers the app sends) — ruling out the auth-before-token and missing-column hypotheses before touching any code, per instruction. Fixed with `context.pop()` (go_router's own idiom, matching the rest of the codebase, and confirmed to bypass `PopScope.canPop` the same documented way `Navigator.pop()` does) and broadened the failure handling to catch `Object`, not just the typed `ApiException`, so an unexpected response shape can never fail silently either — every failure now sets `_error` and re-enables the button, a real retry. Confirmed PART 2's intent screen (`/account-intent`, `AccountIntentScreen`), the reworked phone-sign-in header ("Sign in or create an account"), and the new-account OTP notice were all still intact from the prior session's work and needed no changes beyond this fix reaching them. New coverage: `api/tests/Feature/Auth/TermsAcceptanceTest.php` (success, unauthenticated, missing version, re-acceptance) and `app/test/features/legal/terms_acceptance_screen_test.dart`, which pushes the real screen under a real `GoRouter` (a bare `Navigator.push` doesn't reproduce this class of bug, since `context.pop()` needs a `GoRouter` ancestor to fail the same way) and asserts it actually closes on success and stays open with a visible, re-triable error on failure. **A separate, serious finding surfaced while probing production and logged to BLOCKERS.md, not fixed here since it's an infra/env setting, not app code**: `api.sokoni.co.tz` is currently running with `APP_DEBUG=true` — a request with an unexpected header combination returned a full stack trace including server file paths. This needs to become `false` in the production `.env` regardless of this session's fixes. `flutter analyze` clean, all Flutter widget tests pass (2 new), all 117 `php artisan test` cases pass (4 new).
- 2026-08-16 — **Device-testing round, PART 3 complete — a real "For You" feed replaces the plain product grid.** Home now has two tabs: "For You" (default, single-column social feed) and "Shops" (the existing Near You/Trending/New Sellers grid, unchanged in substance but relocated under its own tab with the map⇄list toggle, radius filter and cart icon moved into a local toolbar there — none of the three mean anything on For You, and the spec's "keep the top bar uncluttered" ruled out leaving them global). Backend: a real server-side `FeedService` composes the feed per CLAUDE.md's exact ordering ("followed shops first, then nearby verified sellers, then Offers running now, then Showcase videos inline every 6-8 items") — followed sellers' Listings, then everything else (nearest-first when the buyer's location is known, newest-first otherwise, matching the existing discovery-grid fallback), sponsored Listings pulled to the front of that second tier but never ahead of a followed shop, then running Offers, with one Showcase woven in after every 6 items — paginated 10 per page via `GET /feed`, public like every other discovery endpoint but personalised when a token is present. New `comments` table (one level of replies, enforced in `CommentStoreRequest` not the schema) with full CRUD, wired into the existing polymorphic report system (`comment` joins `product`/`shop`/`message` in `StoreReportRequest::REPORTABLE_TYPES`, `Report::offendingUser()` gained a Comment branch) so "report a comment" reaches the same moderation queue for free. `products` gained `is_sponsored`/`sponsored_until`/`sponsor_contact_method` (seller-toggleable via a new `PATCH /products/{id}/boost`, no payment flow yet per instruction) and a computed `comments_count`. Found two real, separate bugs while building this: (1) a Laravel gotcha that would have silently broken every `AppNotification`-shaped response — a `JsonResource` whose own `toArray()` output already contains a top-level `data` key defeats Laravel's automatic response-wrapping entirely (traced to `ResourceResponse::haveDefaultWrapperAndDataIsUnwrapped`'s `array_key_exists('data', $data)` check), so the notification payload field is exposed as `payload`, not `data`, in the API (the DB column keeps its name) — documented in the resource itself since this exact trap will recur the moment any future resource happens to have its own field called `data`; (2) the same `Fillable`-vs-`update()` silent-no-op class of bug this codebase has hit repeatedly (Phase 2, Phase 7) recurred in `NotificationController::markRead` (`read_at` deliberately isn't Fillable) — fixed with `forceFill()`, caught by a feature test before it shipped, not in production. Also added a `seller_profiles.logo` column (Instagram-style headers on both the feed card and Part 4's shop profile need a shop avatar, which never existed — only `users.avatar`, the signed-in person's own photo) with a real upload endpoint (`PATCH /sellers/{id}/logo`) and, separately, a real persisted in-app notification inbox (`app_notifications` table, `GET/PATCH /notifications`) — CLAUDE.md Part 3 names a Notifications top-bar icon, and this app had no in-app notification record at all (only ephemeral, currently-mocked FCM pushes), so `LogPushNotifier` now also writes a durable row on every call, and the icon opens a real list rather than a dead button. Frontend: `FeedCard` dispatches on item type (product/offer/showcase) inside one shared shell (seller header with a real Follow button — `SellerSummary` gained `is_following`/`logo` to support it — Save/Comment/Share/Chat action row, 4:5 media, price line, `@handle`-prefixed caption, comment count, Order button), muted-autoplay-above-60%-visibility video via `visibility_detector` (the one place in this app a scrolling, not paged, list actually needs it — every other video surface has each item fully on/off screen already), a comment bottom sheet with one level of replies and a seller badge, and a seller-side boost dialog (duration + contact-method chips) on the My Shop dashboard. `isSellerFollowed`/`toggleSellerFollow` were generalised from taking a `SellerProfile` to taking `{sellerId, handle, isFollowing}` directly so the same optimistic-follow mechanism covers both the shop profile's button and the feed card's. **Scoping calls made explicitly, not silently**: the Order button on every card variant opens the product detail screen (the app's one real, fully-built checkout entry point) rather than duplicating checkout inline in the feed; Offers get their own dedicated card (reusing the Offers-row card layout) rather than *also* overlaying strikethrough pricing onto a product's own card when both happen to reference the same Listing, since one clear treatment beats a duplicated, harder-to-reason-about one. `flutter analyze` clean, all Flutter widget tests pass (2 new: a `TermsAcceptanceScreen` regression pair from PART 1, a `FeedCard` render test), all 144 `php artisan test` cases pass (26 new — Feed composition/sponsorship/boost-authorization, Comment CRUD/one-level-reply enforcement/moderation, seller logo upload, and the notification inbox).
- 2026-08-16 — **Device-testing round, PART 4 complete — the shop profile, Instagram-style.** Rebuilt around the pattern CLAUDE.md asked for: circular shop avatar (the new `seller_profiles.logo` from PART 3) with a small yellow verified tick, three counts (Listings/Customers/Following — `products_count` and `following_count` added to `SellerProfileResource`, `customer_count` already existed), bio block (category, description, location line, tappable WhatsApp row), Edit profile + Share for the owner or Follow + Message for everyone else, and an owner-only 30-day dashboard strip. Tab row switched from five text-labelled tabs to four icon tabs (grid/play/person/star for Listings/Showcase/Updates/Reviews) — dropped the old generic "Media" tab entirely (`ShopMediaGrid`, deleted) since Part 3 already gave Updates and Showcases their own dedicated homes, making an all-media-mixed grid redundant; kept Reviews as a fourth tab despite Part 4's own list naming only three, since dropping ratings/reviews would cut a CLAUDE.md core requirement (feature 2's distribution bar) that Part 4 never asked to remove. The Listings tab itself became a real 3-column grid (`ShopListingsGrid`, one tile per Listing, play badge on video covers, stacked-squares badge on carousels) replacing the old 2-column `ProductCard` grid, and the Showcase tab's tiles gained real view counts. **Owner dashboard strip is honestly scoped, not a placeholder pretending to be one**: `SellerDashboardController` reports saves and orders genuinely scoped to the last 30 days (both have real timestamped rows to query), but *total* views, not a 30-day figure — `products.views` is a single lifetime-incrementing counter with no per-event log behind it, and fabricating a 30-day-looking number from a lifetime counter would be exactly the "placeholder pretending to be data" CLAUDE.md rules out. Built the "Edit profile" flow that turned out not to exist anywhere in the app at all — no screen, no repository method, nothing — for bio/WhatsApp-visibility/logo (shop name, handle and category stay onboarding-only, matching every other identity field in this app). `flutter analyze` clean, all Flutter widget tests pass, all 147 `php artisan test` cases pass (6 new — dashboard scoping/authorization, logo upload/authorization).
- 2026-08-16 — **Device-testing round, PART 5 complete — visual polish.** The real Plus Jakarta Sans `.ttf` files (four weights) had landed in `app/assets/fonts/` since the last check — wired up `pubspec.yaml`'s previously-commented `fonts:` block (no code changes needed, `typography.dart` already referenced the family name from an earlier session); this was the single highest-impact item in this part per instruction, and BLOCKERS.md's font entry is now resolved. Found and fixed the reported bottom-nav bug at its actual mechanism, not by guessing at a different icon: `Icons.search_outlined`'s bundled glyph in Flutter's Material Icons font has no real thin-stroke artwork distinct from its filled counterpart (a known limitation specific to the search icon — home/receipt/person all have genuine outlined art) — swapped the inactive state to the classic `Icons.search`, a guaranteed normal-weight glyph, while every other nav icon's outlined/filled pairing was confirmed already correct. `ProductCard` lightened per the brief: image gets 12dp top-only corners (the card's own border now follows that same silhouette instead of independently fully-rounding), price promoted to the strongest element (moved above the title, bumped to `titleSmall`/700 weight), and rating+distance collapsed from two separately-styled elements into one muted single-line caption (`"4.5 · 2.3 km away"`) — updated the existing widget test to match. Added a reusable `BottomGradientScrim` and used it everywhere an icon/badge/count sits directly on top of image or video content without a separate text block underneath (the new `FeedVisibilityVideo`'s mute icon, the Listings grid's video-play badge, the Showcase grid's play+view-count) — CLAUDE.md: "so overlaid text is always legible." Centralised haptic feedback into `toggleProductFavorite`/`toggleSellerFollow` themselves rather than at each call site (Save and Follow now fire consistently everywhere either function is called, including call sites from earlier phases this part never otherwise touched) and added it explicitly to the two genuinely separate "Order" actions (a feed card's Order button, checkout's Place Order). Audited tap targets in every widget built this device-testing round and found one real violation — the feed card's Follow button had an explicit `minimumSize: Size(44, 32)`, shrinking it *below* the theme's own global 44dp button-height default (`app_theme.dart` already enforces `minTapTarget` on every Filled/Outlined/TextButton app-wide) — fixed to match the theme baseline rather than override it downward. Audited the 4pt spacing scale for arbitrary literals: found exactly four, all pre-existing, all inside motion-primitive internals or bootstrap error UI with a real physics/one-off reason for not using a named constant — left alone rather than churn motion-primitive code for its own sake. `flutter analyze` clean, all Flutter widget tests pass (1 updated for the new caption format), all 147 `php artisan test` cases pass (backend untouched this part, run anyway per instruction).
- 2026-08-16 — **Device-testing round complete — release deliverables.** Built `app-prod-release.apk` (71.5MB) and `app-diagnostic-release.apk` (71.3MB), both `--flavor` release builds, both confirmed signed with the real Sokoni release certificate via `apksigner verify --print-certs` (`CN=Sokoni, SHA-1: 06e890e4bff2a859386ae2a641c1939db6f5f730` on both — not the debug fallback, and matching each other's fingerprint) — in `app/build/app/outputs/flutter-apk/`. Sizes are up from Phase 10's ~24.7MB analyze-size figure mainly because that figure was a single-ABI (`--target-platform android-arm64`) size-analysis run, not the actual shipped multi-ABI universal APK this build (and every prior real release build this session) produces — not a regression, an apples-to-oranges comparison. Built `sokoni-api-update-2.zip` (28KB, 31 files) containing exactly the backend files that changed since the previous deploy zip (`sokoni-api-update.zip`, built at the end of the prior session's PART 4) — computed by diffing the current working tree against that zip's own extracted contents file-by-file (not just against the last git commit, since nothing has been committed since Phase 10 and a commit-based diff would have re-included everything already shipped in that first zip), rather than assumed. Test files excluded again, same reasoning and precedent as the first zip. 4 new migrations to run on the server, in order: `2026_08_16_135854_add_sponsorship_to_products_table`, `2026_08_16_135856_create_comments_table`, `2026_08_16_142024_add_logo_to_seller_profiles_table`, `2026_08_16_143136_create_app_notifications_table`.
- 2026-08-16 — **PART 4 complete — polish & ship.** EN/SW review of everything added in Parts 2-3: 302/302 keys 1:1 between `app_en.arb`/`app_sw.arb`, zero placeholder mismatches, and the required terminology (Bidhaa/Taarifa/Punguzo/Onyesho/Mteja/Hifadhi) confirmed used consistently across every new string. `flutter build apk --analyze-size --target-platform android-arm64` shows a reasonable ~24.7MB payload, no runaway dependency; `prefer_const_constructors` is already an enforced lint (`flutter analyze` clean covers it) and the global 60MB/200-entry image cache cap from Phase 10 automatically covers every new `CachedNetworkImage` use, no changes needed. Built both `app-prod-release.apk` and `app-diagnostic-release.apk` (multi-ABI, `--flavor prod`/`--flavor diagnostic --release`), confirmed signed with the real release keystore (not the debug fallback) via `android/key.properties`'s presence and `build.gradle.kts`'s own signing-config selection logic. Updated `docs/API.md` with every Part 2/3 endpoint (`/auth/intent`, Updates/Offers/Showcases/Customers, the updated User/SellerProfile object shapes) and refreshed the SMS/push "what's stubbed" notes. Backend prepped for deployment: confirmed `fakerphp/faker` already sits in `require` (not `require-dev`) from an earlier phase, ran `composer install --no-dev --optimize-autoloader`, and built `sokoni-api-update.zip` at the repo root from exactly the 51 backend source files that differ from the last commit (`c6de251`, Phase 10) — everything Parts 2-3 touched, since the "post-launch" fixes between then and now were Flutter-only. Test files were deliberately left out of the zip (dev-only, no functional role in production) but are listed in DECISIONS.md for the record. 6 new migrations to run on the server, in order: `2026_08_15_140000_add_account_intent_to_users_table`, `2026_08_15_150000_create_customers_table`, `2026_08_15_150100_add_customer_count_to_seller_profiles_table`, `2026_08_15_150200_create_updates_table`, `2026_08_15_150300_create_offers_table`, `2026_08_15_150400_create_showcases_table`.
