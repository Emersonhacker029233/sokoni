# Progress Log

One line per completed phase, appended in order. See `DECISIONS.md` for judgement calls and `BLOCKERS.md` for anything needing external input.

- 2026-08-13 — **Phase 0 complete**: repo skeleton, docs (CLAUDE.md/README/PROGRESS/DECISIONS/BLOCKERS), `.gitignore`/`.editorconfig`, generated placeholder brand logo. Flutter app scaffolded (`app/`, upgraded to Flutter 3.47.0/Dart 3.13.0) with every spec-listed package installed and resolving cleanly — `flutter analyze` reports no issues. Laravel API scaffolded (`api/`, Laravel 13.25 — current major, spec assumed 12) with Sanctum, Filament (v5.7 — v3 is incompatible with Laravel 13's Symfony 8), Socialite, Intervention Image, and kreait/laravel-firebase installed — `php artisan test` passes. See DECISIONS.md for every version/conflict call made along the way.
- 2026-08-13 — **Phase 1 complete**: design foundation built — `core/theme/` (colors, typography via Plus Jakarta Sans, dimens, light+dark `ThemeData`), EN+SW ARB files wired through `flutter_localizations`/`intl`, and all twelve motion primitives implemented in `core/motion/` (page transitions, hero image, staggered reveal, shimmer skeletons, animated chips, badge bounce, bottom sheets, success checkmark, nav bar, pull-to-refresh, parallax carousel, splash transition) with a `/motion-gallery` debug route demonstrating each live. `flutter analyze` clean, widget test passes — the test caught a real bug (three primitives called `MediaQuery.disableAnimationsOf(context)` inside `initState()`, which Flutter forbids; fixed by moving the check to `didChangeDependencies()`). Visual verification on an Android emulator was skipped per instruction — final visual check deferred to the release APK at the end of the build.
- 2026-08-13 — **Phase 2 complete**: full API skeleton — 13 migrations (all spec tables + Sanctum's `personal_access_tokens`), Eloquent models with relationships/scopes/casts, `ReviewObserver`/`ReportObserver` for denormalised ratings and the 3-upheld-reports auto-hide rule, and every spec'd service interface (`SmsGateway`→log mock, `NidaVerifier`→manual-review mock, `PaymentGateway`→unimplemented stub, `SocialAuthVerifier`→real Google/Facebook/Apple server-side verification, `PushNotifier`→log mock, `DistanceQuery` for MySQL/SQLite-portable radius search). Seeded 10 categories, 20 sellers (14 verified/4 pending/2 rejected), 120 products with placeholder media, 70 orders (60 completed + 10 mixed status — see DECISIONS.md), 60 reviews. 37 REST endpoints covering auth (phone OTP + social), discovery, seller onboarding, products, favourites, orders/checkout, reviews, chat, reports, and devices, with rate limiting on OTP requests and writes. 45 feature tests pass (`php artisan test`), including a full-surface smoke test and targeted coverage of every money path (order-gated reviews, single-seller carts, seller-only status transitions, pending-seller product visibility). Manual smoke-testing against a real running server caught and fixed several bugs before formalizing tests as automated coverage — see DECISIONS.md for the full list (missing `AuthorizesRequests` trait, mass-assignment silently dropping status-transition fields, a `Stringable === string` always-false comparison, and stale cached-relation reads in authorization checks).
- 2026-08-13 — **Phase 3 complete**: app shell wired end to end — go_router `StatefulShellRoute` with the 5 bottom-nav tabs (Home/Search/Sell/Orders/Profile, Sell as the centre FAB) sharing a fade-through transition on switch while `IndexedStack` preserves each branch's state; dio client with an auth interceptor (attaches the Sanctum token, clears session + notifies on 401) and typed `ApiException` mapping; drift-backed offline cache (JSON-blob pattern, schema-agnostic); `flutter_secure_storage` for the token; Riverpod providers tying it together (`authStateProvider`, `isOnlineProvider`, etc.); a connectivity banner; TZS/phone formatters and form validators; and a real (not stubbed) phone-OTP sign-in bottom sheet against the Phase 2 API, pulled slightly ahead of Phase 6 so the Profile/Orders/Sell tabs' sign-in buttons actually do something. Also stood up the `data/api` (retrofit) + `data/models` (freezed) + `data/repositories` layers the spec's file tree calls for, starting with the Auth domain — the pattern Phase 4+ will reuse for products/orders/etc. `flutter analyze` clean, widget test passes. See DECISIONS.md for the go_router `navigatorContainerBuilder` workaround, the nav-item-vs-branch-index mapping bug caught before it shipped, and a retrofit_generator codegen bug worked around.
- 2026-08-13 — **Phase 4 complete**: discovery feed — Home tab with a sticky animated category chip row (`SliverPersistentHeader`), Near You/Trending/New Sellers sort, radius filter sheet (1/5/10/25km/All), location permission flow with a Dar es Salaam district-picker fallback (never blocks the app), List⇄Map toggle with real (not approximated) seller pin positions, staggered-reveal + shimmer-skeleton product grid, and cache-aware repositories so a network failure falls back to the last cached results. Search tab with 400ms-debounced search and persisted history (drift key/value). Extended the API's `SellerSummaryResource` with `lat`/`lng` after the first map implementation turned out to be showing pins at fabricated positions — a correctness fix, not a UI tweak; see DECISIONS.md. `flutter analyze` clean; 3 widget tests pass, including a new `ProductCard` test (per CLAUDE.md's testing rule) that caught a real overflow bug on first run.
- 2026-08-13 — **Phase 5 complete**: product detail (shared-axis-Z hero transition, mixed image/video media carousel with autoplay-while-active muted video, favourite/share/report actions, seller card, WhatsApp + message-seller buttons) and shop profile (products/gallery/reviews tabs, review distribution bar, share deep link). Backend gained a `seller_id` filter on `GET /products` (shop tabs need one seller's products) and `whatsapp` on `SellerSummaryResource` (the product page's WhatsApp button needs the seller's real number — the first draft used the seller's database id, caught immediately). Also caught and fixed a real routing bug: every `Navigator.pushNamed(...)` call written this phase would have failed at runtime, since the app uses go_router exclusively with no named-route table — replaced with `context.push()` throughout and registered the missing routes. `flutter analyze` clean; all widget tests and all 45 `php artisan test` cases pass.
- 2026-08-13 — **Phase 6 complete**: all four sign-in methods (phone OTP from Phase 3, plus real Google/Facebook/Apple native SDK flows in `social_sign_in_buttons.dart`, each ending in a server-verified `/auth/social` POST — tokens are never trusted client-side) and the full 4-step seller onboarding wizard (`features/seller/presentation/onboarding/`) with a progress bar, shared-axis-Z entry transition, and drift-backed draft resumption (each step PATCHes the server-side `SellerProfile` row created in step 1, so a killed app resumes at the right step): business details, a real Google-Maps pin picker with reverse geocoding, NIDA number + client-compressed ID photo, and licence upload (image or PDF, 5MB cap) via `file_picker`. Wizard completion shows the full-screen drawn-checkmark success moment, then routes into a real My Shop status view (pending/verified/rejected badge) — CLAUDE.md's "nav gains a My Shop tab once one exists," implemented as the Sell tab's content morphing rather than a 6th nav item. "Start selling" on Profile and Sell now both route into the real wizard. Google/Apple buttons gate on `SokoniSocialAuthConfig` and disable with a tooltip until their real credentials land (BLOCKERS.md item 3); Facebook's App ID is native-platform config, not Dart, so it has no such gate. Also fixed a real, previously-undetected local-dev-loop bug: `flutter test` on this Windows machine always compiles the host platform's plugin registrant even though the app ships Android/iOS-only, and the existing `win32: ^6.3.0` override (needed to reconcile `file_picker`/`share_plus`/`wakelock_plus`) silently broke `file_picker`'s actual Windows source, which is written against win32 5.x's API — `flutter analyze` never caught it since it doesn't fully compile platform-conditional code. Fixed by lowering the override to `win32: ^5.9.0`, letting pub resolve compatible `share_plus`/`wakelock_plus` versions automatically. `flutter analyze` clean, all widget tests pass.
- 2026-08-13 — **Phase 7 complete**: selling & orders, end to end. Backend gained real multi-size image processing (`ImageVariants` + Intervention Image v4 — thumb 300/card 800/full 1600, a new `card_path` column added since the original schema only had two of the three sizes CLAUDE.md calls for), a video-plus-client-thumbnail upload path (`ProductMediaController`), an 8-item-per-product cap, `GET /shop/products` for the seller's own (including hidden/pending) listing, and `is_active`/`is_hidden` on `ProductResource` — 5 new feature tests, 50/50 passing. Client gained: product create/edit (`product_form_screen.dart`) with in-place media management (camera/gallery photo, gallery video — both compressed client-side, video gets a `video_compress`-extracted poster frame); a My Shop dashboard (list, add, edit, delete) wired into the Sell tab under its existing status badge; a single-seller cart (`CartController`) with a bounce-badged cart icon on the home app bar; checkout (delivery/pickup, address with a "use current location" action, cash-on-delivery/pay-on-pickup, running total) ending in the full-screen success-checkmark motion primitive and a straight hop into the order's auto-opened chat thread; and buyer + seller order lists/detail with the real per-order timeline and status-advance/cancel actions, gated by comparing the signed-in user against `order.buyer.id` (no seller-profile-id field needed — the backend policy already guarantees only the two right parties can ever see an order). Also built the real client-side FCM registration flow (`core/push/push_service.dart`), wired to fire on every sign-in — it fails soft (logs and moves on) with no Firebase project configured yet (BLOCKERS.md item 1), which is also where a stale claim from an earlier session (that this was already wired) got corrected. Caught and fixed a real bug before it shipped: `ref.watch(cartProvider.notifier).xxx` doesn't rebuild on cart changes — Riverpod's `.notifier` modifier subscribes to the controller instance, not its state — see DECISIONS.md. `flutter analyze` clean, all widget tests and all 50 `php artisan test` cases pass.
- 2026-08-13 — **Phase 8 complete**: chat & reviews. Backend gained a real (not simulated) typing indicator over the existing 5s-polling transport — per-party `typing_until` timestamps on `conversations`, refreshed via a new `PATCH /conversations/{id}/typing` and read back on the same poll — plus fixed chat image attachments to store full URLs like every other media field already does (`MessageController` previously saved bare disk-relative paths the client couldn't load), and added `seller_handle` to `UserResource` so the shop review tab can tell its own owner apart from a browsing stranger. 7 chat feature tests, 52/52 passing overall. Client gained a full conversation list + thread UI (`features/chat/`) replacing the Phase 3 placeholder: 5s polling for both messages and typing state, read receipts (double-check icon once the other party's poll marks a message read), image attachments via `image_picker`, and the pinned per-product context card CLAUDE.md calls for — plus order-gated review submission (a star-rating bottom sheet on the order detail screen, shown only once an order is completed and unreviewed) and seller reply UI on the shop profile's review tab. First hand-written Riverpod family `AsyncNotifier` in the codebase (`ChatThreadController`, one polling timer per open conversation) — see DECISIONS.md for the constructor-carries-the-arg shape that pattern needs in this API. `flutter analyze` clean, all widget tests and all 52 `php artisan test` cases pass.
- 2026-08-13 — **Phase 9 complete**: trust & admin. The Filament panel itself had never actually been installed despite `FilamentUser` scaffolding since Phase 2 — ran `filament:install --panels`, branded it (Sokoni yellow, "Sokoni Admin"), and built four resources: the seller verification queue (`SellerProfileResource` — NIDA photo, licence file link, and a Google Maps link shown side by side, verify/reject actions using `forceFill()` for the same reason every other protected status transition in this codebase does), the moderation queue (`ReportResource` — uphold/dismiss plus direct hide/warn/suspend/ban actions resolved to the right underlying user via a new `Report::offendingUser()`), user management (`UserResource` — warn/suspend/ban/unban, admin account creation), and category management (`CategoryResource`), plus a dashboard stats widget. "Suspend" needed two new `users` columns (`banned_until`, `ban_reason`) since the spec's schema only had a permanent-ban timestamp with nowhere to persist a reason or an expiry — banning/suspending now also revokes the user's existing Sanctum tokens, not just blocks future logins. Caught and fixed a real bug via the new panel tests: `canAccessPanel()` threw a `TypeError` instead of returning `false` for any user whose `is_admin` read as `null` in memory (true of nearly every seeded user) — every non-admin panel visit was one crash away from a 500. 12 new backend tests (panel access + reports), 66/66 passing. Client gained message-level reporting (long-press in the chat thread, joining the existing product/shop report buttons) and real Terms of Service / Privacy Policy screens — genuinely written for Sokoni's actual features in EN/SW, versioned, with a one-time acceptance gate shown right after every sign-in. `flutter analyze` clean, all widget tests pass.
- 2026-08-14 — **Phase 10 complete — the app is done.** Audited every screen for CLAUDE.md's "no blank screens, every error offers retry" rule and fixed the real gaps found (bare error text with no retry on the shop dashboard, onboarding wizard, and two category dropdowns; infinite spinners with no error branch on Profile/Sell; an ad hoc empty-map widget swapped for the shared component; the "showing saved results" banner extended to Search, Shop Profile, and Home's map view — all of which fall back to cached data on a network failure but weren't saying so). Replaced four raw `Image.network` calls with `CachedNetworkImage` (the only screens still bypassing the disk-backed cache) and capped the global in-memory image cache (60MB/200 entries) against unbounded growth while browsing a long feed — CLAUDE.md's "mid-range Android on patchy 3G/4G" target taken literally. Independent full EN/SW translation review (all 210 keys 1:1, zero placeholder mismatches) found and fixed six genuine issues: a real mistranslation ("gallery" → "ghala", i.e. warehouse), two terminology-consistency slips, one ambiguous field label, and two naturalness/capitalization nits. Generated real app icon/splash from the brand logo (`flutter_launcher_icons`/`flutter_native_splash`) and a real release keystore, then spent most of this phase chasing three more win32-major-version conflicts through `flutter build apk` (`package_info_plus`, `flutter_secure_storage_windows`, both pinned to older win32-5.x-compatible lines) plus a JVM-target mismatch in an outdated plugin template (`flutter_facebook_auth`, fixed with a root-level Gradle block forcing JVM 17 across every subproject) — all local-dev-machine-only issues, irrelevant to the shipped Android/iOS build, but blocking the ability to produce one on this Windows machine at all. Wired `config/filesystems.php`'s `public` disk to the `PUBLIC_UPLOADS_ROOT`/`PUBLIC_UPLOADS_URL` env vars the deploy runbook needs (the vars existed in `.env.example` since an earlier phase but were never actually read anywhere) and found a second stale BLOCKERS.md claim — no Google Maps API key placeholder actually existed in `AndroidManifest.xml`/`AppDelegate.swift` despite BLOCKERS.md saying so — added both for real. Wrote `docs/API.md` (full endpoint reference), `docs/DEPLOY.md` (the cPanel runbook, updated with the real filesystem/build-flag specifics), and `docs/DEMO.md` (a 12-step, two-device client demo script). Final state: `flutter analyze` clean, all Flutter widget tests pass, all 66 `php artisan test` cases pass, `flutter build apk --analyze-size` shows a reasonable ~22MB arm64 payload with no runaway dependency, and both `app-release.apk` and `app-release.aab` are built, signed with a real (not debug) keystore, and verified via `apksigner`.
- 2026-08-14 — **Post-launch: real brand logo dropped in.** The client's real logo replaced the placeholder at both `assets/brand/sokoni_logo.png` paths (arrived as `.jpeg`, losslessly converted to PNG). Regenerated app icons and splash screens from it. Along the way, found the full logo lockup (bag+S glyph plus "Sokoni" wordmark) doesn't work directly as a launcher icon — the wordmark turns illegible and gets clipped by the adaptive-icon mask — so cropped a second icon-only asset (`sokoni_logo_icon.png`) and pointed `flutter_launcher_icons` at that while `flutter_native_splash` keeps the full lockup. Also swapped `SokoniSplashTransition` and the pull-to-refresh `SokoniBagSpinner`, both previously drawn with a generic `Icons.shopping_bag_rounded` stand-in from before the real logo existed, over to the real cropped mark. See DECISIONS.md for the transparent-cutout attempt that didn't work out. `flutter analyze` clean, all widget tests pass.
- 2026-08-15 — **Post-launch: fixed a release-build startup hang, tracked it to Android 16, then to Facebook.** The production API went live, but the signed release APK hung on the splash screen with no error and never reached the feed. First pass added bounded timeouts across the whole startup path (Firebase init, location, secure storage, the first API call) and a 5-second watchdog with a real error+retry screen — all real, correct fixes, but the hang persisted on the actual test device (an Android 16 Infinix Note Edge), because the Dart watchdog never even fired: the freeze was before Flutter's engine started, before any Dart code ran. Built a `diagnostic` Gradle flavor with a bare-minimum `FlutterEngine`/entrypoint that registers zero plugins, installable alongside the real app — it booted fine on the same device, isolating the cause to native plugin registration. Read the actual plugin source of every suspect rather than guessing from version numbers: `flutter_facebook_auth`'s Android plugin constructs a `FacebookAuth()` (initialising the native Facebook SDK) unconditionally at plugin-registration time, on every launch, with no Facebook App ID ever configured — a well-documented Facebook SDK hang scenario, worse on a security-locked device. Removed Facebook sign-in entirely, client and server, rather than work around a bug in an unconfigured feature: `pubspec.yaml`, the sign-in button/handler, `SokoniSocialAuthConfig`, `HttpSocialAuthVerifier`'s Facebook branch, `services.facebook` config, `.env`/`.env.example`, and the Terms/Privacy copy that mentioned it. Confirmed empirically (not assumed) that the JVM-17 Gradle workaround from Phase 10 was needed only because of that one package, and removed it too. Google, Apple, and phone OTP untouched. `flutter analyze` clean, all Flutter widget tests pass, all 73 `php artisan test` cases pass, and both a `prod` and a `diagnostic` release APK build and verify against the real release keystore.
- 2026-08-15 — **Post-launch: startup hang wasn't Facebook after all — bisected plugin registration on the real device, cleared all 22 plugins, traced the real culprit to the app's own pre-first-frame startup path.** Rebuilt `DiagnosticMainActivity` to register every plugin one at a time (in a fixed, most-likely-suspects-last order) instead of registering none, writing each plugin's name to both an on-screen label and a `Downloads/sokoni-boot.txt` log file (via `MediaStore`, no adb/storage-permission needed) immediately before each attempt — with a short scheduled delay first, since `TextView.setText()` only schedules a repaint rather than painting synchronously, and an immediate hang right after setting the label risked freezing on the *previous* step's stale text instead of naming the real one. On the real device: all 22 plugins registered clean, reaching "Sokoni starting." Traced every line from `main()` to the first frame by hand and confirmed there's no `redirect:` callback anywhere in `app_router.dart` gating route resolution, and nothing in `SplashScreen.initState()` is awaited — the hang has to be before or during that chain, which structurally rules out the per-widget watchdog ever being able to catch it. Found and fixed a real, concrete bug that matches the "3G only, never WiFi" clue exactly, by reading `google_fonts`' own source rather than guessing: its runtime font-fetch HTTP call has no timeout at all, and this app was never bundling the font locally (no real Plus Jakarta Sans files were available to bundle) — not confirmed as *the* cause since the call is fire-and-forget and shouldn't block first paint on its own, but bounded it to 8s via a wrapped `http.Client` regardless, since CLAUDE.md explicitly targets patchy 3G/4G. Rearchitected the watchdog to arm globally in `main()`, before `runApp()`, independent of whether any widget ever builds — cancelled via `WidgetsBinding.instance.addPostFrameCallback` on the first real frame, whatever route it turns out to be, with a minimal standalone fallback screen if it fires. Added `BootLog`, a silently-fails-soft `MethodChannel` call instrumenting `main()` and `SplashScreen`'s key phases, and wired `DiagnosticMainActivity` to receive and log those calls too — then rebuilt the diagnostic APK targeting the *real* `lib/main.dart` (not the placeholder `main_diagnostic.dart`) so the next test run continues logging phase-by-phase into the app's actual startup path instead of stopping at a bare "Sokoni starting" screen. `flutter analyze` clean, all widget tests pass, both `prod` and `diagnostic` release APKs rebuilt and verified against the real release keystore. Root cause still not conclusively identified — the next real-device run is what should reveal it.
- 2026-08-15 — **Post-launch: that device run came back completely dark past plugin registration — zero Dart-side log lines, so fixed the blindness itself before chasing the bug further.** All 22 plugins registered clean again, then nothing — meaning either Dart's `main()` never runs at all, or `BootLog` fails silently, and there was no way to tell which. Made `DiagnosticMainActivity` prove the channel rather than assume it: logs its own handler attachment, logs the raw method+arguments of every message it ever receives (not just well-formed ones), and logs the exact `DartEntrypoint` (bundle path, library, function name) immediately before *and* after calling `executeDartEntrypoint()` — that call is non-blocking, so the "returned" line alone rules out a hang in the native call itself. On the Dart side, wrapped the entirety of `main()` in `runZonedGuarded` and wired `FlutterError.onError`/`PlatformDispatcher.instance.onError` through `BootLog` — a silent black screen with no log output is the textbook signature of an uncaught exception before first paint, and this exists to name it. Made the true first working statement (after the binding init a `MethodChannel` ping structurally requires to have any chance of sending anything at all) a `BootLog` ping, so "did Dart even start" now has a real answer either way. Also removed `google_fonts` entirely per instruction, not just timeout-bounded it as the previous pass did — `SokoniTypography` now builds its `TextTheme` from Flutter's own `Typography.material2021()` with `fontFamily: 'PlusJakartaSans'` applied, no network involved at all. Could not source the real Plus Jakarta Sans `.ttf` files anywhere on this machine (no network access, nothing cached) to actually bundle — fabricating a substitute felt worse than leaving it wired-but-unpopulated, so it currently falls back to the platform default font safely; the `fonts:` block is ready and commented in `pubspec.yaml` for whenever the real files are sourced. `flutter analyze` clean, all widget tests pass, both `prod` and `diagnostic` release APKs rebuilt and verified against the real release keystore. Root cause still not identified — this run exists specifically to stop guessing about it.
- 2026-08-15 — **Post-launch: found it. The log finally showed a real, specific failure — "SplashScreen watchdog fired — animation never completed" — and it led to a genuinely oversized brand asset, not a hidden startup dependency.** Traced `SokoniSplashTransition` end to end: no Future, no image load, nothing awaited anywhere on the path to its `onComplete` callback — the only way it can fail to fire is the `AnimationController`'s own ticker never reaching `completed`. Checked the actual asset behind its `Image.asset` call and found `sokoni_logo_icon.png` had become a 6250×6250 placeholder (~149MB decoded as raw RGBA) for a widget rendered at 96×96, decoded fresh on every single animation tick with no `cacheWidth`/`cacheHeight` cap — exactly the kind of main-thread/memory pressure that could stall a ticker on a real mid-range device. Caught a bug in my own fix along the way: the placeholder's background is fully transparent, not white as it appears in an image viewer, so a naive brightness-based crop treated the whole image as one block — fixed by detecting content via the alpha channel instead, same as the crop technique from earlier this session. Resized the full lockup to 1080×1080 and re-cropped an icon-only glyph onto a white canvas this time (this placeholder's glyph is black-on-transparent, not white-on-black like the earlier real logo, so the previous black-canvas convention would have made it invisible) — regenerated launcher icons and splash from the corrected assets, and added `cacheWidth`/`cacheHeight` to both `Image.asset` call sites that reference this file so an oversized source asset can't silently balloon decode cost like this again. Also rearchitected `SplashScreen` on principle, not just to patch this one bug: navigation to home now runs off a plain timer independent of the animation's own completion signal — an animation must never gate routing — and the 5-second watchdog no longer shows an error screen, it forces the same navigation instead, since a user should never be stranded on a splash screen with nowhere to go. `flutter analyze` clean, all widget tests pass, both `prod` and `diagnostic` release APKs rebuilt and verified against the real release keystore.
- 2026-08-15 — **PART 1 complete — contrast/readability audit.** Traced the reported location-dialog bug to its actual mechanism rather than patching the symptom: `SokoniTypography` built its base `TextTheme` from `Typography.material2021().black` (a fixed light-theme variant) for *both* themes, so any TextTheme role not explicitly overridden with a theme-aware colour — including `headlineSmall`, which is exactly what `AlertDialog`'s Material 3 default title style reads — stayed dark-on-dark in dark mode regardless of the rest of the theme being correct. Fixed at the root (pick `.white`/`.black` based on actual brightness) and belt-and-suspenders at `dialogTheme` (explicit `titleTextStyle`/`contentTextStyle`, bypassing Material's default-resolution chain entirely). Fixed the bottom nav bar's selected-tab colour, which was separately hardcoded black — redesigned as a yellow pill behind a black icon plus theme-`onSurface` label text, since raw yellow text/icons fail WCAG contrast badly on either surface (~1.6:1), which the brief called out explicitly. Delegated a full-codebase sweep for the same two bug classes (hardcoded colours ignoring brightness; yellow-as-text) to a research agent and fixed everything it found: product cards, product/shop detail screens, seller onboarding, the radius filter sheet, order status chips, the seller "pending" status label, empty states, the chat message bubble, and several lower-severity cosmetic-only spots. Built `/theme-audit`, a debug route showing every themed component in both light and dark theme side by side regardless of the device's actual system setting. Re-checked both outstanding assets (the placeholder logo — still a placeholder but correctly sized now, not the 6250×6250 that caused the earlier startup hang; Plus Jakarta Sans — still no font files available) and logged them in `BLOCKERS.md`. `flutter analyze` clean, all Flutter widget tests pass, all 73 `php artisan test` cases pass.
- 2026-08-15 — **PART 2 complete — sign-up clarity and account intent.** Reworked the phone OTP sheet: header now reads "Sign in or create an account" in both languages, and once a code is sent to a never-seen number the OTP step plainly states a new account is about to be created (`requestOtp` now returns whether the number is new, threaded through from a new `is_new_account` field on `POST /auth/otp/request`). Added a one-question intent screen (`/account-intent`, `AccountIntentScreen`) shown exactly once, right after a brand-new account's first sign-in across every sign-in method (phone OTP, Google, Apple) via one shared `completeSignIn()` helper — "buy" and "decide later" land on the feed, "sell" goes straight into the existing seller onboarding wizard, and the choice is persisted server-side (`users.account_intent`, `POST /auth/intent`) so it can never reappear; deliberately keyed off the sign-in response's transient `is_new_account` flag rather than `account_intent IS NULL`, since the latter would incorrectly re-trigger for every pre-existing account that signed up before this field existed. One account model throughout, exactly as instructed — no new user types, no split apps. Implemented `BeemSmsGateway` for real OTP delivery against Beem Africa's send-SMS API, bound automatically in `AppServiceProvider` only when both `BEEM_SMS_API_KEY`/`BEEM_SMS_SECRET_KEY` are set, falling back to the existing `LogSmsGateway` otherwise (local dev/CI unaffected); documented the whole integration, what a production Beem account needs, and how it's tested without sending real SMS in `docs/SMS.md`. `flutter analyze` clean, all Flutter widget tests pass, all 85 `php artisan test` cases pass (5 new Beem/SmsGateway-binding unit tests, 7 new phone-OTP/intent feature tests).
- 2026-08-15 — **PART 3 complete — the social business layer.** Added Updates/Offers/Showcases/Customers as three concrete tables plus a real follow model, every one with a non-nullable `seller_id` FK to `seller_profiles` (Offers/Showcases also a non-nullable `product_id`) — a schema guarantee against standalone personal content, not just request validation, proved directly in tests (`test_the_database_rejects_an_update/offer/showcase_with_no_seller/product`, expecting a raw `QueryException`). Backend: `Update`/`Offer`/`Showcase`/`Customer` models with `scopeVisible()`/`scopeFollowedBy()`, `CustomerObserver` denormalising `seller_profiles.customer_count` the same way `ReviewObserver` already does `rating_avg`/`rating_count`, ownership Policies mirroring `ProductPolicy`, FormRequests enforcing "must be one of the acting seller's own Listings" via `withValidator`, full CRUD controllers + routes, `PushNotifier` wired to notify a shop's followers on a new Update/Offer, and an `updates:delete-expired` scheduled command (the first `Schedule::` entry in this codebase) hard-deleting Updates past their 24h expiry along with their media files. 31 new feature/unit tests, 113/113 `php artisan test` passing — caught and fixed a real bug along the way (`OfferController` was persisting an unstringified `Stringable` into `discount_type`, which crashed `discountedPrice()`'s `match` on every single offer creation; the test suite caught it before it shipped). Frontend: full data/API/repository/provider layer for all four resources, a new `StoryRing` motion primitive (yellow ring, unseen state, same single-`AnimationController`-fires-on-flip skeleton as every other primitive) with a "last seen per shop" mark stored locally via `AppDatabase`'s existing key/value cache (not synced server-side — see DECISIONS.md), an Instagram-style Updates tray + full-screen story viewer, an Offers row with live countdown + strikethrough pricing, a Showcase preview row leading into a full-screen vertical video feed (video-controller lifecycle reused from `ProductMediaCarousel`, not `visibility_detector`), a centre-FAB create sheet (Listing/Update/Offer/Showcase for a seller, "Start selling" for a buyer) with three new composer screens reusing the existing image/video compression pipeline, shop profile gained Showcase/Updates tabs plus a customer count and a real follow button, and a shared "Following" filter. Renamed "Products" to "Listings" in the shop profile tab and every new Part 3 surface per the required terminology, and "Favourites" to "Saved" — did **not** sweep every pre-existing "Product"-labelled string app-wide (product form titles, cart/checkout, search hints, etc.); see DECISIONS.md for the exact boundary. `flutter analyze` clean, all Flutter widget tests pass (1 new `StoryRing` test), all 113 `php artisan test` cases pass.
- 2026-08-16 — **Device-testing round, PARTS 1–2 complete — the account-creation blocker, and the intent screen it was silently eating.** `TermsAcceptanceScreen` wraps itself in `PopScope(canPop: false)` (correctly — it should never be back-swipeable while the request is in flight), but on a *successful* acceptance it called `Navigator.of(context).maybePop()`, and `maybePop()` explicitly honours `PopScope.canPop` the same way it honours a back gesture — confirmed against the Flutter SDK's own `NavigatorState` source (`popDisposition`/`maybePop` in `navigator.dart`), not guessed. So the accept call succeeded and persisted server-side every time, but the pop was silently refused: spinner, then back to the same screen, forever — exactly the reported symptom, and a full account-creation blocker since `ensureTermsAccepted()` awaits that route's future before anything past it (including PART 2's intent screen) can run. This also fully explains PART 2's "missing" intent screen without it being a second bug: a tester who force-closed and reopened the app after getting stuck would land straight on the home feed on the next launch (only the fresh sign-in path enforces the terms/intent gate — re-opening the app just checks for a stored token), which matches "phone → code → full name → terms → home" precisely. Verified the route, middleware, and `AcceptTermsRequest` validation are all correct by probing the live production API directly (`https://api.sokoni.co.tz/api/auth/terms/accept` returns a clean `401 {"message":"Unauthenticated."}` for the exact headers the app sends) — ruling out the auth-before-token and missing-column hypotheses before touching any code, per instruction. Fixed with `context.pop()` (go_router's own idiom, matching the rest of the codebase, and confirmed to bypass `PopScope.canPop` the same documented way `Navigator.pop()` does) and broadened the failure handling to catch `Object`, not just the typed `ApiException`, so an unexpected response shape can never fail silently either — every failure now sets `_error` and re-enables the button, a real retry. Confirmed PART 2's intent screen (`/account-intent`, `AccountIntentScreen`), the reworked phone-sign-in header ("Sign in or create an account"), and the new-account OTP notice were all still intact from the prior session's work and needed no changes beyond this fix reaching them. New coverage: `api/tests/Feature/Auth/TermsAcceptanceTest.php` (success, unauthenticated, missing version, re-acceptance) and `app/test/features/legal/terms_acceptance_screen_test.dart`, which pushes the real screen under a real `GoRouter` (a bare `Navigator.push` doesn't reproduce this class of bug, since `context.pop()` needs a `GoRouter` ancestor to fail the same way) and asserts it actually closes on success and stays open with a visible, re-triable error on failure. **A separate, serious finding surfaced while probing production and logged to BLOCKERS.md, not fixed here since it's an infra/env setting, not app code**: `api.sokoni.co.tz` is currently running with `APP_DEBUG=true` — a request with an unexpected header combination returned a full stack trace including server file paths. This needs to become `false` in the production `.env` regardless of this session's fixes. `flutter analyze` clean, all Flutter widget tests pass (2 new), all 117 `php artisan test` cases pass (4 new).
- 2026-08-16 — **Device-testing round, PART 3 complete — a real "For You" feed replaces the plain product grid.** Home now has two tabs: "For You" (default, single-column social feed) and "Shops" (the existing Near You/Trending/New Sellers grid, unchanged in substance but relocated under its own tab with the map⇄list toggle, radius filter and cart icon moved into a local toolbar there — none of the three mean anything on For You, and the spec's "keep the top bar uncluttered" ruled out leaving them global). Backend: a real server-side `FeedService` composes the feed per CLAUDE.md's exact ordering ("followed shops first, then nearby verified sellers, then Offers running now, then Showcase videos inline every 6-8 items") — followed sellers' Listings, then everything else (nearest-first when the buyer's location is known, newest-first otherwise, matching the existing discovery-grid fallback), sponsored Listings pulled to the front of that second tier but never ahead of a followed shop, then running Offers, with one Showcase woven in after every 6 items — paginated 10 per page via `GET /feed`, public like every other discovery endpoint but personalised when a token is present. New `comments` table (one level of replies, enforced in `CommentStoreRequest` not the schema) with full CRUD, wired into the existing polymorphic report system (`comment` joins `product`/`shop`/`message` in `StoreReportRequest::REPORTABLE_TYPES`, `Report::offendingUser()` gained a Comment branch) so "report a comment" reaches the same moderation queue for free. `products` gained `is_sponsored`/`sponsored_until`/`sponsor_contact_method` (seller-toggleable via a new `PATCH /products/{id}/boost`, no payment flow yet per instruction) and a computed `comments_count`. Found two real, separate bugs while building this: (1) a Laravel gotcha that would have silently broken every `AppNotification`-shaped response — a `JsonResource` whose own `toArray()` output already contains a top-level `data` key defeats Laravel's automatic response-wrapping entirely (traced to `ResourceResponse::haveDefaultWrapperAndDataIsUnwrapped`'s `array_key_exists('data', $data)` check), so the notification payload field is exposed as `payload`, not `data`, in the API (the DB column keeps its name) — documented in the resource itself since this exact trap will recur the moment any future resource happens to have its own field called `data`; (2) the same `Fillable`-vs-`update()` silent-no-op class of bug this codebase has hit repeatedly (Phase 2, Phase 7) recurred in `NotificationController::markRead` (`read_at` deliberately isn't Fillable) — fixed with `forceFill()`, caught by a feature test before it shipped, not in production. Also added a `seller_profiles.logo` column (Instagram-style headers on both the feed card and Part 4's shop profile need a shop avatar, which never existed — only `users.avatar`, the signed-in person's own photo) with a real upload endpoint (`PATCH /sellers/{id}/logo`) and, separately, a real persisted in-app notification inbox (`app_notifications` table, `GET/PATCH /notifications`) — CLAUDE.md Part 3 names a Notifications top-bar icon, and this app had no in-app notification record at all (only ephemeral, currently-mocked FCM pushes), so `LogPushNotifier` now also writes a durable row on every call, and the icon opens a real list rather than a dead button. Frontend: `FeedCard` dispatches on item type (product/offer/showcase) inside one shared shell (seller header with a real Follow button — `SellerSummary` gained `is_following`/`logo` to support it — Save/Comment/Share/Chat action row, 4:5 media, price line, `@handle`-prefixed caption, comment count, Order button), muted-autoplay-above-60%-visibility video via `visibility_detector` (the one place in this app a scrolling, not paged, list actually needs it — every other video surface has each item fully on/off screen already), a comment bottom sheet with one level of replies and a seller badge, and a seller-side boost dialog (duration + contact-method chips) on the My Shop dashboard. `isSellerFollowed`/`toggleSellerFollow` were generalised from taking a `SellerProfile` to taking `{sellerId, handle, isFollowing}` directly so the same optimistic-follow mechanism covers both the shop profile's button and the feed card's. **Scoping calls made explicitly, not silently**: the Order button on every card variant opens the product detail screen (the app's one real, fully-built checkout entry point) rather than duplicating checkout inline in the feed; Offers get their own dedicated card (reusing the Offers-row card layout) rather than *also* overlaying strikethrough pricing onto a product's own card when both happen to reference the same Listing, since one clear treatment beats a duplicated, harder-to-reason-about one. `flutter analyze` clean, all Flutter widget tests pass (2 new: a `TermsAcceptanceScreen` regression pair from PART 1, a `FeedCard` render test), all 144 `php artisan test` cases pass (26 new — Feed composition/sponsorship/boost-authorization, Comment CRUD/one-level-reply enforcement/moderation, seller logo upload, and the notification inbox).
- 2026-08-16 — **Device-testing round, PART 4 complete — the shop profile, Instagram-style.** Rebuilt around the pattern CLAUDE.md asked for: circular shop avatar (the new `seller_profiles.logo` from PART 3) with a small yellow verified tick, three counts (Listings/Customers/Following — `products_count` and `following_count` added to `SellerProfileResource`, `customer_count` already existed), bio block (category, description, location line, tappable WhatsApp row), Edit profile + Share for the owner or Follow + Message for everyone else, and an owner-only 30-day dashboard strip. Tab row switched from five text-labelled tabs to four icon tabs (grid/play/person/star for Listings/Showcase/Updates/Reviews) — dropped the old generic "Media" tab entirely (`ShopMediaGrid`, deleted) since Part 3 already gave Updates and Showcases their own dedicated homes, making an all-media-mixed grid redundant; kept Reviews as a fourth tab despite Part 4's own list naming only three, since dropping ratings/reviews would cut a CLAUDE.md core requirement (feature 2's distribution bar) that Part 4 never asked to remove. The Listings tab itself became a real 3-column grid (`ShopListingsGrid`, one tile per Listing, play badge on video covers, stacked-squares badge on carousels) replacing the old 2-column `ProductCard` grid, and the Showcase tab's tiles gained real view counts. **Owner dashboard strip is honestly scoped, not a placeholder pretending to be one**: `SellerDashboardController` reports saves and orders genuinely scoped to the last 30 days (both have real timestamped rows to query), but *total* views, not a 30-day figure — `products.views` is a single lifetime-incrementing counter with no per-event log behind it, and fabricating a 30-day-looking number from a lifetime counter would be exactly the "placeholder pretending to be data" CLAUDE.md rules out. Built the "Edit profile" flow that turned out not to exist anywhere in the app at all — no screen, no repository method, nothing — for bio/WhatsApp-visibility/logo (shop name, handle and category stay onboarding-only, matching every other identity field in this app). `flutter analyze` clean, all Flutter widget tests pass, all 147 `php artisan test` cases pass (6 new — dashboard scoping/authorization, logo upload/authorization).
- 2026-08-16 — **Device-testing round, PART 5 complete — visual polish.** The real Plus Jakarta Sans `.ttf` files (four weights) had landed in `app/assets/fonts/` since the last check — wired up `pubspec.yaml`'s previously-commented `fonts:` block (no code changes needed, `typography.dart` already referenced the family name from an earlier session); this was the single highest-impact item in this part per instruction, and BLOCKERS.md's font entry is now resolved. Found and fixed the reported bottom-nav bug at its actual mechanism, not by guessing at a different icon: `Icons.search_outlined`'s bundled glyph in Flutter's Material Icons font has no real thin-stroke artwork distinct from its filled counterpart (a known limitation specific to the search icon — home/receipt/person all have genuine outlined art) — swapped the inactive state to the classic `Icons.search`, a guaranteed normal-weight glyph, while every other nav icon's outlined/filled pairing was confirmed already correct. `ProductCard` lightened per the brief: image gets 12dp top-only corners (the card's own border now follows that same silhouette instead of independently fully-rounding), price promoted to the strongest element (moved above the title, bumped to `titleSmall`/700 weight), and rating+distance collapsed from two separately-styled elements into one muted single-line caption (`"4.5 · 2.3 km away"`) — updated the existing widget test to match. Added a reusable `BottomGradientScrim` and used it everywhere an icon/badge/count sits directly on top of image or video content without a separate text block underneath (the new `FeedVisibilityVideo`'s mute icon, the Listings grid's video-play badge, the Showcase grid's play+view-count) — CLAUDE.md: "so overlaid text is always legible." Centralised haptic feedback into `toggleProductFavorite`/`toggleSellerFollow` themselves rather than at each call site (Save and Follow now fire consistently everywhere either function is called, including call sites from earlier phases this part never otherwise touched) and added it explicitly to the two genuinely separate "Order" actions (a feed card's Order button, checkout's Place Order). Audited tap targets in every widget built this device-testing round and found one real violation — the feed card's Follow button had an explicit `minimumSize: Size(44, 32)`, shrinking it *below* the theme's own global 44dp button-height default (`app_theme.dart` already enforces `minTapTarget` on every Filled/Outlined/TextButton app-wide) — fixed to match the theme baseline rather than override it downward. Audited the 4pt spacing scale for arbitrary literals: found exactly four, all pre-existing, all inside motion-primitive internals or bootstrap error UI with a real physics/one-off reason for not using a named constant — left alone rather than churn motion-primitive code for its own sake. `flutter analyze` clean, all Flutter widget tests pass (1 updated for the new caption format), all 147 `php artisan test` cases pass (backend untouched this part, run anyway per instruction).
- 2026-08-16 — **Device-testing round complete — release deliverables.** Built `app-prod-release.apk` (71.5MB) and `app-diagnostic-release.apk` (71.3MB), both `--flavor` release builds, both confirmed signed with the real Sokoni release certificate via `apksigner verify --print-certs` (`CN=Sokoni, SHA-1: 06e890e4bff2a859386ae2a641c1939db6f5f730` on both — not the debug fallback, and matching each other's fingerprint) — in `app/build/app/outputs/flutter-apk/`. Sizes are up from Phase 10's ~24.7MB analyze-size figure mainly because that figure was a single-ABI (`--target-platform android-arm64`) size-analysis run, not the actual shipped multi-ABI universal APK this build (and every prior real release build this session) produces — not a regression, an apples-to-oranges comparison. Built `sokoni-api-update-2.zip` (28KB, 31 files) containing exactly the backend files that changed since the previous deploy zip (`sokoni-api-update.zip`, built at the end of the prior session's PART 4) — computed by diffing the current working tree against that zip's own extracted contents file-by-file (not just against the last git commit, since nothing has been committed since Phase 10 and a commit-based diff would have re-included everything already shipped in that first zip), rather than assumed. Test files excluded again, same reasoning and precedent as the first zip. 4 new migrations to run on the server, in order: `2026_08_16_135854_add_sponsorship_to_products_table`, `2026_08_16_135856_create_comments_table`, `2026_08_16_142024_add_logo_to_seller_profiles_table`, `2026_08_16_143136_create_app_notifications_table`.
- 2026-08-16 — **PART 4 complete — polish & ship.** EN/SW review of everything added in Parts 2-3: 302/302 keys 1:1 between `app_en.arb`/`app_sw.arb`, zero placeholder mismatches, and the required terminology (Bidhaa/Taarifa/Punguzo/Onyesho/Mteja/Hifadhi) confirmed used consistently across every new string. `flutter build apk --analyze-size --target-platform android-arm64` shows a reasonable ~24.7MB payload, no runaway dependency; `prefer_const_constructors` is already an enforced lint (`flutter analyze` clean covers it) and the global 60MB/200-entry image cache cap from Phase 10 automatically covers every new `CachedNetworkImage` use, no changes needed. Built both `app-prod-release.apk` and `app-diagnostic-release.apk` (multi-ABI, `--flavor prod`/`--flavor diagnostic --release`), confirmed signed with the real release keystore (not the debug fallback) via `android/key.properties`'s presence and `build.gradle.kts`'s own signing-config selection logic. Updated `docs/API.md` with every Part 2/3 endpoint (`/auth/intent`, Updates/Offers/Showcases/Customers, the updated User/SellerProfile object shapes) and refreshed the SMS/push "what's stubbed" notes. Backend prepped for deployment: confirmed `fakerphp/faker` already sits in `require` (not `require-dev`) from an earlier phase, ran `composer install --no-dev --optimize-autoloader`, and built `sokoni-api-update.zip` at the repo root from exactly the 51 backend source files that differ from the last commit (`c6de251`, Phase 10) — everything Parts 2-3 touched, since the "post-launch" fixes between then and now were Flutter-only. Test files were deliberately left out of the zip (dev-only, no functional role in production) but are listed in DECISIONS.md for the record. 6 new migrations to run on the server, in order: `2026_08_15_140000_add_account_intent_to_users_table`, `2026_08_15_150000_create_customers_table`, `2026_08_15_150100_add_customer_count_to_seller_profiles_table`, `2026_08_15_150200_create_updates_table`, `2026_08_15_150300_create_offers_table`, `2026_08_15_150400_create_showcases_table`.
- 2026-08-17 — **Admin panel rebuild, SECTION 1 complete — seller verification actually works now.** Diagnosed with the real Filament Livewire action rather than guessing: the backend (`forceFill`, the policy, the request lifecycle) was already correct — a direct `Livewire::test(...)->callTableAction('verify', $seller)` passed with zero errors on the first try. The real bug was that `ViewSellerProfile` (the page showing the NIDA photo, licence and map — the only page with the evidence a reviewer needs) had **no header actions at all**; verify/reject only existed as row buttons on the list table, which displays none of that evidence. Extracted `SellerProfileResource::verifyAction()`/`rejectAction()` as shared factories and wired them into both the table and the view page's header. Replaced the old test (which called `forceFill` directly in the test body instead of the real action, so it could never have caught this) with real Livewire-driven coverage proving an admin can verify from the review page and that the seller's previously-404ing product becomes publicly visible immediately after, plus reject-with-reason and reject-requires-a-reason cases. `php artisan test`: 150/150 passing. Flutter app, SMS delivery and seeders untouched, per instruction.
- 2026-08-17 — **Fixed a real "No internet connection" false-positive on a genuinely online Android 16 device.** Confirmed the browser could load `https://api.sokoni.co.tz/api/products` on the same phone at the same moment the app reported offline — so this was never actually a connectivity problem, and an audit of the whole request path found no code anywhere that pre-checks `isOnlineProvider`/`connectivity_plus` before attempting a request (`ConnectivityBanner` was already purely advisory, never a gate — confirmed by grepping every call site). The real cause was `dio_client.dart`'s old 15s connect / 20s receive timeouts combined with `mapDioError` collapsing `connectionTimeout`/`sendTimeout`/`receiveTimeout`/`transformTimeout`/`connectionError`/`unknown` all into the same generic `NetworkException('No internet connection.')` — on a genuinely slow 3G connection (this app's explicit CLAUDE.md target), a request that was still in flight past 15s was indistinguishable from one that never had a connection at all, and both the feed and the sign-in sheet surface `ApiException.message` directly with no cache fallback for either. Raised timeouts to 30s connect / 30s send / 45s receive, and split the mapping into distinct types: `RequestTimeoutException` (not `TimeoutException` — that collides with `dart:async`'s own class, and `checkout_screen.dart` imports both) for the four timeout variants with a "connection is slow" message, `TlsException` for `badCertificate`, and a new `UnknownException` that surfaces the real underlying error text (`error.error`/`error.message`) instead of a generic line — only genuine `connectionError` (DNS failure/connection refused/no route to host) still maps to `NetworkException`. `ProductRepository`'s cache-fallback condition now also triggers on `RequestTimeoutException`, since a timeout is just as good a reason to show cached results on 3G as a hard offline state. Hardened `isOnlineProvider` itself against a real Android 16 `connectivity_plus` quirk (reported: misreports when both Wi-Fi and mobile radios are active) — switched from `!results.contains(none)` to `results.any((r) => r != none)`, so a stale spurious `none` entry alongside a real connection type no longer flips the (purely advisory, still never a gate) banner to offline. New regression test (`connectivity_does_not_gate_requests_test.dart`) proves the repository layer attempts and completes a real request while `connectivityProvider` reports offline, and that a genuine connection failure still maps to a distinct, catchable `NetworkException` — building it surfaced a real, non-obvious Riverpod 3.x gotcha along the way: `container.read(streamProvider.future)` on a provider with no active listener never resolves in a plain test environment (hung indefinitely, reproduced and isolated via a bisection down to a minimal repro), while `container.listen(provider, ...)` followed by a couple of zero-duration pumps reliably does — used the latter, and wrote the test as plain `test()` rather than `testWidgets()` since it exercises no widget tree. `flutter analyze` clean, all 11 Flutter test cases pass. Rebuilt and re-verified `app-prod-release.apk` against the real release keystore.
- 2026-08-18 — **Admin panel rebuild, Sections 2-3 + cross-cutting infrastructure.** Branding: measured real WCAG contrast ratios via Filament's own `Color::calculateContrastRatio()` rather than guessing — confirmed every shade 50-600 of raw `#FAC902` fails AA text contrast against white (max 3.92:1 at 600, need 4.5), matching the exact bug class already fixed once in the Flutter app. Built a custom 11-shade primary palette (50-500 stay true brand yellow for backgrounds/badges/buttons; 600-950 blend toward Sokoni near-black `#0A0A0A` instead of Filament's default same-hue darken, verified 700+/600+ pass AA text-on-white at 6+:1) and set the panel's `gray` to `Color::Neutral` (`#0a0a0a` at 950 — an exact match for CLAUDE.md's dark-mode surface, vs. Filament's default blue-tinted `#030712`). Wired `brandLogo`/`favicon` to the real Sokoni mark (copied into `public/images/brand/`) and set explicit `navigationGroups()` ordering (Overview → Catalog → Commerce → Community → Trust & Safety → System) so it doesn't fall back to alphabetical. Renamed the seller-profiles nav item to "Seller Verification". Built three pieces of infrastructure before any new resource, so every resource built from here on wires into them from the start rather than a later retrofit: (1) **Roles** — a nullable `users.role` enum (`admin`/`staff`), `User::isAdminRole()`/`isStaffRole()`, a real `UserPolicy` gating all of UserResource's standard CRUD (Filament checks these automatically per-request, not just at nav-render time — proved with a feature test hitting `/admin/users` directly as Staff and getting a real 403). (2) **Activity Log** — a plain `activity_logs` table (causer/action/subject-morph/reason/meta) plus `App\Support\ActivityLogger::record()`, wired into every existing moderation action (seller verify/reject, report uphold/dismiss/hide/warn/suspend/ban) as they were touched, not left for later. (3) **Settings** — a `settings` key/value table plus `App\Support\Settings`, reading DB overrides with `config/sokoni.php` fallbacks, wired into four *real* enforcement points: `ProductMediaController`'s per-product media cap (was a hardcoded `const MAX_ITEMS = 8`), `StoreOfferRequest`'s duration ceiling (was `between:1,7`), `ReportObserver`'s auto-hide threshold (was `Report::AUTO_HIDE_THRESHOLD`), each proven with a test that changes the setting and checks the *behavior* changed, not just that a row got saved. The fourth requested setting (default search radius) is genuinely stored/editable but deliberately **not** wired into `ProductController`'s request handling — traced the Flutter client's actual contract first and confirmed it always sends an explicit `radius_km` (defaulting to 5 client-side) and only omits it when the user explicitly picks "All"; the server currently can't distinguish "client didn't say" from "user chose All" (both arrive as absent), so adding a server-side default would silently override a real user choice. Logged as a deliberate scope boundary, not an oversight — see DECISIONS.md. Admin-only pages (Settings, User management) enforce via `canAccess()`/`UserPolicy` checking `isAdminRole()`, proven with a Staff-vs-Admin pair of tests for each. `php artisan test`: 158/158 passing (8 new).
- 2026-08-18 — **Admin panel rebuild, Section 4 — every missing resource built.** **Products** (Catalog): thumbnail/title/shop/category/price/stock/status/views list, filters for category/seller/status/sponsored/price-range, search by title *and* shop name (both columns marked `->searchable()`, Filament's global search covers both), row actions view/hide/unhide/feature, bulk hide and bulk category reassignment, detail page with media gallery/order history (via a new `orderItems` repeatable entry)/reports (added `Product::reports(): MorphMany`, a real relation the model was missing). **Media** (Catalog): every uploaded photo/video browsable across the whole catalog, filterable by type, delete-with-log. **Orders** (Commerce): code/buyer/seller/total/status/date list, status/date-range/seller filters, detail page with item breakdown/delivery info/full status timeline/conversation summary — no create or edit page exists anywhere in this resource, and no field in it ever touches price/subtotal/total; the only mutation is `cancel`, gated on `Order::canTransitionTo('cancelled')` and going through the exact same forceFill path `OrderController::updateStatus` already uses. **Seller Verification rebuilt as a real dedicated queue page** (`App\Filament\Pages\SellerVerificationQueue`, not a resource) — shows every pending applicant's NIDA photo, licence link, map link and business details inline, oldest-waiting-first, with Verify/Reject (reason required, plain Livewire methods + `wire:confirm`, not a table row you click into) right there; `SellerProfileResource` stays live for deep-linking from Products/Media but is off the main nav now (`shouldRegisterNavigation(): false`) so there's exactly one "Seller Verification" nav entry as specified. **Reviews & Comments** (Community): both needed a real schema addition — neither had a hide/moderation flag at all (`is_hidden` added to both via migration), wired all the way through: `ReviewObserver` now excludes hidden reviews from the seller's public `rating_avg`/`rating_count` recalculation (not just the list), and both `ReviewController::forSeller`/`CommentController::index` exclude hidden content from the public API, each proven with a test that hides content and checks the *public-facing* result changed. Reviews have no direct product FK (CLAUDE.md's model: a review is of the seller via a completed order, which can hold several products) — surfaced the order's actual item(s) honestly (`ReviewProductSummary`) instead of inventing a field. Comments show parent/reply threading via the existing `parent`/`replies` relations, no schema change needed there. **Updates/Offers/Showcases** (Community/Commerce): list + moderate (a straight "Remove" action with a logged reason, not a soft-hide — matches their already-ephemeral nature, Updates already auto-expire in 24h); Offers compute a live Active/Upcoming/Ended status badge plus a human countdown (`isActive()`/`diffForHumans()`), not a static date. Found and fixed two admin-visibility gaps in existing policies while wiring all this up: `ProductPolicy`/`OrderPolicy` had `update`/`view` methods scoped only to the *owning* buyer/seller (correct for the app's own API) with no admin path at all — Filament would have 403'd every admin trying to view a product or order. Added `viewAny`/`view` methods checking `is_admin` alongside the existing owner checks, changing nothing about the API's own authorization. Also found `database/factories/CommentFactory.php` never existed despite `Comment::class` using `HasFactory` — added it. `php artisan test`: 191/191 passing (33 new).
- 2026-08-18 — **Admin panel rebuild, Sections 5-6 — a real dashboard and the remaining operational features.** **Dashboard**: `SokoniStatsOverview` rebuilt so every stat shows a real trailing-30-days-vs-prior-30-days trend, not just a total — and when the prior period is genuinely empty (division by zero, a real state on a young platform), shows "New" rather than a fabricated percentage. Added `OrdersOverTimeChart` (line, 30 days, zero-filled so a quiet day isn't just missing from the axis), `RevenueByCategoryChart` (bar, lifetime — a 30-day window would hide categories that only sell occasionally), `NewUsersAndSellersChart` (two real series), `TopSellersByRevenueTable` (a genuine `withSum` aggregate over each seller's completed orders, not a guess), `RecentOrdersTable`, and `PendingVerificationsTable`/`ModerationQueueTable` (each row links straight to the item — the queue page for verifications, the report's own view page for moderation). All auto-discovered via the panel's existing `discoverWidgets()`, sorted into stats → actionable lists → charts → tables via explicit `$sort`. **Global search**: wired `getGloballySearchableAttributes()` for Products (title + shop name via dot-notation), Shops, Users, and Orders (code + buyer/seller name) — and while verifying it end-to-end, found a real pre-existing bug affecting the *whole panel*, not just these four: not one resource (old or new) had ever set `$recordTitleAttribute`, so `getRecordTitle()` was silently falling back to the generic model label everywhere a record needed a display name — global search results, delete confirmations, breadcrumbs — every product showed as "product", every user as "user", indistinguishable from each other. Fixed on all six affected resources (Category, Report, SellerProfile, User, Product, Order). Caught via a test that asserted a search result's title matched the record's actual name and got back the model label instead — exactly the kind of thing that looks fine in a screenshot (a search *works*, returns *a* result) but is useless in practice. **CSV export** for Orders/Products/Users, each respecting the table's current filters (`getFilteredSortedTableQuery()`) — built as a plain synchronous `StreamedResponse` (`App\Support\CsvExporter`), deliberately *not* Filament's native async `ExportAction`, which dispatches to a queue; this app's production target is cPanel shared hosting with no queue worker, only a once-a-minute cron (`docs/DEPLOY.md`) — a queued export would sit in the `jobs` table forever, which is worse than no button. Same reasoning CLAUDE.md itself already applied to chat (polling instead of WebSockets) for the identical hosting constraint. Confirmed the mechanism is real, not just test-shaped, by tracing Filament's own `callMountedAction()` through to Livewire 3's native "an action can return a Response and it gets sent as the real HTTP response" behaviour, rather than assuming. Roles/Activity Log/Settings were already built as Section 2-3 infrastructure; empty/loading states and pagination are Filament defaults everywhere except the two dashboard list widgets and the verification queue, which got explicit custom copy. `php artisan test`: 212/212 passing (21 new).
- 2026-08-18 — **Admin panel rebuild complete — release deliverables.** Built `sokoni-api-update-3.zip` (86 files, 108KB) by reconstructing the actual deployed server state first — layered `sokoni-api.zip` (the original base install) + `sokoni-api-update.zip` + `sokoni-api-update-2.zip` into a merged baseline, then diffed the current working tree against *that*, not against the latest delta zip alone (diffing against just `update-2.zip` falsely flagged ~300 untouched files as "new" — it was only ever a 31-file delta, never a full snapshot). Result: 25 modified + 59 new backend files + the 2 new brand image assets, `.env` and the local SQLite/test-cache excluded. 5 new migrations, listed below in run order. **Filament's compiled JS/CSS bundle does not need republishing** — confirmed by inspecting both the published `public/css/filament/filament/app.css` (no hardcoded color values in it) and Filament's own base layout Blade template (`vendor/filament/filament/resources/views/components/layout/base.blade.php`), which injects `--primary-*`/`--gray-*` as inline `<style>` custom properties computed from `AdminPanelProvider`'s PHP config on every request — a color/branding change takes effect on the next page load with nothing to rebuild. **Two real static files do need uploading** (already included in the zip at `public/images/brand/`): `sokoni_logo.png` and `sokoni_logo_icon.png`, referenced by the new `brandLogo()`/`favicon()` calls — without them the sidebar logo and favicon 404 even though everything else about the branding works.
  - Migrations to run, in order: `2026_08_18_090000_add_role_to_users_table`, `2026_08_18_090100_create_activity_logs_table`, `2026_08_18_090200_create_settings_table`, `2026_08_18_090300_add_is_hidden_to_reviews_table`, `2026_08_18_090400_add_is_hidden_to_comments_table`.
  - `php artisan test`: 212/212 passing, final run before packaging. Flutter app untouched all session, per instruction — not re-tested since nothing there changed.
- 2026-08-19 — **Public website (sokoni.co.tz) built, all 9 sections.** Server-rendered inside the existing Laravel app on a new `web` session guard, sharing every model/business rule with the API and admin panel — no Flutter Web, no SPA. Home (hero search + region picker, category grid with live counts, Near You/Offers/featured shops/latest rows), category + search results (`/c/{category}/{child?}`, `/search`) with real filters (price/condition/region/verified/video/sponsored), sort, sponsored-pinned-and-labelled results, pagination and an illustrated empty state; the product page (`/p/{id}/{slug?}`) with a lightbox gallery, offer countdown, seller card, Message/WhatsApp/Call-reveal (logged as a `Lead`) actions, login-gated ordering, and full schema.org Product + Open Graph/Twitter markup; the shop page (`/@handle`) with cover/logo/rating distribution/opening hours (new `seller_profiles.opening_hours` JSON column — see DECISIONS.md for why the app-side editor is explicitly not part of this task) and LocalBusiness schema.org markup; phone-OTP + optional Google auth reusing the same `SmsGateway`/terms-version machinery as the app, plus a post-signup buy/sell intent question; a full account area (orders, saved items, polling-based messages, settings, and a seller shop dashboard with product create/edit); static/trust pages (About, How it works, Safety, Terms, Privacy, Contact, Sell); a split sitemap (`/sitemap.xml` + products/shops/categories) and `/robots.txt`; and a performance pass (lazy-loaded/srcset images, cached category counts and home data, gzip + far-future cache headers added to `public/.htaccess`). Three services extracted out of the API's controllers so both surfaces share one implementation (`ProductSearchService`, `SocialUserResolver`, `SellerDashboardStats`), confirmed non-regressive by re-running the full API suite after each extraction. `php artisan test`: 250/250 passing (38 new). New migrations, in order: `2026_08_19_080000_add_opening_hours_to_seller_profiles_table`, `2026_08_19_090000_create_leads_table`. Four items explicitly deferred per the task's own scope boundary (new listing verticals, paid-placement billing, banner ads, AI search) — see DECISIONS.md. `docs/DEPLOY_WEB.md` and the upload zip are next.
- 2026-08-20 — **Production incident response, all 3 parts.** Part 1: audited every `Cache::remember`/`put` call site in the codebase (only 4 exist; the 2 caching Eloquent models were already the prior hotfix, the other 2 cache plain strings, confirmed safe); built `AllWebRoutesAvoidServerErrorsTest`, a real regression guard walking every registered `web.*` GET route via the router itself, which immediately caught a genuine bug — `/account/messages/{conversation}` was 500ing in every environment, root-caused to Laravel's `@json()` Blade directive naively splitting its argument on every comma (no paren/bracket awareness), silently truncating a multi-key array literal into invalid compiled PHP — fixed. The originally-reported `/c/food-groceries` 500 was not reproduced locally despite exhaustive attempts and is logged as genuinely unresolved rather than guessed at, with a concrete next step (read `storage/logs/laravel.log` on production). Part 2: found and fixed the real root cause of the reported layout breakage — `app.css`'s `@theme` block only named 7 of the many spacing/sizing numbers actually used across the site, leaving every other one (`py-14`, `py-40`, `h-24`, ...) silently resolving at Tailwind's default 4x-larger scale instead of the intended 1px-per-unit; overriding the base `--spacing` multiplier directly fixed oversized inputs, horizontal overflow, and the "nothing below the hero" report (confirmed via a strengthened `HomePageTest`, not just assumed) in one change, plus two explicit-pixel-value fixes for the sidebar/offer-card widths that had been *accidentally* correct under the old bug. Fixed the duplicated search bar (header's compact search now hides on the home page, where the hero search is the dominant element). Part 3: implemented the highest-leverage pieces of the full design-system brief — the typography scale, text-hierarchy colors, product card hover/offer-badge treatment, a WhatsApp-green button class, a sticky product-gallery + mobile bottom sticky bar, a real 4-column dark footer, branded 404/500 error pages (previously bare Laravel defaults), a trust band, "See all" links with horizontal-scroll-on-mobile on every home section, sticky header shadow, fade-masked category strip with active-state highlighting, and `prefers-reduced-motion` support — while explicitly deferring the desktop mega-menu, a fully spec-matched mobile search icon, and a deeper category/search/shop page restyle, logged as deferred rather than silently skipped. `php artisan test`: 256/256 passing (6 new/strengthened this session). Vite assets rebuilt; a minimal update zip covering only the changed files is next.
- 2026-08-20 — **Second production incident round, all 3 parts.** Part 1: `/c/food-groceries` still doesn't reproduce locally after testing every named candidate (real children, a live sponsored product, the full real 10-category set) as real permanent tests, not one-off checks — status stays honestly **open**. Shipped the tools to close it without SSH instead: a token-gated `/_diagnostic/log` route tailing `storage/logs/laravel.log`, and a distinctive `[CATEGORY_500_DIAGNOSTIC]` log marker wrapping `CategoryController`'s body (logs and re-throws, no behavior change). Both marked TEMPORARY with exact removal instructions. Part 2: category icons were printing raw Material Symbols names (`devices`, `checkroom`, ...) as visible text — the website has no icon font. Built `<x-category-icon>`, mapping all 10 real seeded icon names to hand-authored inline SVGs plus a generic fallback, the only place `icon` was ever printed raw. Part 3: standardized card-grid gaps to 16px/24px sitewide (found one real inconsistency), reinforced equal-height cards; built a full banner system — `banners` table, `Banner` model, a Filament `BannerResource` under a new Marketing nav group with image upload/scheduling/activate-deactivate, a reusable `<x-banner-slot>` that renders at most one banner per position and collapses to nothing when empty, server-side impression counting and a click-redirect route — restrained to one `home_hero` + one `home_mid` slot on the home page plus `category_top`/`sidebar` slots on category/search pages; a real bug in the banner form (editing without re-uploading always failed validation) was caught by its own test and fixed before shipping; replaced the EN/SW text toggle with a proper language dropdown; logged the no-currency-switcher reasoning in DECISIONS.md as instructed (TZS-only marketplace, no FX infrastructure, a display-only converter would be the honest scoped version if ever requested); moved the category strip into its own dark two-tier secondary bar, Amazon-style. `php artisan test`: 277/277 passing (34 new this round). New migration: `2026_08_20_100000_create_banners_table`. Vite rebuilt; update zip next.
- 2026-08-22 — **Tester feedback round (live at beta.sokoni.co.tz), all 3 parts.** Part 1: built the missing buyer/seller registration flow — testers could reach the intent screen but "sell" led to a dead-end static marketing page with no real form. `SellerWebRegistrationRequest` composes its rules directly from the app's own four onboarding-step requests (no duplicated validation logic); `SellerRegistrationController` writes the same `SellerProfile` fields, in the same order, as the app's API — same `pending` status, same manual-review queue. No interactive map picker (deliberately out of scope — see DECISIONS.md); region/district/address plus a best-effort server-side geocode via OpenStreetMap's free Nominatim API, which never blocks submission on failure. Caught and fixed a real bug before shipping: `validated()` omits a key entirely when the field is completely absent from the submission (not just empty), which every `nullable` lat/lng submission from this map-less web form triggered — fixed with null-coalescing assignment after validation. "Start selling" added to the account nav (previously buyers had no path to registration from their own menu at all). Part 2: shop page now shows the full address prominently in the header (was buried below) plus a "member since" year; the Google Maps directions link (no API key, none coming) was replaced with a lazy-loaded OpenStreetMap iframe embed bounded tightly around the shop's own coordinates, shown only when a location is actually set, plus a `geo:` URI link with a plain-web fallback for directions. Part 3: replaced the header's ad hoc link collection with the 5 named nav destinations (Home/Stores/Explore/Chats/Profile) — desktop in the header row, mobile as a new fixed bottom nav bar matching the app's own. The standalone Sign-in/My-account button was removed outright — Profile now serves as the single account entry point. Chats and Profile sit outside the site's usual auth-gate entirely so a signed-out visitor sees an explanatory sign-in prompt instead of an immediate redirect, with a closed-allowlist (not raw-URL) return-to-where-you-were mechanism after signing in, proven safe against open-redirect abuse by a dedicated test. Stores (`/stores`) is a genuinely new shop directory page — search, region/category filters, rating/newest/listings sort, paginated. Explore (`/explore`) reuses the existing search/filter service unchanged. Unread-message badge on Chats via a plain per-request count, only queried for signed-in visitors. `php artisan test`: 301/301 passing (34 new this round). No new migrations. Vite rebuilt; update zip next, noting beta's separate document root (`/home/sokoftsn/beta.sokoni.co.tz`) needs its own copy of `build/` alongside `sokoni-api/public/build`.
- 2026-08-23 — **Demo content, Part 1 complete — `DemoSeeder` / `php artisan demo:seed {--fresh}`.** 12 real-sounding Dar es Salaam shops (8 verified/3 pending/1 rejected) across genuine districts with bilingual bios, opening hours and WhatsApp numbers; 60 products with Tanzanian-market-anchored pricing and bilingual descriptions; live offers, updates, showcases, mixed-rating bilingual reviews with seller replies, product comment threads, and orders across every status. Added `seller_profiles.bio_sw`/`products.description_sw` columns with locale-aware `localizedBio()`/`localizedDescription()` accessors (see DECISIONS.md for the Eloquent method-name-collision bug this caught), generated inline-SVG shop logos and category-placeholder product images (flagged as placeholders needing real photography before launch), and a fixed handle/phone-based `--fresh` cleanup proven never to touch a real account. `php artisan test`: 309/309 passing (8 new).
- 2026-08-23 — **Demo content, Part 2 complete — dead links and placeholder-URL cleanup.** Social links and the Google Play badge/app-download banner are now `config('sokoni.*')`-backed and hidden entirely when unset rather than pointing at fake URLs; the App Store link was removed outright (no iOS build exists). `support@sokoni.co.tz` made configurable but kept visible by default (it's the client's real domain). Added a `NoindexBetaHost` middleware sending `X-Robots-Tag: noindex, nofollow` gated on `config('sokoni.beta_hostname')`, deliberately not a `robots.txt Disallow`, so it stops applying automatically once the site serves from the production domain. `php artisan test`: 318/318 passing (9 new).
- 2026-08-24 — **App bug fix, Part 1 complete — session now survives an app restart.** Root-caused to three compounding issues: `AuthStateController`'s 5s timeout collapsing any read failure into "signed out", `flutter_secure_storage`'s Android write path using `apply()` not `commit()` in every version (confirmed via source, not just 9.2.4), and `AuthInterceptor` actively wiping a valid token on a 401 caused by a purely local read glitch rather than a real server rejection. `AuthState` is now a 4-value status (`loading`/`authenticated`/`unauthenticated`/`unknown`) with a 10s last-resort safety net that fails to "unknown, retry" — never "signed out" — and the interceptor only clears a session when a token it actually sent was rejected. `flutter analyze` clean, `flutter test`: 22/22 passing (11 new).
- 2026-08-24 — **App feature, Part 2 complete — Instagram-style double-tap to Save.** New `DoubleTapToSave`/`SpringOnTrue` motion primitives wired into the For You feed, product detail gallery, and Showcase video feed (which gained its first-ever Save affordance); double-tapping always saves (never unsaves) via a new idempotent `saveProductViaDoubleTap`, with optimistic fill + quiet toast-on-failure revert. `flutter analyze` clean, `flutter test`: 30/30 passing (8 new). Signed `app-prod-release.apk` rebuilt (71.5MB); no backend changes this round, so no API update zip.
- 2026-08-25 — **App bug fixes + sign-up restructure, all three parts complete.** Part 1: every guest-attemptable action (save, follow, message, order) now invites sign-in via a new `showSignInPrompt` sheet instead of showing a wrong or missing error — audited and fixed favorites, follow, both chat entry points, and checkout's guard. Part 2: confirmed the "returning user asked for a name" report was cosmetic (server already found the right row every time — `users.phone` unique + consistent E.164 normalisation both sides), then rebuilt the "Sign in" path as its own flow (`sign_in_sheet.dart`) that never shows a name field for a returning number. Part 3: full sign-up restructure — a new entry chooser (`auth_entry_sheet.dart`), and a new 4-step "Create an account" wizard (intent → details → terms → verify, phone verified last) with live phone/handle validation and a fully-local persisted draft (`create_account_providers.dart`) so a dropped connection never loses a part-filled form. New backend: `POST /auth/check-phone`, `GET /sellers/handle-availability`, `POST /auth/register` (creates the User and, for a seller, the SellerProfile atomically, then hands off to the *existing* onboarding wizard's Identity step for NIDA/licence — no new upload UI built). `php artisan test`: 335/335 passing (15 new). `flutter analyze` clean, `flutter test`: 48/48 passing (18 new this round). Signed `app-prod-release.apk` rebuilt; backend update zip produced (built with Python's `zipfile`, not `Compress-Archive`).
- 2026-08-26 — **Production incident fix: `/auth/check-phone` 404 (deploy issue, diagnosed) + a dead Next button (real client bug, fixed).** The 404 traced to `routes/api.php`/`AuthController.php`/`SellerProfileController.php` all being pre-existing server files that needed overwriting during extraction — not a code defect. Separately found and fixed the actual bug the incident exposed: Step 2's live phone/handle checks had no timeout or error handling, so any failure left `_CheckStatus.checking` stuck forever and silently blocked the Next button with zero explanation — the second time a validation check has trapped a user in this app. Both checks now fail open (new `_CheckStatus.unknown`, bounded 6s timeout) — a broken or slow check can never again disable Next; only a *confirmed* problem does. `flutter analyze` clean, `flutter test`: 51/51 passing (3 new). No server-side code changed this round, so no new backend zip.
- 2026-08-26 — **Real SMS provider wired up: Africa's Talking replaces Beem as the default.** The client's actual account was confirmed as Africa's Talking (app `skyfar`), not Beem — built `AfricasTalkingSmsGateway` against their SMS v1 endpoint (`apiKey` header, form-encoded body), correctly treating HTTP 201 as "accepted for processing" rather than "delivered" and parsing the real per-recipient `status`/`statusCode` instead, with `InsufficientBalance` logged at `critical` (every other failure at `error`) since that's the single most common way prepaid SMS silently stops working. Every request/response now goes to a dedicated daily-rotating `sms` log channel, diagnosable from cPanel File Manager alone with no shell access. `SmsGateway::sendOtp()` gained an optional `$locale` parameter for bilingual message text — the website passes its own reliable `SetWebLocale` value through today; the mobile API accepts an opt-in `locale` field defaulting to English, since the Flutter client doesn't send it yet (a documented, deliberate gap, not an oversight). Gateway selection is now explicit (`SMS_DRIVER=africastalking|beem`, unset/unrecognised → `LogSmsGateway`) rather than sniffed from which credentials happen to be set; Beem is kept, not deleted, as a clearly-labelled non-default alternative. The existing OTP rate limiter's window widened from 10 to 15 minutes to match the requested 3-per-15-minutes exactly, protecting the client's prepaid SMS credit as much as the phone. `php artisan test`: 350/350 passing (15 new). Deploy zip built with Python's `zipfile`: 1 new file (`AfricasTalkingSmsGateway.php`), 10 modified. `docs/SMS.md` rewritten in full. Flutter app untouched this round.
- 2026-08-27 — **Second SMS provider swap: Textify Africa is now the active driver, Africa's Talking kept as a fallback alongside Beem.** Built `TextifySmsGateway` (JSON POST, bearer-token auth, local-format `0XXXXXXXXX` numbers converted from the app's stored `+255...` at the boundary and always logged both ways) without touching `AfricasTalkingSmsGateway`/`BeemSmsGateway` — `SMS_DRIVER` now supports `textify|africastalking|beem`, defaulting to `textify` in `.env.example`. `success: false` in the response body is treated as a failure regardless of HTTP status (a 401 and a 200 can both carry it), `invalid_token` is logged at `critical` (every other failure at `error`) since a bad key silently stops all sending, and a response with no `success` field at all is treated as malformed rather than coerced into a default. `docs/SMS.md` restructured so the driver-agnostic Logging/Message-text/Rate-limiting sections sit at the top level instead of nested under one provider. `php artisan test`: 358/358 passing (8 new). Deploy zip built with Python's `zipfile`: 1 new file, 2 modified. Flutter app untouched this round.
- 2026-08-30 — **Beta tester feedback round complete — 2 bugs + 8 design fixes.** Bug 1: product upload's "only one image" was a discoverability/UX defect, not a server limit — the backend already accepted several files. Replaced the bare file input with a real AJAX photo manager (drag-and-drop, client-side compression to the same 1600px/0.8-quality target `ImageVariants` uses server-side, real per-file XHR progress, thumbnail previews with remove, native drag-to-reorder that sets the cover photo) via a new web-guard `ShopProductMediaController` mirroring the API's own `Api\ProductMediaController`. Product creation now redirects to the edit page ("now add your photos") rather than the list, matching the Flutter app's own existing two-step pattern. Bug 2: gallery flicker traced to a real site-wide latent bug — `x-cloak` had no CSS definition anywhere (`[x-cloak]{display:none}` was simply missing from `app.css`), so every `x-cloak`'d element on the whole site had been rendering visible-then-hidden on every page load; fixed the missing rule, cloaked every gallery slide but the first, and converted slide-switching to an opacity crossfade. Design fixes: category strip counts removed (item 3); the dark category bar's white-corner artefacts traced to a `mask-image` fading the bar's own background, fixed by replacing the mask with layered gradient overlays (item 4); header/footer logos enlarged 32px→40px (items 5, 8); home hero rebuilt with a dark gradient + a small inline-SVG brand-palette pattern (no real market photography exists in the repo — used the task's own documented fallback) and white text (item 6); "Use my current location" added to seller registration via browser geolocation + client-side Nominatim reverse-geocoding, matching the server's existing free-geocoder choice (item 7); the home trust band gained inline SVG icons plus a real one-line description per item, bilingually (item 9); the About page's hardware-constraint closing line replaced with a market-focused one in both languages (item 10). `php artisan test`: 367/367 passing (11 new). Vite rebuilt. Deploy zip built with Python's `zipfile`, two top-level folders (`sokoni-api/`, `beta-docroot/`) since beta's document root is physically separate and needs its own copy of `build/`. No new migrations. Flagged to the user: `seller-register.blade.php` has no git history and the one prior zip that seems to include it had backslash path separators — genuinely uncertain whether it's live on beta, so it's called out to upload individually regardless of what Extract reports.
- 2026-08-30 — **Production data cleanup tool: `setup.php` gained `cleanup-original-seed`.** Removes only the Phase 2 Faker `DatabaseSeeder` content (20 shops) still live alongside `DemoSeeder`'s 12, which was outranking them on the home feed. Identifies candidates positively, not by exclusion: a seller matches only if both its `nida_image` and `licence_file` paths carry the literal `seed/` prefix `SellerProfileFactory` hardcodes, distinct from real uploads (`sellers/...`) and `DemoSeeder` (`demo/...`) — confirmed by grepping the whole app that no other code path produces that prefix. Refuses to run if a match overlaps a real `DemoSeeder` handle or an admin account (should be structurally impossible, checked anyway). Dry run by default; `&confirm=1` executes inside a transaction. Verified end to end against a disposable isolated SQLite copy (never the real dev or production DB): 20 candidates identified correctly out of 32 total sellers, zero false positives, and after deletion the counts matched `DatabaseSeeder`'s own documented totals exactly (−70 orders, −60 reviews). Deliberately doesn't touch the original seeder's 40 anonymous fake buyer accounts — not named in the request, no safe structural marker of their own, and harmless leftovers that don't affect shop rankings.
- 2026-08-30 — **Two homepage bugs fixed, both root-caused before touching code.** "Latest listings" was rendering ascending id order instead of newest-first — confirmed directly against the dev database that dozens of products share an identical `created_at` second (bulk seeding has no sub-second precision) and that `Product::latest()` alone has no tiebreaker for those ties, which is exactly what produced the reported ordering. Fixed with an explicit `->orderByDesc('id')` tiebreaker. "Browse categories" no longer shows a raw product count (a `0` reads as "this marketplace is empty") and hides any category with zero currently-visible products from that section entirely — scoped to the home page's own filtered copy of the category list only; `CategoryCatalogService::withCounts()` itself is untouched so the header nav and direct `/c/{category}` links still resolve a temporarily-empty category correctly. All three new tests were verified to actually fail against the pre-fix code before being accepted, which caught two real mistakes in the first draft of two of them (a `lat`/`lng` test-isolation gap and a wrong-assumption assertion) — see DECISIONS.md. `php artisan test`: 370/370 passing (3 new). Flutter app untouched; no new migrations; no deploy zip needed for this round yet (pending explicit request).
- 2026-09-03 — **Client/tester feedback round, Part A (website bugs) complete — 7 items (A1-A7).** Fixed the literal `@{{ }}` Blade-escape bug on the shop page and admin verification queue; built the website's first real conversation-start flow, review submission, and report flows (message-level reporting and full comments deferred as out-of-proportion for this round — see DECISIONS.md); unified flash messaging into one shared partial (fixing a real one-hop-too-early session-flash gotcha found while testing); shop logo upload with client-side compression; a first-ever opening-hours editor; mobile header wordmark/tap-target fixes. `php artisan test`: 400/400 passing (27 new). See DECISIONS.md for full detail.
- 2026-09-03 — **Client/tester feedback round, Part B (app bugs) complete — 6 items (B1-B6).** B1 was three compounding bugs (SVG rendering via a new `SokoniNetworkImage`, stale baked-in media-host URLs now fixable via `setup.php`'s `rewrite-media-host`, and Update/Showcase media paths that were never full URLs at all); added the missing app-bar brand mark (B2); replaced Google Maps with `flutter_map`/OpenStreetMap everywhere, needing no API key or billing (B3), closing that BLOCKERS.md item by removal; found and added entirely-missing Android/iOS location-permission declarations, the real root cause of B4; fixed a codebase-wide validation-message bug in `mapDioError` that was showing Laravel's generic 422 message instead of the real field error (B5); added a persistent delivery/payment notice to the order detail screen (B6). `flutter analyze` clean, `flutter test`: 62/62 passing (11 new). See DECISIONS.md for full detail.
- 2026-09-02 — **`DemoSeeder` made genuinely self-healing for product/Update/Showcase media, not just idempotent.** Root cause: the old guard checked "does a media *row* exist," not "does the *file* it points to exist" — a row surviving a storage reset/move (files gone, database intact) meant the file was never rewritten, while shop logos were fine because that path unconditionally rewrites every run. Fixed with a new `ensureFileExists()` helper: every product/Update/Showcase's disk path is deterministic, so it's checked directly against the disk on every run, independent of row state. Found and fixed a second, separate bug this surfaced along the way — Updates and Showcases never had any file-writing code at all, ever, so their media paths were always dangling; built that for the first time reusing the existing SVG placeholder generator, and corrected both paths' extension from `.jpg` to `.svg` to match the actual content type (a mismatched extension gets served with the wrong Content-Type and silently fails to render). `Showcase.video_path` stays deliberately, explicitly unfixed — no video-generation capability exists anywhere in this codebase, and faking bytes at a `.mp4` path would be a more deceptive failure than a visibly-missing file. All three new tests verified to actually fail against the pre-fix code first. `php artisan test`: 373/373 passing (3 new). Deploy zip next.
- 2026-09-03 — **Client/tester feedback round, Part C, C1-C5 complete.** C1: category renames done via a raw-SQL migration (not the usual name-keyed seeder pattern, which would have orphaned FKs), plus Real Estate/Kids added and old-slug redirects so no existing link 404s. C2/C3: home search now matches shop names alongside products, and the search sidebar gained a category tree with in-result counts. C4 (NIDA-only verification): business licence is now optional everywhere (web form, API, and the Flutter onboarding wizard's step 4, which now reads "Skip" when nothing's selected) — NIDA remains the actual basis of verification, confirmed by reading `Api\SellerProfileController::updateIdentity()` rather than trusting a stale docblock claim. C5: real transactional email — SMTP config, `MustVerifyEmail` wired up with a bilingual verification notification, optional email at the app's registration step and on both web/app profile settings, order-placed/status-changed and review-received emails alongside the existing push notifications, all sent synchronously and fail-safe (`SafeMail`) since this host's queue has no worker. `php artisan test`: 444/444 passing (11 new for C1-C4, 21 new for C5). `flutter analyze` clean, `flutter test`: 69/69 passing. New migrations: category rename/add, `add_email_verified_at_to_users_table`. See DECISIONS.md for full detail on each item.
- 2026-09-03 — **Whole A-C feedback batch shipped: Vite rebuilt, signed `app-prod-release.apk` rebuilt (73.8MB, verified against the real release keystore — V2 signer CN=Sokoni), deploy zip built with Python's `zipfile`.** The zip (`sokoni-api-2026-09-03-abc-feedback-batch.zip`, 188 entries) is scoped to everything different from the last real git commit (`f3237d4`) — 78 modified files (individual-upload list in `DEPLOY_MANIFEST_2026-09-03.md`, since cPanel's Extract skips existing files) and 96 new ones, including 7 compiled `public/build/` assets written twice (`sokoni-api/public/build` and `public_html/build` — two separate physical docroots per docs/DEPLOY_WEB.md, `public_html` now being the live site). A handful of "new" files predate this batch (created in earlier, never-committed rounds) and are flagged `[possibly already live]` in the manifest rather than assumed either way. 7 migrations to run in order, newest three being this round's own (`add_email_verified_at_to_users_table`, `add_marketing_consent_to_users_table`, `create_sms_blasts_table`).
- 2026-09-03 — **Subcategory taxonomy, seller subcategory picker (web+app), and a server-rendered mega menu with a cached tree.** `CategorySeeder` gained ~76 subcategories across 12 of 13 top-level categories (idempotent, "Other" deliberately excluded), plus a `seed-categories` step in `setup.php` to backfill production without SSH. Found and fixed a real latent bug this exposed: `Product::scopeInCategory()` was an exact `category_id` match, so browsing a parent category would never have shown a product tagged with one of its own subcategories — fixed to expand a parent id to itself + its children. Sellers can now optionally pick a subcategory when posting a product (defaulting to the parent) via a cascading two-select UI on web and two new derived Riverpod providers on the app, with no schema change needed. The header's category bar is now a full mega menu — hover/tap opens a full-width subcategories panel (150ms open delay, keyboard/Escape/outside-click accessible, a mobile accordion drawer instead of hover) — entirely server-rendered for indexability, with the tree cached as a plain array specifically to avoid repeating the project's earlier `__PHP_Incomplete_Class` cache incident. Verified the C3 search sidebar against the real seeded taxonomy and proved drill-down preserves every other active filter, both with tests confirmed to fail against a reverted implementation first. `php artisan test`: 488/488 passing (20 new). `flutter analyze` clean, `flutter test`: 71/71 passing. No new migrations. See DECISIONS.md for the click-outside race-condition bug caught before shipping.
- 2026-09-03 — **Client/tester feedback round, Part C, C6 (admin bulk SMS) complete — the whole A-C batch is done.** Built a Filament "Bulk SMS" page (Marketing nav group) that composes a message, previews live recipient count/character/unit estimates for an audience (all verified sellers / all buyers / everyone, optionally consenting-only via the new `marketing_consent` field), confirms, then snapshots matching recipients into new `sms_blasts`/`sms_blast_recipients` tables rather than sending inline — a new `sms:process-blasts` command, cron-scheduled every minute, sends one throttled batch at a time until each blast completes, the same "no queue worker but cron exists" pattern already used for `updates:delete-expired`. `SmsGateway` gained a `sendMessage()` method (all four drivers) reusing each one's existing transport/logging, so per-recipient logging to the `sms` channel falls out for free. A hard cap (`Settings::maxSmsBlastSize()`, admin-configurable, default 500) and the audience/cap are both re-checked at actual send time, not just at preview. `marketing_consent` (opt-in checkbox) was added at signup on both web (phone-OTP form) and app ("Create an account" Step 3, beside the Terms checkbox) — unlike C5's optional email, a single checkbox was judged low-friction enough to include on the fast web signup form too. `php artisan test`: 467/467 passing (23 new). `flutter analyze` clean, `flutter test`: 71/71 passing (2 new). New migrations: `add_marketing_consent_to_users_table`, `create_sms_blasts_table`. See DECISIONS.md for full detail, including the `#[Fillable([])]`-vs-`forceFill()` bug the first draft's command hit and how it was fixed.
- 2026-09-08 — **Second client testing feedback round, Parts A-D complete (28 items) plus final deliverables.** Part A (7 website bugs, all re-diagnosed from scratch rather than assumed): a review-gate explanation instead of a blank tab, a permanent `.skeleton` pulse class removed from product cards (the real A2 cause, unrelated to the earlier x-cloak fix), real upload progress added to the shop-logo uploader, a live-polling unread-message badge/toast reaching desktop for the first time, push+email on a new review, an A6 category-list mismatch traced to a missing `whereNull('parent_id')` filter (and audited site-wide, catching a second instance), and the verified badge recolored to match the app. Part B: NIDA-photo and business-licence fields removed entirely (web, app, admin, factories, site copy, six lang keys) via non-destructive `SoftDeletes`-free column retention; "Other"-last on the homepage verified already-correct rather than blindly patched; a real favicon set replacing a 0-byte `favicon.ico`. Part C: Google OAuth found code-complete (a real `isConfigured()` consistency bug fixed along the way) with exact Google Cloud Console values documented in `BLOCKERS.md`; admin soft-delete for products/users with cascade, required reason, activity log, and a 30-day media-purge command; a generic `product_attributes` table powering Cars' Make/Model on web, API, and app, built for Real Estate to reuse later. Part D: `SafeArea` added to the bottom nav and (more consequentially) to the one shared bottom-sheet primitive every sheet in the app uses; product-photo SVG/URL handling re-verified sound (one real `errorBuilder` gap closed) with the actual live-database fix logged as a `BLOCKERS.md` action item; opening hours and an OpenStreetMap preview added to the app's shop profile (the API simply never sent the field before); the subcategory picker rigorously tested from four angles with no reproducible defect found; the splash screen rebuilt into a ~1.8s brand moment (larger mark, glow, letter-by-letter wordmark) with navigation kept strictly independent of the animation; a new app icon processed from a transparent, non-square source onto a proper solid-white 1080×1080 canvas and regenerated across every Android/iOS density plus the adaptive icon — surfacing and fixing one unrelated dead build target (`DiagnosticMainActivity.kt` still referencing the `google_maps_flutter` plugin class removed two rounds ago). `php artisan test`: 536/536 passing (79 new this round). `flutter analyze` clean, `flutter test`: 90/90 passing (19 new). Vite rebuilt (no CSS/JS content changed — hashes identical). Signed `app-prod-release.apk` rebuilt (70.5MB, verified against the real release keystore — V2 signer CN=Sokoni, package `tz.co.sokoni.sokoni`). Two deploy zips built with Python's `zipfile` (`sokoni-api-2026-09-08-app-root.zip`, `sokoni-api-2026-09-08-public-docroot.zip`) per `DEPLOY_MANIFEST_2026-09-08.md`, which also flags an assumption about the beta docroot's folder layout for the user to confirm. 2 new migrations. The host's `C:` drive hit 0 bytes free mid-session (unrelated to this repo — the user's own `Downloads` folder); the user freed space directly rather than have anything deleted for them.
- 2026-09-09 — **Third client testing feedback round, Part A verified (no code needed), Part B in progress — B1-B4 and B6 done, B5 verified already fixed. Stopped by explicit user request before B7; see "Where this stops" below.**
  - **Part A (NIDA photo + licence removal) was re-checked line by line against every checkpoint the client listed and found genuinely already complete** — this is the entire 2026-09-08 batch below, which had never actually reached the live server (176 files sat as uncommitted local changes this whole time, confirmed by `git status` at the start of this round). No new code; committed as-is.
  - **B1 (photos not showing)**: built `setup.php?step=diagnose-media`, a real diagnostic — for a sample of `product_media` rows, prints the stored URL, the resolved disk path under the currently-configured root, and `file_exists()` against both `public_html/uploads` and `beta.sokoni.co.tz/uploads` explicitly, regardless of current config. **Blocked on the user running it against the live server and reporting back** — this session has no server access, so the actual fix (whichever of "stale URL," "files in the wrong folder," or something else it reveals) is not yet applied.
  - **B2 (homepage flicker)**: re-diagnosed live. The skeleton-pulse fix was already shipped (per Part A's finding above) and confirmed correct; the actual remaining gap was the "Near you" section — the first product grid on the page — defaulting every card to `loading="lazy"`, deferring the very photos visible on load. First 4 cards now eager + `fetchpriority="high"`. Deliberately rejected a JS-driven crossfade after tracing both directions to a worse failure mode than a plain pop-in (see DECISIONS.md).
  - **B3 (no order/message notifications)**: the underlying `app_notifications` rows were already being written correctly on order placement, order status changes, and every new message — the website simply had no bell, unread count, or list. Built all three, reusing the existing 20s-poll pattern.
  - **B4 (no seller-verification notification)**: verify/reject in the admin panel now send both an in-app notification and an email (rejection includes the reason).
  - **B5 (no upload progress)**: verified already fully correct — both `productMediaManager` and `shopLogoUploader` have real XHR `onprogress` handlers and correctly-bound progress-bar markup, confirmed via a passing end-to-end AJAX test. No code change.
  - **B6 ("Other" missing from homepage category icons)**: the grid hides any category with zero currently-visible products — correct for a real category, but "Other" is a permanent catch-all with no products of its own most of the time by definition, so the same rule silently erased it. Exempted it from that one filter.
  - `php artisan test`: 551/551 passing (all new work this round, on top of the 2026-09-08 batch below). Committed in 16 logical groups (see git log) rather than the usual single end-of-round commit, at the user's explicit request.
  - **Where this stops**: B7 (email verification end-to-end test against production SMTP), B8 (verification badge parity — likely already fine per Part A's website-badge-color fix last round, needs re-checking against the app specifically), B9 (Google sign-in — C1 from the 2026-09-08 round already fixed a real bug here and documented exact Google Cloud Console values in `BLOCKERS.md`; client reports it's still not working, needs re-diagnosis against whatever's actually live), B10 (favicon — already done last round per B4/2026-09-08, needs re-verifying against this round's specific ask: check `public/images/` for a client-supplied icon file first). **Part C this round is a bigger ask than the Cars Make/Model already shipped 2026-09-08** — adds a third dependent dropdown (Year, 1990-current) on top of the existing Make→Model mechanism, not yet built. **Part D is unstarted**: full product/user CRUD audit (create, restore-from-trash, bulk-delete already exist from 2026-09-08's C2 — edit-every-field and the exact admin capabilities the client lists need auditing against what's actually there), and the seller-verification-queue layout rebuild (two-column, capped/lightboxed images, prominent Approve/Reject, oldest-first) is unstarted.
